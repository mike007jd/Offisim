{
  "name": "@offisim/desktop",
  "version": "1.0.0-rc.1",
  "private": true,
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "prebuild": "node ../../scripts/check-platform-tauri-origin-sync.mjs && node ../../scripts/check-attachment-capabilities.mjs",
    "build": "tauri build --bundles app",
    "build:frontend": "pnpm -C ../.. build:runtime-deps && pnpm -C ../.. build:claude-agent-host && pnpm -C ../.. build:codex-agent-host && pnpm -C ../.. --filter @offisim/web... build",
    "typecheck": "echo 'no TS in desktop — Rust only'"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "license": "MIT"
}

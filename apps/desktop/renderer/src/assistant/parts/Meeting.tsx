/** Meeting follow-up has no live producer in the Pi Agent runtime yet. Keep the
 *  reserved pitbar slot honest instead of projecting fake meeting state from
 *  conversation runs. */
export function MeetingTray() {
  return null;
}

# Assistant Runtime Boundary

Use assistant-ui runtime primitives here, adapted to Offisim's existing desktop runtime and event model.

Do not replace Offisim model transport, local tool execution, or harness ownership with assistant-ui examples. The assistant-ui layer is presentation/runtime glue for the Office rail.

/**
 * Re-export the real SettingsPage from @offisim/ui-office/web.
 * This file exists so the lazy-load import in WorkspaceRouter resolves correctly.
 */
export { SettingsPage as default } from '@offisim/ui-office/web';

/**
 * Re-export the real ActivityLogPage from @offisim/ui-office/web.
 * This file exists so the lazy-load import in WorkspaceRouter resolves correctly.
 */
export { ActivityLogPage as default } from '@offisim/ui-office/web';

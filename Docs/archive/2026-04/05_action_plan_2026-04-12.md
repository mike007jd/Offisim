# Action Plan — 2026-04-12

Phase 5 consolidated action plan. Merges Phase 2 locked cleanup checklist with Phase 3 (robustness) + Phase 4 (feature sanity) findings into an ordered commit sequence.

**Rules of execution (per locked framing F5)**:
- Each commit targets ONE finding ID or one tight logical group
- Each commit is small, surgical, reversible
- Run `pnpm --filter '!@offisim/desktop' --filter '!@offisim/launcher' typecheck` after each commit
- Auto-pause every 5 commits for user status report
- Never bypass hooks (`--no-verify`)
- Do NOT touch: LLM adapters, `a2a/**`, `gateway/openclaw-client.ts`, `openclaw-client` backend, any sleeping asset
- Do NOT add architecture docs, `@deprecated` tags, or expand scope

---

## Commit sequence (safest first)

### Batch 1 — doc / comment only (zero typecheck risk)

| # | Finding | Files | Nature |
|---|---|---|---|
| 1 | L1 | `apps/web/src/lib/tauri-seed.ts:1-6` — delete first docstring block (the one that claims it seeds companies + employees in a transaction, all three claims false) | Delete stale docstring |
| 2 | L3 | `packages/ui-office/src/components/studio/StudioPage.tsx:236` + `StudioToolbar.tsx:260` — delete both TODO comments about "Skill §14 timings" (referenced doc no longer exists; animation polish is Known Debt non-goal) | Delete 2 TODO comments |
| 3 | M3 | `packages/ui-office/src/runtime/offisim-runtime-context.tsx:42` — fix stale comment: "null in Tauri mode or when runtime is not yet ready" → "null only during bootstrap / in tauri-runtime-lite mode" | 1 comment edit |
| 4 | M2 | `packages/renderer/src/tokens/departments.ts:4-8` — delete TODO block about migrating roleSlugs to shared-types; decision: do NOT migrate (out of audit scope), delete the TODO | Delete 5-line comment block |
| 5 | CLAUDE.md | Add one-line addendum in Gotchas section: "`packages/core/src/a2a/` 和 `packages/core/src/gateway/openclaw-client.ts` 是外派 agent 接入的扩展点代码（未来接外部 agent 用），当前未启用也无 UI 入口。核心员工 runtime 是 `anthropic-adapter` / `openai-adapter` / `subscription-adapter (ACP)`，不要混淆。" | 1-line doc add |

**After Batch 1**: run `pnpm typecheck` once (should be clean — all doc changes). Auto-pause for user status.

---

### Batch 2 — dead code removal (interface-stable deletes)

| # | Finding | Files | Nature |
|---|---|---|---|
| 6 | L2 | `packages/core/src/browser.ts:279` — drop `MockToolExecutor` re-export from browser bundle. Keep `index.ts` re-export for test utilities | 1 export removal |
| 7 | H1 | `packages/core/src/services/thread-fork-service.ts` — delete file + `core/src/index.ts:135,281` re-exports + `core/src/browser.ts:148` type re-export (zero production callers, only a `throw new Error('not implemented')` stub) | Delete file + 3 re-exports |
| 8 | C1 part 1 | `packages/ui-office/src/hooks/useOpenClaw.ts` — delete entire file (mock-only hook, user-facing Gateway tab ships broken) | Delete file |
| 9 | C1 part 2 | `packages/ui-office/src/components/openclaw/OpenClawSettings.tsx` + `LobsterInvitePanel.tsx` — delete entire directory | Delete 2 files |
| 10 | C1 part 3 | `packages/ui-office/src/components/settings/SettingsTabNav.tsx:9` + `SettingsContentArea.tsx:1,22` + `SettingsWorkspaceSurface.tsx:24,36,616,638` — remove `openclaw` tab entry + OpenClawSettings import + rendering branches | Tab removal across 3 Settings files |

**After Batch 2**: run typecheck. Critical — this is the riskiest batch because Settings UI wiring changes. Auto-pause after commit 10.

---

### Batch 3 — install-mock removal + renames

| # | Finding | Files | Nature |
|---|---|---|---|
| 11 | C2 | `packages/ui-office/src/lib/install-mock.ts` — delete file. `packages/ui-office/src/hooks/useInstallFlow.ts` — remove `MOCK_INSTALL_PLAN` import + 3 `!installService` fallback branches at :129-138, :185-194, :275-285 → replace with `setStep('error'); setError('Install service not ready — retry once runtime is loaded')` | Delete mock + patch 3 branches |
| 12 | M1 | Rename `apps/web/src/components/workspaces/placeholders/` → `lazy-wrappers/`, update `WorkspaceRouter.tsx:54` comment + imports | Directory rename + import update |
| 13 | H3 | `packages/ui-office/src/components/employees/EmployeeCreatorOverlay.tsx:167-181` — hide 3D avatar toggle button so the "Coming Soon" surface is unreachable (do NOT label it Coming Soon; hide the toggle entirely) | 1 conditional hide |

**After Batch 3**: typecheck. Auto-pause.

---

### Batch 4 — Phase 3 robustness fixes

| # | Finding | Files | Nature |
|---|---|---|---|
| 14 | R2 | `packages/ui-office/src/hooks/useProjectAssignments.ts:25` — add `.catch((err) => { if (!cancelled) console.error('[useProjectAssignments]', err); })` to the `.then` chain | 3-line edit |
| 15 | R3 | `packages/ui-office/src/components/employees/EmployeeEditorDialog.tsx:543` + `components/employees/interview-steps/ModelStep.tsx:65` — replace `\|\| 4096` with explicit NaN guard (`const n = Number.parseInt(e.target.value, 10); Number.isFinite(n) && n > 0 ? n : 4096`) | 2 call sites, parallel edits |
| 16 | R4 | `packages/ui-office/src/components/chat/ChatInput.tsx:357-366` — add `maxLength={8000}` to textarea | 1 attribute add |
| 17 | R1 | **LANDED as commit 1a0d0e6 after Phase 5 reopen.** Two surgical edits: (a) dropped `async` from the 5 drizzle create methods used inside `transact` (employees/sopTemplates/officeLayouts/prefabInstances/zones), each now returns `Promise.resolve(row)` — sync throws bypass the async wrapper and reach the transact callback so better-sqlite3 rolls back; (b) company-template-service stopped calling `zoneService.seedSystemZones` inside transact (its `await`-in-for-loop suspended as a microtask and leaked zones 2..7 outside the commit span). Now it iterates `zoneTemplates` directly and calls `this.zoneRepo.create` per row. Memory + tauri repo impls unchanged. Parity test 37/37, core 918/918, ui-office 328/328. | Low — parity test covers interface, memory/tauri paths untouched |

**After Batch 4**: typecheck + test. Commit 17 (R1) is the highest-risk in Phase 5 — stop and report if parity test fails.

---

### Batch 5 — Phase 4 feature sanity fixes

| # | Finding | Files | Nature |
|---|---|---|---|
| 18 | FS1 | `packages/ui-office/src/hooks/useInterviewWizard.ts:201-238` — add catch clause, expose `error` + `clearError` from hook; `components/employees/InterviewWizard.tsx:56-59` — don't call `onClose()` on submit failure, render error banner in footer | HIGH — real user-facing bug |
| 19 | FS4 | `packages/ui-office/src/hooks/useCompanyCreation.ts:131` + `components/settings/SettingsWorkspaceSurface.tsx:340` — add `if (isCreating/isSaving) return` early-returns at top of each handler | 2 one-line guards |
| 20 | FS2 | `components/settings/SettingsWorkspaceSurface.tsx` — keep `isSaving` true until `OffisimRuntimeStatusContext.version` bumps past pre-save value (observe via `useRuntimeStatus()`) | MEDIUM UX |
| 21 | FS5 | `components/sop/SopViewSurface.tsx` — add "Saving…" / "All changes saved" indicator driven by `updateDefinition` pending state. NOT a beforeunload handler (SOP changes are persisted eagerly per edit, so no dirty tracking needed; only the indicator) | MEDIUM UX |

**After Batch 5**: typecheck + test. Auto-pause.

---

### Post-Phase 5 closures

After the user reopened Phase 5 to push past the "small surgical" bound, the following deferred items were also landed:

- **R1** → commit `1a0d0e6` (see row 17 above)
- **FS6** Studio save re-entry → commit `bffdd04` (source-verified the guard was absent, added `savingRef`)
- **FS7** CompanyCreationWizard Back + Escape → commit `063a582` (confirmed parent doesn't handle Escape, added `onDismiss` with capture-phase handler)
- **FS3** chat abort ceremony reset → commit `6925bc0` (added `execution.aborted` event + useSceneOrchestrator subscription)
- **FS9/FS10/FS11** verified as false positives during follow-up: `StudioPage:485` already resolves the pending save promise on modal cancel; `StudioState.resetForCompany:160-162` already clears `focusedZoneId` + `isEditingZone`; the SOP toast race was already mitigated by the R2 `confirmedSelectedIdRef` pattern.

### Deferred / not-in-Phase-5

These items remain intentionally outside Phase 5 scope:

- **H2** (A2A `tasks/get` `-32001`) — no-fix per Phase 2 revision (sleeping asset)
- **FS8** (SOP DAG pan/zoom persistence) — real but it's an enhancement, not a bug. Post-ship UX.
- **office3d-employees.tsx:311-312** (`isOpenClaw` + `openClawBrandColor` scene hooks) — leave in place per Phase 2 decision (consistent with sleeping-asset approach)
- **Framework-level fix for event ID collisions** (monotonic seq/UUID on RuntimeEvent) — cross-package, post-ship
- **Repository 3-way dedup beyond parity test** — Known Debt non-goal
- **Bundle size, test coverage, Scene V2** — Known Debt non-goals

---

## Verification after all batches

```bash
pnpm --filter '!@offisim/desktop' --filter '!@offisim/launcher' typecheck
pnpm --filter '!@offisim/desktop' --filter '!@offisim/launcher' test
pnpm --filter '!@offisim/desktop' --filter '!@offisim/launcher' lint
```

All three must pass before Phase 5 is considered complete. If any fail, stop and report.

**Smoke verification** (user must run `pnpm dev`):
1. Settings → provider tab still works, no Gateway tab visible
2. Chat → dispatch → scene ceremony (FS3 NEEDS-SMOKE)
3. Employee creator dialog → submit with invalid input → error banner visible (FS1 verification)
4. SOP edit → switch workspace → verify no data loss
5. Install flow → real path during runtime-ready (no MOCK_INSTALL_PLAN)
6. Activity Log → recent events (R1 fix from prior session already deployed)

---

## Summary

| Batch | Commits | Scope | Risk |
|---|---|---|---|
| 1 | 5 | Doc / comment only | Zero |
| 2 | 5 | Dead code removal + Settings UI wiring | Medium (Settings layout) |
| 3 | 3 | install-mock + rename + H3 hide toggle | Low-medium |
| 4 | 4 | Phase 3 robustness fixes | High (R1 transact) |
| 5 | 4 | Phase 4 feature sanity fixes | Medium (FS1 + FS2) |
| **Total** | **21 commits** | | |

21 commits vs Phase 2 preview of 13. Delta = 4 Phase 3 + 4 Phase 4 items. Three Phase 4 items (FS3/FS6/FS7) deferred pending browser smoke or second verification.

Auto-pause points: after 5, 10, 13, 17, 21. User can interrupt / redirect at any pause.

Phase 5 execution begins at Batch 1.

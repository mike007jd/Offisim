# Discovery — 2026-04-12

Ship-grade audit Phase 1 snapshot. Built from 4 parallel Explore scans + git log + CLAUDE.md + MEMORY.md. READ-ONLY facts to anchor Phase 2-5.

Rule: **code > CLAUDE.md > memory** when in conflict. Everything here is source-verified.

---

## Workspace map (actually-shipping view)

### apps/web — React 19 SPA

- Entry: `apps/web/src/main.tsx:110-114` → `apps/web/src/App.tsx:87-641`
- 5 peer workspaces, routed by `WorkspaceRouter` (`App.tsx:366-377`):
  - `office` → `OfficeWorkspaceShellLazy` (lazy) — `packages/ui-office/.../OfficeWorkspaceShell.tsx`
  - `sops` → `SopViewSurface` — `packages/ui-office/src/components/sop/SopViewSurface.tsx:117`
  - `market` → `MarketWorkspacePage` (alias of `MarketPage`) — `packages/ui-office/src/components/marketplace/MarketPage.tsx:240`
  - `activity-log` → `ActivityLogPage` — `packages/ui-office/src/components/events/ActivityLogPage.tsx`
  - `settings` → `SettingsPage` — `packages/ui-office/src/components/settings/SettingsPage.tsx`
- Non-office → `FullPageWorkspaceShell` (`App.tsx:541-547`)
- Legacy overlays intentionally preserved:
  - `EmployeeCreatorOverlay` (`App.tsx:488-497`)
  - `OfficeEditorOverlay` (`App.tsx:501-504`)
  - `MarketplaceDetailOverlay` (`App.tsx:72-78`) — deep-link install only

### apps/desktop — Pure Tauri shell (confirmed)

- Zero npm deps (only `@tauri-apps/cli` devDep). `frontendDist → ../../web/dist` (`tauri.conf.json:10`).
- Tauri plugins registered in `src-tauri/src/lib.rs:142-155`:
  1. `tauri_plugin_sql` — SQLite + 21 migrations (v1→v21) at `lib.rs:6-137`
  2. `tauri_plugin_cors_fetch`
  3. `tauri_plugin_deep_link` (`offisim://`)
  4. `mcp_bridge::init()` (custom, desktop-exclusive)
- Custom Tauri commands: `runtime_secret_status/set/clear`, `git_exec`
- MCP bridge commands (`src-tauri/src/mcp_bridge/mod.rs:18-27`): `mcp_list_registered_servers`, `mcp_register_server`, `mcp_unregister_server`, `mcp_connect_registered`, `mcp_call_tool`, `mcp_kill`, `mcp_list_servers`, `mcp_reconnect`
- No desktop-only leak into web bundle — Tauri imports stubbed in browser dev (per CLAUDE.md).

### apps/platform — Hono API (port 4100)

- `app.ts:41` — `optionalAuth` on `*`; per-route `requireAuth` / `requireCreator` middleware
- Route groups (`apps/platform/src/routes/`):
  - `health.ts:7-15` — DB health check, 503 on DB failure (public)
  - `auth.ts` — Better-Auth handler; `POST /register-creator` (`auth.ts:15-76`, `requireAuth`)
  - `market.ts` — public listing/search/reviews/forks/lineage; `PATCH /:id/status` + `POST /:id/reports` require auth
  - `creators.ts` — public creator stats
  - `reviews.ts` — write paths `requireAuth`
  - `publish.ts` — `requireAuth` + rate limited; `/me` returns `{creator:null}` if not-a-creator (exempt from `requireCreator`); drafts CRUD gated by `requireCreator`; **moderation is synchronous in-request** at `publish.ts:251-309` → returns 202 immediately (matches MEMORY.md source-verified fact, no drift)
  - `install.ts` — public version/artifact resolution
  - `me.ts` — `requireAuth` user library
- Rate limiter trusts only `X-Forwarded-For` nth-from-right per `TRUSTED_PROXY_DEPTH` (not `X-Real-IP`) — matches CLAUDE.md

### apps/launcher — Alive (dev/deploy launcher)

- Tauri + React 19 + Tailwind; v1.0.0-rc.1
- `LaunchPanel` / `LogViewer` (max 5000 lines) / `StatusBar`
- IPC: `getLogs`, `getStatus`, `launchMode`, `onLog`, `onProcessExit`, `restartPlatform`, `startPostgresDocker`, `stopMode`

---

## Package inventory (core + data + rendering)

### packages/core — 26 services, LangGraph kernel

**LangGraph nodes** (`packages/core/src/graph/main-graph.ts:322-447`):

- Main: `boss` / `manager` / `pm_planner` / `step_dispatcher` / `employee` (self-looping) / `step_advance` / `employee_direct_setup` / `error_handler` / `hr` / `pm_heartbeat` / `pm_replan` / `boss_summary`
- Meeting subgraph: `meeting_start` / `participant_turn` / `meeting_end` / `meeting_paused` / `meeting_resume` / `meeting_inject`

**Service list**:

1. `agent-context-pack-service.ts` — LLM context assembly (summary + interaction pack)
2. `company-template-service.ts` — templates + zone blueprints
3. `conversation-budget-service.ts` — per-thread message budget
4. `event-consolidator.ts` — dedupe redundant LLM call events
5. `execution-trace-service.ts` — checkpoints + traversal replay
6. `file-history-service.ts` — version history snapshots
7. `git-auto-commit-service.ts` — desktop-only (browser no-op)
8. `interaction-service.ts` — user interaction records
9. `library-service.ts` — markdown doc cache
10. `logger.ts` — structured logging
11. `memory-service.ts` — persistent LLM memory
12. `memory-update-queue-service.ts` — batches memory writes
13. `node-summary-service.ts` — node exec summary cache
14. `notification-bridge.ts` — events → user notifications
15. `orchestration-service.ts` — wraps `graph.stream()`, emits `graph.node.exited`
16. `prefab-service.ts` — 3D asset placements
17. `project-service.ts` — project CRUD
18. `rack-slot-service.ts` — server rack / slot allocation
19. `sop-service.ts` — SOP load/validate
20. `sop-sync-service.ts` — remote SOP sync
21. `thread-fork-service.ts` — **SUSPECT**: `ThreadForkServiceStub` throws `not implemented, Phase 3+` (verify if it's ever called in production path)
22. `tool-telemetry-service.ts` — tool exec metrics
23. `user-memory-service.ts` — LLM-enhanced user memory
24. `workspace-staleness-service.ts` — stale state detection
25. `workstation-tool-resolver.ts` — role → tools mapping
26. `zone-service.ts` — zone registry

**HookRegistry vs EventBus**: both live and distinct (sync vs async) — matches CLAUDE.md, no drift.

### packages/install-core — marketplace install state machine

- `materializer.ts` — CONFIRMED: **only `employee` kind is actually materialized**.
  - `materializer.ts:195` (transacted path) and `:270` (memory fallback) gate `repos.employees.create()` on `if (asset.kind === 'employee')`.
  - All other kinds (`sop`, `company_template`, `office_layout`, `prefab`, `bundle`) create `installed_assets` + `asset_bindings` rows but **skip runtime-entity creation**. These assets exist as metadata only. Matches MEMORY.md claim.
- `install-planner.ts` — kind-uniform pipeline (extractPackage → checkIntegrity → checkCompatibility → resolveBindings). No per-kind routing at plan time.
- `rollback.ts:30-65` — reverse-order delete (bindings → assets → employees → package). `Promise.allSettled` parallel delete per type. Graceful best-effort on partial failure.

### packages/ui-office — surface map

**Full-surface components by feature area**:

| Area | Key files |
|---|---|
| Office scene | `scene/Office3DView`, `scene/Office2DView`, `scene/SceneCanvas` (3D-crash fallback after 2 crashes) |
| Ceremony | `office/MeetingPanel`, `MeetingControls`, `MeetingControlsAutoWired` |
| SOP | `sop/SopViewSurface`, `SopDagCanvas`, `SopSidebar` |
| Marketplace | `marketplace/MarketPage` (only `employee` in INSTALLABLE_KINDS) |
| Settings | `settings/SettingsWorkspaceSurface`, `SettingsPage`, `McpConfigPanel` |
| Company | `company/CompanySelectionPage`, `company-editor-primitives.tsx` |
| Employees | `employees/` — interview wizard steps, editor dialog |
| Chat | `chat/ChatPanel`, `ChatDrawer`, bubbles, decision cards |
| Dashboard | `dashboard/TaskDetailPanel`, cost/metrics, kanban |
| Install | `install/InstallDialog`, `BindingForm`, `ManifestReview`, `SkillReview` |
| Studio | `studio/StudioPage` — raw-UUID zone IDs by design (reparented on save) |

**Key state hooks (owns >1 piece of state)**: `useSceneOrchestrator` (ceremony+motion), `useSops`, `useDashboardMetrics` (11 fields), `useOfficeLayout`, `useCompanyEditor`, `useEmployeeEditor`, `useCompanyCreation`, `useInstallFlow`, `useMeeting`, `useInterviewWizard`, `useSopRuntimeState`.

### packages/db-local — 40 tables (Drizzle + SQLite, desktop)

- Schema: `packages/db-local/src/schema.ts` (873 lines)
- Tables actively written (spot check: all 40 referenced in repos). No DEAD tables.
- Notable groups: companies / workstations / racks / slots / employees / installTransactions / installedPackages / installedAssets / assetBindings / graphThreads / projects / graphCheckpoints / taskRuns / toolCalls / handoffEvents / meetingSessions / runtimeEvents / employeeVersions / llmCalls / memoryEntries / sopTemplates / mcpAuditLog / nodeSummaries / compactSummaries / activeThreadInteractions / interactionHistory / fileHistory / libraryDocuments / prefabInstances / agentEvents / recoveryKnowledge / officeLayouts / zones

### packages/db-platform — 18 tables, 6 migrations (Drizzle + Postgres)

- Migrations: `001_auth_and_creators.sql` → `006_user_library_unique.sql` (6 files, matches MEMORY.md note)
- Better-Auth tables: `baUser`, `baSession`, `baAccount`, `baVerification`
- Marketplace: `creators`, `listings`, `packageVersions`, `listingTags`, `listingPreviews`, `publishDrafts`, `packageLineage`, `moderationJobs`, `moderationFlags`, `reviews`, `userLibrary`, `installReceipts`, `apiTokens`

### packages/shared-types — zero-dep base

- **Branded ID types** (`ids.ts`): `CompanyId`, `EmployeeId`, `TaskId`, `MeetingId`, `InstallTxnId`, `InstalledPackageId`, `InstalledAssetId`, `ListingId`, `PackageId`, `AssetBindingId`, `ReportId`. Compile-time only.
- **SUSPECT** (Phase 2 verify): `persona.ts` types `CommunicationFrequency`, `RiskPreference`, `DecisionStyle` — only 14 refs, mostly in test fixtures and install-mock. May be stale.
- Core modules all actively consumed: `zone.ts`, `zone-resolution.ts`, `roles.ts`, `states.ts`, `events.ts` (50+ payloads), `prefab.ts`, `models.ts`, `interactions.ts`, `project.ts`, `install.ts`, `zone-presets.ts`, `zone-overlap.ts`, `prefab-spatial.ts`, `agent-context-pack.ts`, `sop.ts`.

### packages/renderer

- Exports: state tokens, motion tokens, layout engine (`computeFloorPlan`, `computeRestAreaSeats`), prefab catalog
- `builtin-catalog.ts` → **25 prefab definitions** (workstations, meeting tables, lounge, standing/coffee/reading tables, chairs, filing, whiteboard, plants, bookshelves, server racks 2U/4U, vending, water cooler, compute/infra items)
- **DRIFT** (prefab-spatial gap): `packages/ui-office/src/lib/prefab-spatial.ts` has **18 spatial specs** vs 25 catalog entries. Gap is compute/infrastructure prefabs (no collision/seating needed). Gap is intentional but undocumented in CLAUDE.md. → Phase 2 / Phase 5 low-severity doc item.

### packages/doc-engine — 6 export formats

- `'docx' | 'pdf' | 'pptx' | 'csv' | 'html' | 'txt'`
- **CONTRADICTION to verify in Phase 2**: Agent C said "No cdn.sheetjs.com calls detected; xlsx bundled via npm". CLAUDE.md + MEMORY.md claim "xlsx runtime-pulled from cdn.sheetjs.com". → Phase 2 must grep both `xlsx` imports and `cdn.sheetjs.com` URLs to resolve. If CLAUDE.md is stale, correction goes in Phase 5 action plan.

### packages/asset-schema, registry-client, ui-core

- `asset-schema` — `PackageManifest` + Ajv validation. Consumed by ui-office + install-core + registry-client + db-local.
- `registry-client` — `RegistryClient` API facade + types. Consumed by ui-office marketplace surfaces.
- `ui-core` — 12 component exports (Alert, Badge, Button, Card, Dialog, DropdownMenu, Input, Progress, ScrollArea, Select, Tabs, Textarea, ToastBanner) + `cn` util. Consumed at 45+ sites across ui-office.
- All three ACTIVELY CONSUMED. None DEAD.

---

## Drift from CLAUDE.md / MEMORY.md

| Finding | Source | Reality | Severity |
|---|---|---|---|
| **Prefab catalog vs spatial spec count** | CLAUDE.md: "新增 prefab 必须在两边各加一份" | 25 catalog vs 18 spatial — 7 compute/infra prefabs have no spatial entry by design | LOW (doc gap) |
| **doc-engine xlsx source** | CLAUDE.md Gotchas + MEMORY.md: "doc-engine xlsx 来自 cdn.sheetjs.com 运行时拉" | Agent C says `import * as XLSX from 'xlsx'` from npm, no CDN URL found | NEEDS-VERIFY in Phase 2 (could be stale claim or could be agent missed a lazy loader) |
| **`ThreadForkServiceStub`** | Not mentioned in CLAUDE.md | `throws not implemented, Phase 3+` | NEEDS-VERIFY: does any production path call it? If not, intentional stub. |
| `persona.ts` types (`CommunicationFrequency`, `RiskPreference`, `DecisionStyle`) | Not mentioned | 14 refs total, most in test/mock | SUSPECT-STALE (Phase 2/5) |
| 4 dead path refs in `Docs/archive/2026-04/2026-04-11-runtime-smoke-handoff.md` | Archive file | References deleted `Docs/plans/2026-04-11-runtime-smoke-*.md` | LOW (archive, historical) |

**Previously flagged as drift but confirmed NOT drift**:

- Platform moderation is sync in-request — already in MEMORY.md "Source-Verified Facts" (2026-04-11 audit). Not drift.
- Desktop MCP bridge — already documented. Not drift.
- Install materializer employee-only — already documented. Not drift.
- HookRegistry vs EventBus separation — already documented. Not drift.

---

## Suspect regions (Phase 2+ will investigate, do not touch)

1. `ThreadForkServiceStub` — verify it has zero production callers (only test fixtures), else upgrade to CRITICAL.
2. `packages/shared-types/persona.ts` types — verify staleness (grep for non-test imports).
3. `packages/doc-engine` xlsx loader — determine if CDN claim is real or stale doc.
4. `packages/install-core/materializer.ts` non-employee branches — verify they emit correct warnings and don't silently promise install success for a kind they can't install.
5. Marketplace publish path — already narrowed to employee-only per MEMORY.md. Phase 4 will walk the flow end-to-end.
6. Desktop `git_exec` Tauri command — potential command injection surface; Phase 3 security scan target.
7. `runtime_secret_set` — if it stores secrets, verify it doesn't log them in event bus / activity log.
8. 21 SQLite migrations — not individually reviewed; Phase 3 will spot-check the last 3 for reversibility.
9. `useSceneOrchestrator` dispose path — CLAUDE.md flags 3D crash ≥2 locks 2D; Phase 3 will verify geometry/material/texture dispose.
10. `useDashboardMetrics` has 11 fields — R4 fix already landed but Phase 4 will walk any remaining reset asymmetry.

---

## Active Docs tree inventory

| Path | .md | Status |
|---|---|---|
| `Docs/00_start_here/` | 3 | ACTIVE |
| `Docs/02_contracts_and_schemas/` | 1+ | ACTIVE (OpenAPI, install state, manifest, SQL schemas) |
| `Docs/03_migrations/` | 1+ | ACTIVE |
| `Docs/04_runtime_experience/` | 2 | ACTIVE (GDD + scene state matrix) |
| `Docs/design/` | 1 | ACTIVE (spacing/density tokens) |
| `Docs/archive/2026-04/` | 6 | HISTORICAL |
| `Docs/plans/` | 0 | ARCHIVED (empty after sprint cleanup) |
| `Docs/superpowers/` | 0 | ARCHIVED (empty + .DS_Store) |
| `spec/` (root) | 4 | ACTIVE (PROJECT_CONSTITUTION / ENGINEERING_RULES / UX_RULES / DESIGN_RULES) |

Root `.md`: `README.md`, `CLAUDE.md`, `CODE_OF_CONDUCT.md`, `CHANGELOG.md`, `SECURITY.md` — all active.

---

## Recent git context (last 20 commits)

Ship-readiness convergence sprint (2026-04-11) + cleanup sprint (2026-04-12, −6425 lines) were the last two major arcs. R1-R4 UI fixes landed. Track A (zone-id unification) committed at `591e2d2`. Track B explicitly skipped (`98d469f`). Big cleanup lands as `3936fc0` / `603223e` / `9e7493b` / `e74f8bc`. Latest is `8cfbc8d` (CLAUDE.md zone-id doc clarification).

Phase 2 starts from this baseline.

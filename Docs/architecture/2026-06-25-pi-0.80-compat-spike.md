# Pi 0.79.8 → 0.80.2 compatibility spike (go/no-go)

> **Historical / superseded (2026-07-16):** point-in-time dependency spike,
> not current runtime or version guidance. Use
> [Engine-neutral AI Accounts](./2026-07-13-engine-neutral-ai-accounts.md) and
> current lockfiles/official sources for implementation decisions.

Checked at: 2026-06-25 NZST (npm registry + upstream changelog verified this date)
Rechecked at: 2026-06-29 NZST (`npm view @earendil-works/pi-coding-agent version dist-tags --json`)
Status: accepted — **NO-GO for Mission MVP; GO as a separate low-risk Epic**
Slice: VM-004. This is a SPIKE: it inventories the surface and decides timing. It
does **not** bump the pin or change any runtime code (loop hard rule + PRD §16.5/§16.6:
the upgrade is a separate Epic, never bundled with product/schema/UX work).

## Version reality (evidence, not memory)

`npm view @earendil-works/pi-coding-agent` (2026-06-25; rechecked 2026-06-29):
- Local pin: **`0.79.8`** (`package.json:87`, exact; lockfile resolves agent-core / pi-ai / pi-tui at the same 0.79.8).
- npm `latest`: **`0.80.2`** (published 2026-06-23T22:03Z). `0.80.1` same day; `0.79.10`/`0.79.9` are the last 0.79 line.
- typebox peer pinned `1.1.38` (used by Pi internally + by Offisim's extension schemas).
- No newer-than-0.80.2 release exists as of the 2026-06-29 recheck.

## Offisim's actual Pi SDK surface (the "SDK surface matrix")

Offisim imports from **exactly one** specifier — `@earendil-works/pi-coding-agent` (grep
of scripts/packages/apps: zero `@earendil-works/pi-ai`, zero `/base` entrypoints). The
used exports:

| Export | Used at | How |
|---|---|---|
| `createAgentSession` | `scripts/tauri-pi-agent-host.entry.mjs`, `pi-child-supervisor.mjs` | root + child session; opts cwd/agentDir/authStorage/modelRegistry/sessionManager/model/thinkingLevel/tools/resourceLoader |
| `DefaultResourceLoader` | entry.mjs, child-supervisor | `{cwd,agentDir,settingsManager,extensionFactories,appendSystemPrompt}` + `.reload()` |
| `AuthStorage` | entry.mjs | `AuthStorage.create(authPath)` + `.list()` |
| `ModelRegistry` | entry.mjs | `ModelRegistry.create(authStorage, modelsPath)` + getAll/getAvailable/find/getProviderAuthStatus/getError |
| `SessionManager` | entry.mjs | `continueRecent(cwd,sessionDir)` (root), `inMemory(cwd)` (child) |
| `SettingsManager` | entry.mjs | `SettingsManager.create(cwd, agentDir)` |
| `isToolCallEventType` | entry.mjs | `isToolCallEventType('bash', event)` in the permission gate |

Session runtime surface: `session.subscribe/prompt/abort/dispose/bindExtensions`,
`.sessionId/.sessionFile/.model/.messages/.getLastAssistantText()`; events
`agent_start | message_update(assistantMessageEvent: text_delta/thinking_delta) |
message_end(message.role/content/usage/stopReason/errorMessage) |
tool_execution_start/update/end`; `message.usage = {input,output,cacheRead,cacheWrite,cost:{total},...}`.
Extension surface: `pi.registerTool({name,label,description,parameters(TypeBox),execute})`,
`pi.on('tool_call', (event,ctx)=>({block,reason}))`, `ctx.ui` forwarding context
(select/confirm/input/editor + no-op rest), `session.bindExtensions({uiContext,mode:'rpc'})`.

## 0.80.x documented changes vs Offisim's surface

Upstream `packages/coding-agent/CHANGELOG.md` (fetched 2026-06-25):

| Change (0.80.0 unless noted) | Touches Offisim? | Evidence |
|---|---|---|
| `pi-ai` root global API (`stream`/`complete`/`getModel(s)`/`getProviders`/`registerApiProvider`/`getEnvApiKey`) moved to `@earendil-works/pi-ai/compat` | **NO** | Offisim has zero `@earendil-works/pi-ai` imports (grep) |
| Removed `@earendil-works/pi-ai/base` + `@earendil-works/pi-agent-core/base` entrypoints | **NO** | zero `/base` usage (grep) |
| Renamed agent-core harness shell-opts type `ExecutionEnvExecOptions` → `ShellExecOptions` | **NO** | Offisim's `ShellExecOptions` is its OWN type in `packages/core/src/tools/builtin/types.ts` (name collision, not the Pi type) |
| `session_before_compact`/`session_compact` ext events gained `reason`/`willRetry` (0.79.10/0.80.0) | NO (additive) | Offisim subscribes none of these extension hooks |
| `chat-template` thinking for OpenAI-compat providers (0.79.9) | NO (additive) | — |

**Conclusion:** the documented 0.80.x breaking changes are confined to the low-level
`pi-ai` / `pi-agent-core` entrypoints and a type rename — **none intersect Offisim's
import surface.** Offisim consumes only the high-level `pi-coding-agent` API, for which
the changelog documents no breaking change. The PRD's "0.80.0 has breaking model/provider/
harness API changes" is true of the *packages* but not of *Offisim's usage*.

## Build / bundle coupling

The host is esbuild-bundled (`scripts/build-pi-agent-host.mjs` → `build-agent-host-lib.mjs`,
`bundle:true`, target node20) into `apps/desktop/src-tauri/resources/pi-agent-host.mjs`;
the Rust host spawns that frozen bundle (no runtime npm linkage) and validates
`PI_HOST_PROTOCOL_VERSION` on the `ready` handshake. So a pin bump is a build-time change:
update pin → rebundle → the Rust host loads the new bundle. No wire-protocol change is
implied by the SDK bump itself.

## Decision

- **NO-GO to bundle the 0.80.2 upgrade into Mission MVP.** Keep `0.79.8` pinned through
  M0–M2. Bundling an SDK bump with schema/UX/Mission work violates the loop hard rule and
  PRD §16.5/§16.6 and would muddy attribution if a regression appears.
- **GO as a dedicated, low-risk upgrade Epic** when scheduled. The spike de-risks it: the
  documented breaking changes don't touch Offisim's surface, so the expected work is
  mechanical (bump pin + verify typebox 1.1.38 still satisfies Pi 0.80.2's peer range +
  rebundle + run the full gate matrix). Do NOT trigger it before Mission MVP unless a
  security/critical fix lands only in 0.80.x.

## Upgrade Epic checklist (PRD §16.5 — for when the Epic runs, not now)

1. Lock the 0.79.8 behavioral baseline (capture `pnpm qa:loop` ledger + release `.app` evidence).
2. Bump pin to 0.80.2; refresh lockfile; confirm typebox peer range + node engines (`>=22.19.0`).
3. Verify the surface matrix above against 0.80.2 types (createAgentSession opts, registries, session events, `message.usage.cost.total`, extension `registerTool`/`tool_call`/`ctx.ui`, `bindExtensions`).
4. Rebundle `resources/pi-agent-host.mjs`; confirm `PI_HOST_PROTOCOL_VERSION` handshake still valid.
5. Run all Pi gates: `harness:pi-agent-host`, `check:pi-wire-contract`, `harness:pi-permission`, `cargo test`, `pnpm validate`.
6. Live `delegation` concurrency + `permission`/HITL (ask) + a real z.ai run via release `.app`.
7. Confirm existing on-disk Pi sessions still load (`SessionManager.continueRecent`).
8. Two consecutive clean `qa:loop` runs + release `.app` live scenarios twice.
9. Commit separately; do not mix with Mission schema/UX.

## Consequences

- M0–M2 proceed on 0.79.8. The `AgentHarness`/durable-harness migration (PRD §16.6) remains
  gated behind its own, stricter criteria and is out of scope of this spike.
- This ADR is the baseline evidence for the future upgrade Epic; re-verify version reality at
  that time (Pi may have moved past 0.80.2 after the 2026-06-29 check).

## 1. Stage A — Revert desktop window product floor

- [x] 1.1 Edit `apps/desktop/src-tauri/tauri.conf.json` `app.windows[0].minWidth: 360 → 1024`. Confirm `minHeight` stays at `700` and default `width` × `height` stays at `1280x800`.
- [x] 1.2 Cross-read `apps/desktop/src-tauri/tauri.conf.json` and confirm no other narrow-tier accommodation snuck in alongside the revert (e.g., a smaller default `width`, an `acceptFirstMouse`, or a webview viewport meta override).
- [x] 1.3 `pnpm --filter @offisim/desktop build` to confirm the rebuild succeeds at the new floor (no Tauri config validation error). *(由 5.1 desktop full build 共同验证: Tauri config 无 validation error, Rust release compile + .app + .dmg bundling 全通过, exit 0)*

## 2. Stage B — Origin-sync smoke check script

- [x] 2.1 Create `scripts/check-platform-tauri-origin-sync.mjs` (Node ESM). Reads `apps/desktop/src-tauri/tauri.conf.json`, parses `app.security.csp` string, tokenizes the `connect-src` directive, and extracts the origin set.
- [x] 2.2 Extend the script to read `apps/platform/src/startup.ts`, locate the `export const DEV_DEFAULT_ORIGINS = [ ... ];` literal via regex, and parse out the array of string origins.
- [x] 2.3 Extend the script to read the platform default listen port (start from `apps/platform/src/startup.ts` if a default port constant exists; otherwise hardcode `4100` with a code comment pointing at the source location). Allow `OFFISIM_PLATFORM_PORT` env override per `design.md` Decision 4.
- [x] 2.4 Implement Invariant A check: CSP `connect-src` SHALL include `http://localhost:<port>`, `https://localhost:<port>`, and `tauri://localhost`. On miss, print `FAIL — Invariant A: CSP connect-src missing <origin>; edit apps/desktop/src-tauri/tauri.conf.json` and exit 1.
- [x] 2.5 Implement Invariant B check: `DEV_DEFAULT_ORIGINS` SHALL include `tauri://localhost`. On miss, print `FAIL — Invariant B: platform CORS missing tauri://localhost; edit apps/platform/src/startup.ts` and exit 1.
- [x] 2.6 On pass, print a single confirmation line listing which origins were checked under each invariant and exit 0.
- [x] 2.7 Defensive parsing: if `app.security.csp` is missing/non-string, or if the `DEV_DEFAULT_ORIGINS` literal cannot be located, fail loud with a clear actionable error per `design.md` Risks (do not silently pass).
- [x] 2.8 Self-test: run the script manually against the in-tree config and confirm green output. Then mutate `tauri.conf.json` CSP to remove `localhost:4100` (locally, not committed) and confirm Invariant A failure with the expected message; mutate `startup.ts` to remove `tauri://localhost` and confirm Invariant B failure with the expected message; revert both mutations.

## 3. Stage C — Wire smoke check into desktop and platform prebuilds

- [x] 3.1 Edit `apps/desktop/package.json`: add a `prebuild` script that runs `node ../../scripts/check-platform-tauri-origin-sync.mjs` before the existing build step. Confirm path resolution from the desktop package directory works.
- [x] 3.2 Edit `apps/platform/package.json`: add a `prebuild` script that runs the same node script. Confirm path resolution from the platform package directory works.
- [x] 3.3 Verify pnpm `prebuild` hook runs automatically when invoking `pnpm --filter @offisim/desktop build` and `pnpm --filter @offisim/platform build`. *(platform build实测: prebuild 自动跑并打印绿线, pnpm 10.15.1 默认启用 pre/post hook; desktop 同 runner 同语义, 全量 desktop build 留到 5.1)*
- [x] 3.4 Confirm turbo cache does not skip the prebuild step on a clean run; if needed, add the script path to the desktop / platform `turbo.json` `inputs` so cache invalidates when the script changes. *(用 root turbo.json globalDependencies 把 script + tauri.conf.json + startup.ts 三件加进全局依赖, 任一文件改动让 turbo 缓存全失效, 比 per-package inputs 更小代价更稳妥)*

## 4. Stage D — Documentation contract pointers

- [x] 4.1 Edit `apps/desktop/CLAUDE.md`: replace the (older, scattered) CSP / connect-src口诀 lines with a concise pointer to the spec scenarios. Specifically link to the `desktop-llm-credential-isolation` requirement and name the two invariants.
- [x] 4.2 Edit `apps/platform/CLAUDE.md`: add a one-line entry under Gotchas pointing at the same spec for the CORS↔CSP coupling.
- [x] 4.3 Edit root `CLAUDE.md`: in the responsive / Workspace IA section, add an explicit line stating that narrow tier (`390x844`) verification target is the web SPA in browser, not the desktop release `.app`. Reference the `responsive-app-shell` Requirement.
- [x] 4.4 Update the `apps/platform/src/startup.ts` comment block above `DEV_DEFAULT_ORIGINS` to point at the spec scenarios rather than restating the coupling rule (avoid prose duplication that drifts).

## 5. Stage E — Live verification

- [x] 5.1 `pnpm --filter @offisim/ui-office build && pnpm --filter @offisim/desktop build`. Confirm the new prebuild hook runs and prints the green sync line. *(实测: prebuild 第一行打印 `OK — origin-sync: Invariant A (CSP connect-src) covers [http://localhost:4100, https://localhost:4100, tauri://localhost]; Invariant B (platform CORS) covers [tauri://localhost].`，全 monorepo 重建 + Rust release compile 34s + Bundling Offisim.app + Offisim_0.0.1_aarch64.dmg 全产出，exit 0)*
- [x] 5.2 Open the rebuilt release `.app`. Confirm the OS window cannot be dragged below `1024px` width (per the new `responsive-app-shell` floor scenario). Take a screenshot or note as live evidence. *(实测: release `.app` 路径 `apps/desktop/src-tauri/target/release/bundle/macos/Offisim.app`；Computer Use 打开 `com.offisim.desktop` 后用 System Events 强制设置窗口 `{800,720}`，系统实际回报 `1024,720`，证明 OS 层不能低于 1024px 宽。)*
- [x] 5.3 With `pnpm --filter @offisim/platform dev` running on `4100`, exercise Market (browse, install path) inside the release `.app`. Confirm fetch to `localhost:4100` works (Invariant A end-to-end), no CSP violation in the webview console. *(实测: release `.app` 内 Market 成功加载 6 条官方 listing；打开 `Sample Marketing Strategist` 详情，`Approve & Continue` 后弹出 `Installation Complete — The package has been installed successfully.`；截图 `/tmp/offisim-release-install-complete.png`；本地落点确认写入 `~/Library/Application Support/com.offisim.desktop/vault/companies/1a001c00-e7e2-45a3-af0f-8a287d53622b/employees/sample-marketing-strategist/employee.md`，未见 CSP 阻断。)*
- [x] 5.4 Confirm Invariant B end-to-end: in the same release `.app` session, open browser devtools, confirm the platform response to a Market fetch carries `Access-Control-Allow-Origin: tauri://localhost` (or matches the `*` / origin echo path used in dev). If platform responses cannot be inspected from the release webview directly, capture from `pnpm --filter @offisim/platform dev` server-side logs as fallback evidence. *(实测: `pnpm --filter @offisim/platform dev` 启动后 `GET http://localhost:4100/v1/market/search?per_page=6` 携带 `Origin: tauri://localhost` 返回 `200 OK`；header 包含 `access-control-allow-origin: tauri://localhost`、`vary: Origin`；响应 `items.length = 6`。)*
- [x] 5.5 Negative test: temporarily edit `tauri.conf.json` to remove `http://localhost:4100` from CSP `connect-src`. Run `pnpm --filter @offisim/desktop build`. Confirm the prebuild check exits non-zero with the Invariant A failure message. Revert. *(实测: `pnpm --filter @offisim/desktop build` 在 prebuild 阶段秒级 fail，输出 `FAIL — Invariant A: CSP connect-src missing http://localhost:4100; edit apps/desktop/src-tauri/tauri.conf.json` exit 1，未触发 Rust compile，已还原)*
- [x] 5.6 Negative test: temporarily edit `apps/platform/src/startup.ts` to remove `tauri://localhost` from `DEV_DEFAULT_ORIGINS`. Run `pnpm --filter @offisim/platform build`. Confirm the prebuild check exits non-zero with the Invariant B failure message. Revert. *(实测: `pnpm --filter @offisim/platform build` 在 prebuild 秒级 fail，输出 `FAIL — Invariant B: platform CORS missing tauri://localhost; edit apps/platform/src/startup.ts` exit 1，已还原)*
- [x] 5.7 Web SPA narrow tier sanity check: `pnpm --filter @offisim/web dev`, browser DevTools resize to `390x844`, confirm narrow drawer / Personnel / Market still render correctly (no regression from the desktop-floor revert; this is the web verification path per the new spec Requirement). *(实测: chrome-devtools-mcp 启 web dev → navigate localhost:5176 → resize 390x844 → 截图 `/tmp/web-narrow-5.7.png`：紧凑 header (hamburger + Studio Edit Co + overflow)，Personnel/Collaboration 左右 rail 折叠成竖排，3D office 中间渲染，底部 status + chat 收起，无横向溢出；console 仅有一条 dev-only memory deliverable test data warn，无 CSP / runtime error)*
- [x] 5.8 Run repository gates: `pnpm --filter @offisim/web typecheck && pnpm --filter @offisim/web build`, `pnpm --filter @offisim/platform typecheck`. Confirm no type or build regressions from the docs / config edits. *(实测: web typecheck exit 0; web build exit 0 (vite 7.54s); platform typecheck exit 0)*

## 6. Stage F — Archive readiness

- [x] 6.1 Re-read all 5 modified specs / docs and confirm wording is consistent across spec ↔ CLAUDE.md ↔ design.md ↔ proposal.md (Archive Gate three-check item 3). *(跨读发现 root CLAUDE.md L151 还有 stale `Tauri release CSP 与 platform dev origin 必须同步` 旧口诀，已替换为引用 spec + Invariant A/B 表述与新 doc 统一)*
- [x] 6.2 `openspec validate fix-tauri-release-csp-and-revert-narrow-minwidth --strict` passes. *(实测: `Change 'fix-tauri-release-csp-and-revert-narrow-minwidth' is valid` exit 0)*
- [x] 6.3 Confirm protocols ledger row 4 (Tauri 2) does not need an update — this change is config + build script, not a Tauri SDK upgrade. If anything in the row's `Repo claim` text becomes inaccurate (e.g., it claims a different `minWidth`), correct it. *(grep 检查 row 4 不引用 minWidth 或本 change 涉及字段，无需更新)*

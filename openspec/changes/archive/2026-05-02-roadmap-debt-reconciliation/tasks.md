## 1. Roadmap debt rollup

- [x] 1.1 Add a dedicated roadmap debt ledger with `True debt`, `Docs/process cleanup`, and `Historical excluded` sections.
- [x] 1.2 Record standard classification labels for cancelled, deferred, superseded, docs-drift, and bookkeeping-only items.

## 2. Canonical doc sync

- [x] 2.1 Update the `employee-node-boundaries` and `typed-json-field-parsers` contracts to match current code.
- [x] 2.2 Backfill archived `Purpose` text and sync package `CLAUDE.md` files away from legacy skill/runtime wording.

## 3. Historical notes

- [x] 3.1 Annotate the archived task files that were being misread as active roadmap debt.
- [x] 3.2 Verify this cleanup stays scoped to docs/openspec files and does not touch the in-progress provider/Codex implementation track.

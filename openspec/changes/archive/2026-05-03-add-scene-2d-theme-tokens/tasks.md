## 1. Token foundation: extend `Scene3DColors`

- [x] 1.1 Add the 30 new 2D-canvas-only fields to the `Scene3DColors` interface in `packages/ui-core/src/tokens/colors-3d.ts`, grouped under a `// 2D canvas-only fields` comment block: `canvasBackground`, `canvasGrid`, `deskSurface`, `deskScreen`, `deskBezel`, `pillBg`, `pillBgStroke`, `pillText`, `dotRing`, `nameLabelMuted`, `meetingBubbleBg`, `meetingBubbleStroke`, `meetingBubbleTitle`, `meetingBubbleParticipantText`, `meetingBubbleWaitingText`, `meetingBubbleExtraText`, `managerMarkerFill`, `managerMarkerStroke`, `managerMarkerLabel`, `selectionRing2D`, `dragGhostShadow`, `prefabSilhouetteDegraded`, `stateBadgeBg`, `stateBadgeStroke`, `stateBadgeText`, `stateBadgeBgBlocked`, `stateBadgeStrokeBlocked`, `stateBadgeTextBlocked`, `stateBadgeBgSuccess`, `stateBadgeStrokeSuccess`, `stateBadgeTextSuccess`.
- [x] 1.2 Populate `DARK_SCENE_3D` with byte-equivalent dark values from today's hard-coded literals: `canvasBackground: '#020617'`, `canvasGrid: 'rgba(148, 163, 184, 0.06)'`, `deskSurface: 'rgba(30, 41, 59, 0.6)'`, `deskScreen: 'rgba(14, 165, 233, 0.5)'`, `deskBezel: 'rgba(51, 65, 85, 1)'`, `pillBg: '#1e293b'`, `pillBgStroke: 'rgba(255, 255, 255, 0.08)'`, `pillText: '#f8fafc'`, `dotRing: '#1e293b'`, `nameLabelMuted: '#f8fafc'`, `meetingBubbleBg: 'rgba(0, 0, 0, 0.65)'`, `meetingBubbleStroke: 'rgba(255, 255, 255, 0.10)'`, `meetingBubbleTitle: 'rgba(255, 255, 255, 0.85)'`, `meetingBubbleParticipantText: 'rgba(255, 255, 255, 0.35)'`, `meetingBubbleWaitingText: 'rgba(255, 255, 255, 0.55)'`, `meetingBubbleExtraText: 'rgba(255, 255, 255, 0.45)'`, `managerMarkerFill: 'rgba(168, 85, 247, 0.15)'`, `managerMarkerStroke: '#a855f7'`, `managerMarkerLabel: '#ffffff'`, `selectionRing2D: '#6366f1'`, `dragGhostShadow: 'rgba(0, 0, 0, 0.3)'`, `prefabSilhouetteDegraded: 'rgba(100, 116, 139, 0.1)'`, `stateBadgeBg: 'rgba(0, 0, 0, 0.7)'`, `stateBadgeStroke: 'rgba(255, 255, 255, 0.1)'`, `stateBadgeText: 'rgba(255, 255, 255, 0.8)'`, `stateBadgeBgBlocked: 'rgba(239, 68, 68, 0.25)'`, `stateBadgeStrokeBlocked: 'rgba(239, 68, 68, 0.4)'`, `stateBadgeTextBlocked: '#fca5a5'`, `stateBadgeBgSuccess: 'rgba(34, 197, 94, 0.25)'`, `stateBadgeStrokeSuccess: 'rgba(34, 197, 94, 0.4)'`, `stateBadgeTextSuccess: '#86efac'`.
- [x] 1.3 Populate `LIGHT_SCENE_3D` with theme-correct light values that preserve visual semantics: `canvasBackground: '#e8edf4'` (matches existing `LIGHT_SCENE_3D.sceneBackground`), `canvasGrid: 'rgba(71, 85, 105, 0.10)'`, `deskSurface: 'rgba(226, 232, 240, 0.85)'`, `deskScreen: 'rgba(2, 132, 199, 0.55)'`, `deskBezel: 'rgba(148, 163, 184, 0.95)'`, `pillBg: '#ffffff'`, `pillBgStroke: 'rgba(15, 23, 42, 0.10)'`, `pillText: '#0f172a'`, `dotRing: '#ffffff'`, `nameLabelMuted: '#0f172a'`, `meetingBubbleBg: 'rgba(248, 250, 252, 0.95)'`, `meetingBubbleStroke: 'rgba(15, 23, 42, 0.10)'`, `meetingBubbleTitle: 'rgba(15, 23, 42, 0.90)'`, `meetingBubbleParticipantText: 'rgba(15, 23, 42, 0.50)'`, `meetingBubbleWaitingText: 'rgba(15, 23, 42, 0.65)'`, `meetingBubbleExtraText: 'rgba(15, 23, 42, 0.55)'`, `managerMarkerFill: 'rgba(124, 58, 237, 0.18)'`, `managerMarkerStroke: '#7c3aed'`, `managerMarkerLabel: '#0f172a'`, `selectionRing2D: '#2563eb'` (matches existing `LIGHT_SCENE_3D.selectionRing`), `dragGhostShadow: 'rgba(15, 23, 42, 0.20)'`, `prefabSilhouetteDegraded: 'rgba(100, 116, 139, 0.18)'`, `stateBadgeBg: 'rgba(255, 255, 255, 0.90)'`, `stateBadgeStroke: 'rgba(15, 23, 42, 0.12)'`, `stateBadgeText: 'rgba(15, 23, 42, 0.80)'`, `stateBadgeBgBlocked: 'rgba(220, 38, 38, 0.18)'`, `stateBadgeStrokeBlocked: 'rgba(220, 38, 38, 0.45)'`, `stateBadgeTextBlocked: '#991b1b'`, `stateBadgeBgSuccess: 'rgba(22, 163, 74, 0.18)'`, `stateBadgeStrokeSuccess: 'rgba(22, 163, 74, 0.45)'`, `stateBadgeTextSuccess: '#15803d'`.
- [x] 1.4 `pnpm --filter @offisim/ui-core build` so `@offisim/ui-office` picks up the new types.
- [x] 1.5 Verify dark byte-equivalence: assert `DARK_SCENE_3D.canvasBackground === '#020617'`, `DARK_SCENE_3D.pillBg === '#1e293b'`, `DARK_SCENE_3D.managerMarkerStroke === '#a855f7'`, `DARK_SCENE_3D.selectionRing2D === '#6366f1'` are byte-equivalent to today's literals (manual eyeball — no automated test surface).

## 2. Define `SceneCanvasPalette` and extend `FrameContext`

- [x] 2.1 In `packages/ui-office/src/components/scene/office-2d-canvas-renderer.ts`, define `export type SceneCanvasPalette = Pick<Scene3DColors, …>` listing exactly the 30 2D-canvas-only fields (so layer / helper fns get an explicit narrow contract).
- [x] 2.2 Extend the `FrameContext` interface (in the same file) with `palette: SceneCanvasPalette` (non-optional).
- [x] 2.3 Update `drawScene(ctx, snapshot, frame)` so the orchestrator does not itself paint colors; it only routes to layer fns. Pass through the new `frame.palette`.
- [x] 2.4 Wire the palette in `Office2DCanvasView.tsx` / `useCanvasRedrawLoop.ts`: read `useSceneColors()` once, pick the 2D fields into a `SceneCanvasPalette` and stuff into the `FrameContext` constructed each rAF tick.
- [x] 2.5 Migrate `STATUS_COLORS` in `office-2d-canvas-renderer.ts` (the local `Record<string, string>`) to derive from `STATE_COLORS_LIGHT` / `STATE_COLORS_DARK` numeric tokens — convert the active variant to a hex-string map via `('#' + n.toString(16).padStart(6, '0'))` once per palette build. Keep the `getStatusColor(state)` API unchanged.

## 3. Migrate pure helpers

- [x] 3.1 `canvas-primitives.ts`: remove `bgFill = '#1e293b'` default in `drawAvatarCircle`; require `bgFill` from caller. Remove `'#1e293b'`, `'rgba(255, 255, 255, 0.08)'`, `'#f8fafc'` literals from `drawNamePill`; add `palette: Pick<SceneCanvasPalette, 'pillBg' | 'pillBgStroke' | 'pillText'>` parameter (or pass three separate string params) and consume them.
- [x] 3.2 Remove the `// raw-hex-allowed-file:` header from `canvas-primitives.ts`.

## 4. Migrate the 7 canvas-layers files

- [x] 4.1 `canvas-layers/draw-background.ts`: replace `BACKGROUND_COLOR = '#020617'` and `GRID_COLOR = 'rgba(148, 163, 184, 0.06)'` with `frame.palette.canvasBackground` and `frame.palette.canvasGrid`. Remove the `// raw-hex-allowed-file:` header (none currently — this file does not have one). Verify by reading top line.
- [x] 4.2 `canvas-layers/draw-zones.ts`: this file currently uses `zone.accentColor` (snapshot-data, token-derived upstream) and font-only literals. No color literals to migrate. No header to remove. Confirm and document.
- [x] 4.3 `canvas-layers/draw-prefabs.ts`: replace `'rgba(100, 116, 139, 0.1)'` with `frame.palette.prefabSilhouetteDegraded`. Remove `// raw-hex-allowed-file:` header (none — confirm).
- [x] 4.4 `canvas-layers/draw-employees.ts`: replace `drawDeskBackgrounds`'s `'rgba(30, 41, 59, 0.6)'` / `'rgba(14, 165, 233, 0.5)'` / `'rgba(51, 65, 85, 1)'` with `palette.deskSurface` / `palette.deskScreen` / `palette.deskBezel`; replace `drawEmployeeNode`'s `'#1e293b'` (dot ring stroke) and `'#f8fafc'` (degraded name text) with `palette.dotRing` / `palette.nameLabelMuted`; replace `drawStateBadge`'s 7 literals with `palette.stateBadgeBg{Blocked,Success,}` / `palette.stateBadgeStroke{Blocked,Success,}` / `palette.stateBadgeText{Blocked,Success,}`; pass `frame.palette` through to `drawAvatarCircle` (`bgFill: palette.pillBg`) and `drawNamePill` (the three pill fields). Remove the `// raw-hex-allowed-file:` header.
- [x] 4.5 `canvas-layers/draw-ceremony.ts`: replace `drawManagerMarker`'s `'rgba(168, 85, 247, 0.15)'` / `'#a855f7'` / `'#ffffff'` with `palette.managerMarkerFill` / `palette.managerMarkerStroke` / `palette.managerMarkerLabel`; replace `drawMeetingBubble`'s `'rgba(0, 0, 0, 0.65)'` / `'rgba(255, 255, 255, 0.10)'` / `'rgba(255, 255, 255, 0.85)'` / `'rgba(255, 255, 255, 0.35)'` / `'rgba(255, 255, 255, 0.55)'` / `'rgba(255, 255, 255, 0.45)'` with `palette.meetingBubble{Bg,Stroke,Title,ParticipantText,WaitingText,ExtraText}`. Remove the `// raw-hex-allowed-file:` header.
- [x] 4.6 `canvas-layers/draw-interactions.ts`: replace `'#6366f1'` (selection stroke) with `palette.selectionRing2D`; the `emp.statusColor` hover stroke comes from snapshot data (token-derived upstream) and is already token-clean. Remove the `// raw-hex-allowed-file:` header.
- [x] 4.7 `canvas-layers/draw-drag-overlay.ts`: replace `'rgba(0, 0, 0, 0.3)'` (ghost shadow) with `palette.dragGhostShadow`; pass `palette.pillBg` etc. through to `drawAvatarCircle` / `drawNamePill`. Remove the `// raw-hex-allowed-file:` header (none — confirm).

## 5. Migrate the orchestrator + the remaining barrel

- [x] 5.1 `office-2d-canvas-renderer.ts`: ensure no inline color literals remain (the `STATUS_COLORS` map gets removed in 2.5 and replaced with token-derived map). Remove the `// raw-hex-allowed-file:` header.
- [x] 5.2 `office-2d-render-registry.ts`: read the file; if it contains color literals (likely default prefab silhouettes), audit and either move to tokens or document each literal as data (e.g., `prefab.iconColor` if it's data-driven). Remove the `// raw-hex-allowed-file:` header. (May require adding `palette` plumbing into `getPrefabDrawFn` if registry-side prefabs paint colors directly.)
- [x] 5.3 `Office2DCanvasView.tsx`: replace `bg-[#020617]` × 2 with `bg-[var(--color-scene-background)]` or read inline from `useSceneColors().sceneBackground`; verify the `text-white text-red-400 text-gray-400` Tailwind classes in the canvas-error fallback are token-driven (Tailwind utilities resolved via `@theme inline` are already token-driven and out of `tokens:lint-hex` scope). Remove the `// raw-hex-allowed-file:` header.

## 6. Theme-change redraw trigger

- [x] 6.1 In `useCanvasRedrawLoop.ts`, add a `useEffect` watching `resolvedTheme` from `useTheme()` that sets `needsRedrawRef.current = true`. Implemented as palette-identity watch (design.md decision 3 alternative): `palette = useMemo(pickSceneCanvasPalette(sceneColors))` flips identity on theme change because `useSceneColors()` returns a different `Scene3DColors` reference per theme; a dedicated `useEffect` on `[palette, sceneColors]` dirties `needsRedrawRef.current = true` and refreshes the palette refs the rAF callback reads. `useTheme()` is called inside `useSceneColors()` itself — no extra `resolvedTheme` plumbing.
- [ ] 6.2 Verify in dev: theme switch via Settings flips the canvas within one rAF tick with no manual viewport interaction needed. *(Deferred — covered by Section 9 live verify on Tauri release `.app`. Architectural soundness: palette useMemo deps on sceneColors → theme flip → palette identity changes → useEffect dirties needsRedrawRef → next rAF reads new palette.)*

## 7. Verify token lint gate

- [x] 7.1 Run `pnpm tokens:lint-hex` — exit code 0, no violations.
- [x] 7.2 Confirm the 11 files listed in the spec do not carry `// raw-hex-allowed-file:` headers anymore (`grep -l '^// raw-hex-allowed-file:' packages/ui-office/src/components/scene/Office2DCanvasView.tsx packages/ui-office/src/components/scene/office-2d-canvas-renderer.ts packages/ui-office/src/components/scene/office-2d-render-registry.ts packages/ui-office/src/components/scene/canvas-primitives.ts packages/ui-office/src/components/scene/canvas-layers/*.ts | wc -l` returns `0`).

## 8. Build + typecheck

- [x] 8.1 Run the dependency-ordered serial build per CLAUDE.md: `pnpm --filter @offisim/shared-types build` → `pnpm --filter @offisim/ui-core build` → `pnpm --filter @offisim/core build` → `pnpm --filter @offisim/ui-office build` → `pnpm --filter @offisim/web build`. All exit 0.
- [x] 8.2 Run `pnpm typecheck`. Exit 0.
- [x] 8.3 Run `pnpm lint`. The 14 files touched by this change pass `biome check` cleanly. Repo-wide `pnpm lint` exits non-zero due to 48 pre-existing errors in unrelated files (`apps/web/src/lib/skill-install-env.ts`, `packages/core/src/agents/boss-node.ts`, `packages/core/src/skills/skill-scanner.ts`, `packages/core/src/skills/skill-source-resolvers/{git,upload}.ts`, `scripts/check-platform-tauri-origin-sync.mjs`, `scripts/provider-source-registry/lib/catalog.mjs`); verified by stashing this change — `main` also exits non-zero with the same set. Out of scope here.

## 9. Live verify (Tauri release `.app`, real runtime)

**Status (2026-05-03 — final, archived with partial evidence)**: prior full-phase evidence screenshots were invalidated because they were captured from unrelated foreground apps instead of the Offisim Tauri release `.app`; they were deleted from `.live-verify/add-scene-2d-theme-tokens/full-phase/`, and stale root-level png evidence was also removed. The earlier "light scene blank" conclusion is withdrawn as untrusted evidence. Computer Use then re-ran with the Offisim release window pinned (`App=com.offisim.desktop`, `Window: "Offisim"`, `URL: tauri://localhost/?view=2d`) and verified 9.1–9.5 cleanly in the release app. 9.6 partial (six of seven named phases captured across light+2D, four across dark+2D), 9.7 and 9.8 deferred. User explicitly authorized archive with deferred 9.6/9.7/9.8 to avoid burning further cycles on evidence form when the behavior contract is already exercised. 9.7's "no employee drop" finding is a release-app drop-target regression separate from this change's scope and is split into a follow-up backlog item.

**Capture protocol (forward rule)**: every release-`.app` verify screenshot SHALL include either (a) the Offisim window chrome (title bar / traffic-light buttons / Header with `3D|2D` toggle and Office tab visible) or (b) Computer Use state proving `frontmostApp = com.offisim.desktop`. This rule is recorded here so future verify runs cannot regress to wrong-app captures.

- [x] 9.1 Build the desktop release `.app`: `pnpm --filter @offisim/ui-office build && pnpm --filter @offisim/desktop build`.
- [x] 9.2 Launch the precise current-worktree `.app` (do not `open -b com.offisim.desktop`); load a company with active ceremony.
- [x] 9.3 Verify dark theme: 2D canvas background, grid, employee desks, name pills, manager marker, meeting bubble, drag ghost, selection ring, hover ring, state badges, prefab silhouettes all render byte-equivalent to pre-change behavior. Computer Use verified `dark+2D` and `dark+3D` render normally in the release app.
- [x] 9.4 Switch to light theme via Settings → Runtime tab; verify within one rAF tick: every element above flips to light-tuned values, all text remains legible (visual contrast check), no element is invisible against same-color background. Computer Use verified `light+2D` and `light+3D` render normally in the release app; no light blank scene reproduced.
- [x] 9.5 Computer Use release evidence: System theme follows the current OS Light setting; both Office 3D and 2D render correctly under System mode in the release app. (User declined OS-Appearance flip for the dark→light follow-on under System, since the same code path is exercised by the explicit-theme path covered in 9.3 / 9.4.)
- [~] 9.6 Computer Use partial release evidence: dark+2D reached `ANALYZING → ROUTING → PLANNING → READY`; light+2D reached `ANALYZING → ROUTING → PLANNING → EXECUTING → DELIVERING → READY`, with visible employee movement/working badges and theme-correct 2D canvas colors during each transition. **Deferred** — full named 7-phase coverage (`gathering` and missing `executing/delivering` for dark) was not driven; archived with partial evidence. The token migration's per-phase color contract is exercised by the captured states (color SSOT is the same per-phase data path regardless of which phase fires), so the residual gap is evidence-coverage, not behavior risk.
- [ ] 9.7 **Deferred — separate product bug surfaced**. Computer Use release evidence: dragging / panning the 2D canvas itself in light theme stayed visually correct (token colors fine), but dragging an employee card / employee node onto the canvas in the release app did not form a valid drop target. This is a release-app drop-target regression unrelated to the 2D theme token migration; tracked separately as a follow-up (see MEMORY.md Active Backlog).
- [ ] 9.8 **Deferred** — full 14-capture set (dark + light × 7 ceremony phases) not produced; intermediate full-phase captures from the Computer Use run were earlier invalidated due to wrong-app focus and removed. Archived without the 14-capture set. The 2D theme token migration's behavior is covered by 9.3 / 9.4 / 9.5 / partial 9.6 verification; the missing screenshots are evidence form, not coverage of new code paths.

## 10. Spec / docs / memory sync (Archive Gate prep)

- [ ] 10.1 Confirm the new `openspec/specs/scene-2d-theme-tokens/` capability folder is created at archive time; the delta in this change is the source-of-truth for that capability's first spec. *(Archive-time concern; validated by 10.5 strict pass.)*
- [ ] 10.2 Confirm `openspec/specs/design-token-foundation/spec.md` reflects the modified Scene3DColors fields and the narrowed `raw-hex-allowed-file` exemption rule at archive time. *(Archive-time concern; the change's `specs/design-token-foundation/spec.md` modify-delta is in place.)*
- [x] 10.3 Update `packages/ui-office/CLAUDE.md` "UI / Scene / 3D" section: append a one-liner that 2D canvas color SSOT is `useSceneColors()` per `scene-2d-theme-tokens` capability; update the existing 3D material SSOT bullet to note 2D is now also under-token.
- [x] 10.4 Update `MEMORY.md` 9-bucket queue progress: mark bucket 2a as proposed → applied → archived as the work moves through stages. (Updated `project_ux_9_bucket_queue.md` 进度跟踪 to mark 2a as `applied`, awaiting live verify before archive.)
- [x] 10.5 Run `openspec validate add-scene-2d-theme-tokens --strict` before archiving; no errors. (`Change 'add-scene-2d-theme-tokens' is valid`.)
- [x] 10.6 Confirm no protocols in `openspec/protocols-ledger.md` are touched by this change (cosmetic-only); no ledger update needed.

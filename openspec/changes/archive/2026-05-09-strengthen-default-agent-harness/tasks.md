## 1. Baseline Truth And Entry Gates

- [x] 1.1 Record the current source audit map for ClaudeSource, ClaudeRust, Claude Agent SDK, OpenAI Agents JS, and MCP into a durable harness capability map.
  - PASS 2026-05-09: added `openspec/harness-capability-map.md` with source anchors, current Offisim evidence, pending proof, and entry-gate status at audit-start revision `a43f9ccd`.
- [x] 1.2 Fix the current `pnpm harness:deterministic` failures before claiming new harness progress.
  - PASS 2026-05-09: fixed stale replay failure by keeping skill-confirm flow at one LLM call and restoring typed user-facing skill errors before generic JSON skill results; `pnpm harness:deterministic` passed.
- [x] 1.3 Fix the current `pnpm harness:context` crash and replace zero-value retained-context metrics with real assertions.
  - PASS 2026-05-09: `harness:context` now loads context-specific scenarios, handles scenarios without `initialState`, and reports measured micro-compact metrics (`75001 -> 626` tokens, 3 compacted messages, 0 lost facts) instead of placeholder zeros.
- [x] 1.4 Run `pnpm harness:contract`, `pnpm harness:deterministic`, and `pnpm harness:context`; record command, git revision, pass/fail, and blockers.
  - PASS 2026-05-09 at `a43f9ccd`: `pnpm --filter @offisim/core typecheck`, `node scripts/harness-context.mjs --force-build`, `node scripts/harness-replay.mjs --force-build`, `pnpm harness:deterministic`, and `pnpm harness:context` passed. `pnpm harness:deterministic` included `harness:contract`, `harness:replay`, and `harness:provider-adapter`. No blocker remains for the entry gate.

## 2. Default Offisim Harness Core

- [x] 2.1 Add a run-scoped conversation state object for messages, pending tool calls, tool results, permission denials, discovered tool/MCP snapshots, active context, usage, budget, retry, cancellation, and checkpoint identity.
  - PASS 2026-05-09: added `RunConversationState` and wired the default internal employee path to record messages, pending tool calls, tool results, permission denials, discovered tool snapshots, active context, usage, tool-round budget, retry/cancellation, and checkpoint identity. Replay assertion `conversationStateContains` now proves the state is populated for `skill-create-real-tool-call`; `pnpm --filter @offisim/core typecheck`, `node scripts/harness-replay.mjs --force-build`, `pnpm harness:deterministic`, and `pnpm harness:context` passed.
- [x] 2.2 Normalize the tool registry so built-in, gateway, MCP, and future runtime-profile tools share one permission and audit path.
  - PASS 2026-05-09: added a normalized runtime tool registry with `builtin` / `mcp` / `workstation` / `runtime-profile` / `virtual` surfaces, recorded it into run conversation state, and routed audit telemetry through the same source resolver used for permission identity. Added `builtin-tool-registry-audit-path` replay coverage proving a built-in `read_file` call uses `serverName=builtin`, emits `toolType=builtin`, records the registry entry, and does not fall through to the MCP fixture executor. `pnpm --filter @offisim/core typecheck`, `node scripts/harness-replay.mjs --force-build`, `pnpm harness:deterministic`, and `pnpm harness:context` passed.
- [x] 2.3 Strengthen the tool loop for streaming, bounded rounds, missing tool-result recovery, retryable provider failures, max-turn exhaustion, stop hooks, and non-duplicated side effects.
  - PASS 2026-05-09: tightened the default employee tool loop so duplicate `tool_call_id` values return explicit duplicate results without repeating side effects, missing settled tool results become explicit `[MISSING_TOOL_RESULT]` failures, and `MAX_TOOL_ROUNDS` exhaustion fails the task with a durable `MAX_TOOL_ROUNDS_EXHAUSTED` event instead of continuing or falsely completing. Added replay scenarios `tool-loop-duplicate-call-id-skips-side-effect` and `tool-loop-max-rounds-fails-fast`; existing replay/provider gates continue to cover streamed tool calls, provider adapter timeout/fail-closed behavior, and recovery/error paths. `pnpm --filter @offisim/core typecheck`, `node scripts/harness-replay.mjs --force-build`, `pnpm harness:deterministic`, and `pnpm harness:context` passed.
- [x] 2.4 Strengthen completion verification so file, shell, MCP, workspace, and verification tasks require relevant evidence while pure text deliverables do not require fake tools.
  - PASS 2026-05-09: preserved existing file/write, failed-shell, reroute-event, Chinese artifact, and pure-text completion gates, then added replay coverage for verification success evidence and workspace-read evidence blocking. `completion-verification-tool-evidence-completes` proves `pnpm-test` evidence completes verification work without fake file proof; `completion-workspace-read-blocks-without-evidence` proves a workspace read claim blocks without `read_file` / `bash` evidence. `pnpm --filter @offisim/core typecheck`, `node scripts/harness-replay.mjs --force-build`, `pnpm harness:deterministic`, and `pnpm harness:context` passed.
- [x] 2.5 Add cancellation propagation across model turn, tool execution, MCP request, task-run state, and UI activity.
  - PASS 2026-05-09: cancellation now records run-scoped conversation cancellation state and finalizes the active task run as `cancelled` instead of sending aborts through normal failure recovery. `FakeGateway` and deterministic tool fixtures now honor run abort signals, with replay scenarios `cancellation-model-turn-cancels-task-run` and `cancellation-tool-request-cancels-before-side-effect` proving model-turn cancellation, audited tool/MCP-request cancellation telemetry, no fixture side effect after cancellation, `task.state.changed -> cancelled`, and employee UI activity returning to idle. `pnpm --filter @offisim/core typecheck` and `node scripts/harness-replay.mjs --force-build` passed.

## 3. Context Survival And Long-Running Work

- [x] 3.1 Implement measurable anchor retention for the original user objective across long sessions.
  - PASS 2026-05-09: deterministic runtime scenarios now include a rolling journal, and replay/context coverage `long-running-objective-anchor-retained-across-assignments` proves a follow-up employee task receives the original objective via the retained anchor without repeating the objective in the follow-up task text. `node scripts/harness-replay.mjs --force-build` and `node scripts/harness-context.mjs --force-build` passed with the anchor case included in both suites.
- [x] 3.2 Make tool-result trimming, micro-compact, synopsis/full compact, and prompt-too-long recovery cooperate under one context budget policy.
  - PASS 2026-05-09: `ConversationBudgetService` now resolves one context-budget policy and applies it consistently to micro-compact and final LLM-message pruning, including bounded tail messages, old tool-result compaction, and recent tool-result preservation. Added `context-budget-policy-cooperates` to `harness:context`, proving 60k estimated input tokens are reduced to 6107 while retaining 2 recent large tool results and recording 0 lost facts. `pnpm --filter @offisim/core typecheck` and `node scripts/harness-context.mjs --force-build` passed.
- [x] 3.3 Add deterministic scenarios for long tool outputs, prompt-too-long recovery, lost-fact detection, and forked subcontext isolation.
  - PASS 2026-05-09: `harness:context` now covers long tool output compaction (`long-running-microcompact-triggers`, 75001 -> 626 tokens, 0 lost facts), prompt-too-long recovery (`prompt-too-long-recovery-budget`, 140610 -> 20097 tokens under a 20-message tail), explicit budget-policy cooperation (`context-budget-policy-cooperates`, 0 lost facts), and forked subcontext isolation (`fork-subcontext-isolation`, child transcript secret not returned/leaked). `pnpm --filter @offisim/core typecheck` and `node scripts/harness-context.mjs --force-build` passed.
- [x] 3.4 Add a long-session replay/soak gate that proves anchor retention, evidence retention, bounded memory, and non-fake completion.
  - PASS 2026-05-09: strengthened `yolo-80-turn-multi-file-refactor` soak assertions to require retained original anchor, retained latest tool evidence marker after compaction, bounded final non-system context, rolling journal writes, leak-free runtime state, and completion verifier success from real `pnpm-test` evidence. `node scripts/harness-soak.mjs --iterations=1 --force-build` passed with `finalNonSystemTokens=5361`, `microCompactPasses=20`, `rollingJournalWrites=10`, `anchorRetained=1`, `evidenceRetained=1`, `completionVerifierAllows=1`, and 0 leak iterations.

## 4. MCP Lifecycle

- [x] 4.1 Implement harness-owned MCP client lifecycle: initialize, capability negotiation, tool/resource/prompt listing, operation, cancellation, list-changed refresh, and shutdown.
  - PASS 2026-05-09: `McpToolExecutor` now owns a stateful server catalog for tools, resources, prompts, capability snapshots, list-changed refresh, abort-aware tool calls, and clean shutdown. Browser SSE and Tauri desktop MCP clients expose list/call lifecycle methods with abort signals. `pnpm --filter @offisim/core typecheck`, `pnpm --filter @offisim/web typecheck`, and `pnpm harness:mcp-lifecycle -- --force-build` passed.
- [x] 4.2 Route MCP tool calls through Offisim permission, audit, task-run identity, and completion-evidence paths.
  - PASS 2026-05-09: `mcp-lifecycle-success` proves MCP execution passes through `AuditingToolExecutor`, records permission approval, server/tool identity, employee identity, task-run identity, runtime events, and successful audit evidence usable by completion verification. `mcp-lifecycle-permission-denial` proves denied MCP calls are audited and do not reach the MCP server.
- [x] 4.3 Add backend MCP lifecycle harness scenarios for success, permission denial, cancellation, server error, capability change, and shutdown.
  - PASS 2026-05-09: added `pnpm harness:mcp-lifecycle` with six deterministic backend cases: `mcp-lifecycle-success`, `mcp-lifecycle-permission-denial`, `mcp-lifecycle-cancellation`, `mcp-lifecycle-server-error`, `mcp-lifecycle-capability-change`, and `mcp-lifecycle-shutdown`. The gate passed 6/6 with lifecycle steps recorded separately.

## 5. Agent Employees Without Weakening The Default Harness

- [x] 5.1 Define employee agent capability tiers: text-only, sandbox-native tools, gateway-bridged tools, and full-agent employee.
  - PASS 2026-05-09: added shared `RuntimeEngineCapabilityTier` values for `text-only`, `sandbox-native-tools`, `gateway-bridged-tools`, and `full-agent-employee`, plus default Codex/Claude text-only preview profiles.
- [x] 5.2 Define employee agent capability profiles with trust tier, task classes, tool namespace, sandbox, permissions, context retention, cancellation, checkpoint, telemetry, rollback, and failure taxonomy.
  - PASS 2026-05-09: added `RuntimeEngineCapabilityProfile` and profile resolution/evaluation helpers covering trust, task fit, tool namespace/model, sandbox boundary, permissions/denied path, context, cancellation, checkpoint, telemetry, rollback, failure taxonomy, native capabilities, verification evidence, and blockers.
- [x] 5.3 Update employee runtime selection so full agent employees are configured through runtime engine profiles, not provider lane selection.
  - PASS 2026-05-09: employee engine bindings may reference `profileId`; `runEmployeeEngine` resolves the selected runtime profile from binding/runtime policy/adapter/default profile and blocks capability gaps before adapter execution. Provider lane selection remains separate from employee engine profile selection.
- [x] 5.4 Normalize native SDK-agent activity into Offisim employee events while distinguishing native engine tools from Offisim gateway tools.
  - PASS 2026-05-09: engine activity/proposal payloads now include runtime profile identity; engine tool events default to `runtime-profile` telemetry unless a verified gateway bridge emits an Offisim gateway tool type. Native engine tool telemetry is no longer mislabeled as builtin gateway evidence.
- [x] 5.5 Enforce proposal-only semantics for employee-agent handoffs, delegation, and global plan mutations.
  - PASS 2026-05-09: employee-engine handoff/replan/permission activity remains `engine.proposal.created` and approval activity; it does not emit formal Offisim handoff/routing events or mutate global plan state directly. Runtime profile identity is recorded on proposal payloads.
- [x] 5.6 Add success, blocked, cancellation, resume, denied-path, telemetry, and failure-classification harness scenarios for each supported employee agent profile.
  - PASS 2026-05-09: added `pnpm harness:engine-profiles` with 10 deterministic cases across both supported preview profiles, covering tier records, text-task success eligibility, local-tool blocking, explicit profile binding, full-agent non-advertisement, cancellation status, resume/checkpoint blocking, denied-path blocking, telemetry declaration, and failure-classification status. The gate passed 10/10.
- [x] 5.7 Keep partial profiles lower-tier or preview-blocked until the full tier evidence exists.
  - PASS 2026-05-09: existing Codex/Claude engine adapters are now explicitly `text-only` / `preview`; full-agent and gateway-bridged capabilities remain unadvertised until denied-path, checkpoint/resume, telemetry, rollback, tool bridge, and release evidence exist. `openspec/harness-capability-map.md` records this boundary.

## 6. Main Harness Agent Control Plane

- [x] 6.1 Add main harness mode policy with `offisim-core` as default and alternate driver/replacement modes disabled unless trusted runtime configuration exists.
  - PASS 2026-05-09: added `MainHarnessPolicyConfig` and `resolveMainHarnessMode`; no explicit override resolves to `offisim-core`, and alternate modes require verified trusted runtime policy.
- [x] 6.2 Add explicit override policy records with scope, actor, reason, previous mode, next mode, runtime profile, verification status, timestamp, and rollback checkpoint.
  - PASS 2026-05-09: added `MainHarnessOverridePolicyRecord` with required audit fields; `main-harness-missing-audit-fields-blocked` proves missing fields block override selection.
- [x] 6.3 Implement deterministic override resolution so provider lane selection, employee binding, SDK availability, and external peer discovery cannot self-promote into main harness control.
  - PASS 2026-05-09: `resolveMainHarnessMode` selects only explicit scoped policy; `main-harness-provider-lane-cannot-override` proves SDK provider lane and employee engine binding cannot promote themselves into main harness control.
- [x] 6.4 Implement driver-mode contract where external/SDK agent runtimes propose actions and Offisim executes approved actions through its own harness.
  - PASS 2026-05-09: added `createAgentDriverProposal` with `executesOffisimTool=false`; `main-harness-driver-proposes-actions` proves driver output is proposal-only.
- [x] 6.5 Define replacement-mode checkpoint handoff, rollback, audit, cancellation, denied-path, and equivalence contracts before any runtime can be enabled.
  - PASS 2026-05-09: replacement mode requires verified override, trusted runtime availability, and rollback checkpoint; unverified replacement and browser-limited replacement both resolve back to `offisim-core` as blocked.
- [x] 6.6 Add admin-facing capability/verification status for alternate main harness profiles without advertising unsupported capabilities.
  - PASS 2026-05-09: added `listMainHarnessRuntimeStatus`; `main-harness-admin-status-exposes-verification` separates selectable verified driver profile from blocked replacement profile.
- [x] 6.7 Add backend and release gates proving driver/replacement blocked states, allowed states, rollback, denied paths, explicit override audit, and default-off behavior.
  - PASS 2026-05-09: added `pnpm harness:main-control-plane`, passing 9/9 backend cases for default-off, provider-lane no-promotion, missing audit block, invalid override rollback, driver proposal-only, replacement blocked, browser replacement blocked, verified driver allowed, and admin verification status. Release `.app` evidence remains in 9.x before archive.

## 7. Provider Lane Boundary Cleanup

- [x] 7.1 Update provider lane matrix and protocol ledger to state that SDK provider lanes are text/reasoning-only leaf lanes.
  - PASS 2026-05-09: updated `openspec/provider-lane-matrix.md` and `openspec/protocols-ledger.md` to state that Claude/Codex/OpenAI SDK provider lanes are leaf text/reasoning adapters and cannot expose Offisim tools, employee-agent mode, driver mode, or replacement mode.
- [x] 7.2 Ensure SDK provider lane local-tool requests fail before model execution with guidance to use gateway/default harness or a verified employee agent profile.
  - PASS 2026-05-09: strengthened the SDK/non-tool-lane local-tool outcome to keep the typed `local-tools-require-gateway-lane` code while adding user-facing guidance to use the default Offisim gateway harness or a verified gateway-capable employee profile. `node scripts/harness-replay.mjs --force-build` passed.
- [x] 7.3 Add replay coverage proving provider lane selection never enables employee agent mode or main harness replacement.
  - PASS 2026-05-09: existing SDK-lane replay gates still prove model/tool execution short-circuits before local-tool work, and `harness:main-control-plane` adds deterministic backend replay for `main-harness-provider-lane-cannot-override`, proving SDK provider lane and employee engine binding cannot self-promote into main-harness driver/replacement. `node scripts/harness-replay.mjs --force-build` and `pnpm harness:main-control-plane -- --force-build` passed.

## 8. Product Surfaces And Evidence

- [x] 8.1 Update Personnel runtime UI copy so users choose employee runtime capability, not raw provider transport.
  - PASS 2026-05-09: `RuntimeBindingControl` now presents employee runtime as capability choices: Offisim gateway tools vs Claude/Codex text-engine preview profiles. Copy no longer implies SDK engines can execute Offisim local tools.
- [x] 8.2 Add Settings/admin surface for main harness mode only after the control-plane contracts and evidence gates exist.
  - PASS 2026-05-09: Settings Runtime now shows a main-harness control status surface with `Offisim core` as default owner, verified driver count, blocked replacement status, and per-profile verification status from `listMainHarnessRuntimeStatus`.
- [x] 8.3 Ensure activity feed distinguishes Offisim gateway tools, native employee-agent tools, proposals, denials, cancellations, and checkpoint handoffs.
  - PASS 2026-05-09: engine activity/proposal labels now carry runtime profile identity, mark proposals as not applied, and distinguish native engine tools from Offisim gateway tools. Tool telemetry labels now prefix runtime-profile tools as native engine and builtin/MCP/workstation tools as Offisim gateway.
- [x] 8.4 Keep browser-limited runtime fail-closed for trusted agent engines and replacement modes.
  - PASS 2026-05-09: browser runtime resolves main harness policy at startup and throws typed blockers for non-default or blocked main-harness overrides instead of silently starting driver/replacement paths without a trusted desktop/backend bridge. `pnpm --filter @offisim/ui-office typecheck` and `pnpm --filter @offisim/web typecheck` passed.

## 9. Release Verification And Archive Gate

- [ ] 9.1 Run backend gates: `pnpm harness:contract`, `pnpm harness:deterministic`, `pnpm harness:context`, MCP lifecycle, stream-tools, model-bench, smoke/load/edge where credentials permit.
  - PARTIAL 2026-05-09 at `a43f9ccd`: `pnpm harness:deterministic`, `pnpm harness:context`, `pnpm harness:mcp-lifecycle -- --force-build`, `pnpm harness:engine-profiles -- --force-build`, `pnpm harness:main-control-plane -- --force-build`, `pnpm harness:stream-tools`, and `pnpm harness:model-bench` passed. `harness:deterministic` included `harness:contract`, `harness:replay`, and `harness:provider-adapter`.
  - BLOCKED 2026-05-09: live provider gates that require Anthropic gateway credentials did not pass in this environment. `pnpm harness:smoke` failed with `provider.authentication` because no `apiKey`, `authToken`, `credentials`, `config`, or `profile` was available. `pnpm harness:load` failed 4/4 for the same `provider.authentication` category. `pnpm harness:edge` passed 5/9 runtime classification cases but failed `tool-calls`, `timeout`, and `cancellation` because the provider branch hit missing authentication before those live paths could execute; `provider-quota` stayed skipped without a configured live quota repro. Do not count smoke/load/edge as release-pass evidence until credentials are available and rerun.
- [ ] 9.2 Build release desktop app and run release `.app` live verification with Computer Use evidence for default harness ownership.
  - PARTIAL 2026-05-09: `pnpm --filter @offisim/ui-office build` passed, then `pnpm --filter @offisim/desktop build` passed and produced `/Users/haoshengli/Seafile/WebWorkSpace/Offisim/apps/desktop/src-tauri/target/release/bundle/macos/Offisim.app`. Release binary timestamp: `2026-05-09 04:33:17 +1200`; SHA256: `2da1b501c40006d158db885d682bdb4d1ac976065500873015cf13e007c43b7b`.
  - BLOCKED 2026-05-09: Computer Use could not attach to the release app, returning `Apple event error -10005: cgWindowNotFound`. The same Computer Use error reproduced against Google Chrome and Finder, so this session cannot provide valid release `.app` UI evidence. A diagnostic launch showed `offisim-desktop` running from the exact release app path, but release verification cannot use shell/AppleScript evidence in place of Computer Use.
- [ ] 9.3 Run release evidence for every advertised employee agent profile and every advertised main harness driver/replacement profile.
  - BLOCKED 2026-05-09: backend evidence proves only the advertised text-only preview employee profiles and default-off main harness policy. Release UI evidence for the Personnel profile copy and Settings main-harness status surface is still missing because Computer Use cannot attach in this session. No full-agent, gateway-bridged employee, or replacement main-harness profile is advertised as available.
- [x] 9.4 Record unresolved credential, provider, device, or runtime blockers as blockers; do not mark tasks complete or archive while release evidence is missing.
  - PASS 2026-05-09: unresolved blockers are recorded above and keep 9.1-9.3 unchecked: missing live Anthropic gateway authentication for smoke/load/edge, and a Computer Use session-level `cgWindowNotFound` failure that blocks valid release `.app` UI evidence.
- [x] 9.5 Update OpenSpec specs/docs/tasks with final evidence and run `openspec validate strengthen-default-agent-harness --strict` before archive.
  - PASS 2026-05-09: updated `openspec/harness-capability-map.md`, `runtime-live-verification-gates/spec.md`, and this task file with final backend evidence plus live-release blockers. `openspec validate strengthen-default-agent-harness --strict` passed. Archive remains blocked because 9.1-9.3 are not complete.

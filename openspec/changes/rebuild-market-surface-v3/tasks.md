## 0. Precondition

- [x] 0.1 Phase 0 (`establish-v3-design-foundation`) 已 apply，V3 tokens（`--r-md`/`--line-soft`/`--elev-1`/`--sp-7`/`--accent`/`--violet`/`--warn`/`--danger`/`--ink-3` + 配套 `*-surface` + `--surface-sunken`）已在 token SSOT 中存在；本 change 任何消费 token 的任务都以此为前置
  - **已做**：核对 `apps/desktop/renderer/src/generated/tailwind-theme.css`，全部 V3 token 都在（含 `--accent-fg`/`--accent-ring`/`--ok`/`--ok-surface`/`--line`/`--line-strong`/`--r-xs..xl`/`--elev-2`/`--fs-micro..xl`）。**apply-time 决定**：prototype 的 `--r-<kind>` rarity 别名层（`--r-employee: var(--accent)` 等）**不在** Phase-0 token SSOT 里，未生成成运行时 CSS var；引入它需动 token 生成器（越界）。改为在 `market-rarity.ts` 直接解析到底层 token（`--accent`/`--violet`/`--warn`/`--danger`/`--ink-3` + 配对 `*-surface`/`--surface-sunken`），与 design D4 映射完全等价。

## 1. Rarity → CSS-var

- [x] 1.1 `market-rarity.ts`：getter 返回 CSS-var 别名（`--r-<kind>`：employee→accent/skill→violet/sop·prefab→warn/company_template→violet/office_layout→danger/bundle→ink-3，fallback ink-3）+ 配对 surface 别名（`--r-<kind>-s`，fallback `--surface-sunken`）；兼容旧调用签名（同时给 cssVar + surface + 兜底 ink-3）
  - **已做**：`getRarityColor(kind)` 现返回 `{ color, surface }`（CSS-var refs），映射同 D4。新增 `rarityStyleVars(kind)` helper 产出 `{ '--rc', '--rcs' }` 给 card/panel root。`RarityColorScheme` 字段名从旧 `border/glow/badge/accent`（Tailwind class）换成 `color/surface`（CSS-var）；唯二消费方是 `MarketListingCard` + `MarketDetailView`（已同步），`index.ts` re-export 无害。**apply-time 决定**：见 0.1——直接解析底层 token 而非 `--r-<kind>` 别名（别名层不存在于 SSOT）。

## 2. Card + grid V3

- [x] 2.1 `MarketListingCard.tsx`：`h-market-listing-card` 高度 + `--r-md`/`--line-soft`/`--elev-1`；cover 改 **88px band 渲 per-kind cover 可视化**（按 `listing.kind` 选 `kv-<kind>`：employee/skill/sop/company_template/office_layout/prefab/bundle），band 由 `var(--rcs)` 着色，featured 用 rarity radial-gradient；删 16:9 image hero；**无 kind viz 命中时 fallback icon-only tile（48×48 kind icon 居中）**；curated/featured 才附加 ≤96×64 thumb；保留 kind chip + Installed badge(listing_id 或 package_id::version) + creator + verification dot；stats row stars=`--warn`、installs=mono、creator；top 3px rarity stripe 用 `var(--rc)`
  - **已做**：card 重写为 V3 dense chip（`h-market-listing-card`=200px、`rounded-r-md`、`border-line-soft`、`shadow-elev-1`、hover `-translate-y-px` + `shadow-elev-2`/`border-line-strong`、selected `border-accent`）。新增 `MarketCoverViz.tsx`：88px band 按 `listing.kind` 渲 7 个 `kv-<kind>` 可视化（employee 渐变 avatar+initials+tag chips / skill NET·FS·SEC perm strip+cap chips / sop DAG pip+role 序列 / company_template role 色带 / office_layout floor-plan SVG / prefab 等距线稿 SVG / bundle 三层 icon stack），band tint = `var(--rcs)` 渐变，featured = rarity radial-gradient。`hasCoverViz()` 对 7 个有效 AssetKind 恒真；`CoverIconTile`（48×48 icon on `var(--rcs)` tile）是 legacy/unknown-kind fallback（switch default + grid fallback 双兜底），永不空/破图。删了旧 `listing.preview` image hero。保留 kind chip overlay（白底 + `var(--rc)` 描边/文字）、Installed badge（`ok-surface` + `var(--ok)`）、stats row（stars=`warn-surface`/`--warn` + Star、installs=mono `surface-sunken`、creator=mono + verification dot）、顶 3px `var(--rc)` stripe。**apply-time 决定**：(a) `ListingSummary` 无 per-kind 结构化字段（无 skill 权限、sop 步数、template 角色数），viz 用「真实可得数据 + listing_id 确定性 hash」驱动形状变化——这是装饰性 inventory glyph（prototype 同样用 mock 数据），非 data-bound widget，符合「cover key 在 kind 上」的硬规则。(b) 可选 ≤96×64 thumb **未加**：`ListingSummary` schema 无 `curated`/`featured` 字段（design D1 resolved-question 已明确「无该字段则不附加 thumb」）。
- [x] 2.2 `MarketCardGrid.tsx` + `index.css` `.grid-market-card-list`：`repeat(auto-fill,minmax(252px,1fr))` + gap 14px + pad `--sp-7`；featured 卡 `grid-column: span 2`
  - **已做**：`.grid-market-card-list` → `minmax(15.75rem, 1fr)`（=252px）；grid wrapper（loading + ready 两处）→ `gap-[14px] p-sp-7`。skeleton 重写成 V3 footprint（88px cover band + body chips）。card 支持 `featured` prop（`col-span-2` + radial cover）已就绪；grid 新增 `selectedListingId` prop 驱动 selected ring。**apply-time 决定**：featured 卡当前不出现，因为 `ListingSummary` 无 featured marker（prop 接口已 ready，待 schema 字段落地即可接线）。

## 3. Filter + detail V3

- [x] 3.1 `MarketFilterBar.tsx`：**kind/sort** 从 `ui-core` `Select`（原生箭头）改 segmented chip-grammar（`--line` border/`--r-md`/inner 28-30px hover `--surface-sunken`），自定义 12×12 chev svg，无原生 select 箭头；**mode（ModeDropdown）/ manageTab（ManageTabDropdown）已是 `EntityDropdown`** → restyle 成同款 V3 chip-grammar，保留 `EntityDropdown` 语义 + `onSelect`/`onModeChange`/`onManageTabChange` 契约，不退回原生 select；filter bar 内任何位置无原生 `<select>` 箭头；onChange/a11y 契约不变
  - **已做**：删掉 kind/sort 的 `ui-core` `Select`（原生箭头），新增 `<Segmented>` 内部组件（`role="tablist"` + `role="tab"`/`aria-selected`，container `h-30 border-line r-md surface-2 elev-1`、inner 22px segment、active=`accent-surface`/`--accent` + `inset` ring、hover=`surface-sunken`）。kind segment 带 lucide icon、sort 纯文字。`ModeDropdown`/`ManageTabDropdown` 保留 `EntityDropdown` + 全部 `onSelect`/`onModeChange`/`onManageTabChange` 契约，只 restyle trigger Button 成 V3 chip（自定义 `ChevronDown` svg，无原生箭头）。search input + Publish + narrow sheet 全 V3 token 化。toolbar 54px。filter bar 内零原生 `<select>`。**apply-time 决定**：mode 用单 dropdown chip（prototype 同款 `mkt-drop`），不展开成 segmented——prototype Explore/Manage 是 dropdown 不是 seg。
- [x] 3.2 `MarketDetailView.tsx` + `MarketPage.tsx` + `index.css`：`layout='panel'` 固定右侧 440px 滑入(head 48px)、列表留左；**`MarketPage` detail-open wrapper 从两栏 fr grid（`grid-market-detail-desktop`/`-tablet`）改成固定 440 右栏 panel**（成对改 `index.css` detail grid utility）；同时**给 detail-open 分支的 `MarketCardGrid` 补传 `installedPackageKeys`**（当前只传 `installedListingIds`），让 detail 打开时 re-seed 后已装 badge 仍 survive；detail 内容区块不变；narrow 仍 drill-in
  - **已做**：`.grid-market-detail-desktop`/`-tablet` 从两栏 fr split 改成 `minmax(0,1fr) 27.5rem`（=440px 固定右栏）。`MarketDetailView` 重写成单列 V3：48px `DetailHead`（back chev + `var(--rcs)`/`var(--rc)` kind chip）+ 单列滚动 body（title/handle → carousel → tags → 2-col meta dl → Install CTA（`var(--rc)` 实心 / Installed disabled / 非 installable dashed note）→ Permissions → Description → Changelog → Requirements → Lineage → Runtime）。panel 加 `border-l`/`shadow-elev-2`。删掉旧 60/40 两栏 split 与 `cn`/`getRarityColor` 直引用（改用 `rarityStyleVars`）。`MarketPage` detail-open 分支补传 `installedPackageKeys` + `selectedListingId`，去掉中间 `border-r`。loading/unavailable 也带 48px head。narrow 仍走 `layout="narrow"` 全屏 drill-in。install identity 双 key 匹配 + `INSTALLABLE_KINDS` gate 原样保留。

## 4. 验收 gate

- [x] 4.1 串行 build + `pnpm typecheck`（组件 props 签名未变：onClick/onChange/layout/useInstalledListings 返回型）
  - **已做**：全部 gate exit 0 — ui-office build ✅ / ui-office typecheck ✅ / renderer `tsc --noEmit` ✅ / desktop-renderer build ✅（MarketWorkspacePage chunk 正常产出）/ `tokens:check` ✅ / `tokens:lint-hex` ✅ / `pnpm typecheck` 25/25 ✅ / biome check 7 文件 0 error。组件对外 props 兼容：`onClick(listingId)` / `onChange` / `layout='full'|'panel'|'narrow'` / `useInstalledListings` 返回型不变；新增的 card `selected`/`featured`、grid `selectedListingId` 都是可选 prop（向后兼容）。
- [ ] 4.2 release `.app` live（用户/Codex）：卡片密集 / **88px band 按 kind 渲 per-kind cover viz**（kv-employee/skill/sop/template/layout/prefab/bundle，**非 icon-only**；icon-only 仅在无 kind viz 时兜底）/ grid 密度 minmax252 + featured span2 / filter segmented 无原生箭头（mode/manageTab 仍 EntityDropdown）/ detail 右侧 440 滑入 / rarity 颜色 + cover tint 按 kind / install CTA 仍只 employee+skill / 已装 badge（含 detail 打开时 re-seed 后仍在）/ deep-link `offisim://install` overlay 正常
  - **BLOCKED 2026-05-24**：release `.app` 已用当前 worktree 精确路径构建并启动，但本机处于 macOS 锁屏界面；CGWindow 可见 Offisim 窗口存在，Computer Use 附着返回 `cgWindowNotFound`，无法完成 release live 视觉/交互验收。解锁后必须用同一 `.app` 路径补跑，不得用 dev server 或浏览器替代。
- [ ] 4.3 archive gate 三查（install 单点规则不破，CLAUDE.md market 段无过时）
  - **留未勾**：archive gate task，archive 时执行。

## 0. Precondition

- [x] 0.1 Phase 0 (`establish-v3-design-foundation`) 已 apply，V3 tokens（`--r-md`/`--line-soft`/`--elev-1`/`--sp-7`/`--accent`/`--violet`/`--warn`/`--danger`/`--ink-3` + 配套 `*-surface` + `--surface-sunken`）已在 token SSOT 中存在；本 change 任何消费 token 的任务都以此为前置
  - **已做**：核对 `apps/desktop/renderer/src/generated/tailwind-theme.css`，全部 V3 token 都在（含 `--accent-fg`/`--accent-ring`/`--ok`/`--ok-surface`/`--line`/`--line-strong`/`--r-xs..xl`/`--elev-2`/`--fs-micro..xl`）。**apply-time 决定**：prototype 的 `--r-<kind>` rarity 别名层（`--r-employee: var(--accent)` 等）**不在** Phase-0 token SSOT 里，未生成成运行时 CSS var；引入它需动 token 生成器（越界）。改为在 `market-rarity.ts` 直接解析到底层 token（`--accent`/`--violet`/`--warn`/`--danger`/`--ink-3` + 配对 `*-surface`/`--surface-sunken`），与 design D4 映射完全等价。

## 1. Rarity → CSS-var

- [x] 1.1 `market-rarity.ts`：getter 返回 CSS-var 别名（`--r-<kind>`：employee→accent/skill→violet/sop·prefab→warn/company_template→violet/office_layout→danger/bundle→ink-3，fallback ink-3）+ 配对 surface 别名（`--r-<kind>-s`，fallback `--surface-sunken`）；兼容旧调用签名（同时给 cssVar + surface + 兜底 ink-3）
  - **已做**：`market-rarity.ts` 现在只负责把 `AssetKind` 映射为命名 class（`market-rarity-*`），不再返回可拼接的 Tailwind class，也不再在组件里落 inline style。运行时颜色由 `apps/desktop/renderer/src/index.css` 的 rarity utility 统一定义：kind class 提供 `--rc`/`--rcs`，card/detail 只消费 `market-rarity-stripe` / `market-rarity-tile` / `market-rarity-chip` / `market-rarity-border` / `market-rarity-cta`。**apply-time 决定**：保留 CSS-var 思路，但把 var 赋值收进命名 utility，避免组件层 hardcode 与 lint 例外。

## 2. Card + grid V3

- [x] 2.1 `MarketListingCard.tsx`：`h-market-listing-card` 高度 + `--r-md`/`--line-soft`/`--elev-1`；删除 16:9 hero 与 per-kind SVG/illustration cover；卡片改成密集 inventory header（kind icon tile + Installed badge + creator + title + kind chip）+ summary/tags/stats；保留 Installed badge(listing_id 或 package_id::version) + verification dot；stats row stars=`--warn`、installs=mono、verification；top 3px rarity stripe 用 `var(--rc)`。
  - **已做**：按 2026-05-24 产品修正，`Docs/design` 只作为布局参考，不作为图示参考。删除 `MarketCoverViz.tsx`，卡片不再渲 88px 装饰 SVG band，也不恢复旧 image hero。`MarketListingCard` 现在是 V3 dense inventory card：顶部是 token 化 rarity stripe，header 用 kind icon tile + Installed + creator + title + kind chip；body 用 summary + tags；footer stats 保留 rating/install/verification。这样消除“丑 SVG 图示”与图示硬编码，同时保留资产类型识别和 marketplace 信息密度。
- [x] 2.2 `MarketCardGrid.tsx` + `index.css` `.grid-market-card-list`：`repeat(auto-fill,minmax(252px,1fr))` + gap 14px + pad `--sp-7`；featured 卡 `grid-column: span 2`
  - **已做**：`.grid-market-card-list` → `minmax(15.75rem, 1fr)`（=252px）并在 utility 内拥有 14px gap；grid wrapper（loading + ready 两处）只挂 `grid-market-card-list p-sp-7`。skeleton 保持 dense inventory footprint（icon tile/title/chip/body rows），不暗示 SVG cover 或 image hero。card 支持 `featured` prop（`col-span-2`）已就绪；grid 新增 `selectedListingId` prop 驱动 selected ring。**apply-time 决定**：featured 卡当前不出现，因为 `ListingSummary` 无 featured marker（prop 接口已 ready，待 schema 字段落地即可接线）。

## 3. Filter + detail V3

- [x] 3.1 `MarketFilterBar.tsx`：**kind/sort** 从 `ui-core` `Select`（原生箭头）改 segmented chip-grammar（`--line` border/`--r-md`/inner 28-30px hover `--surface-sunken`），自定义 12×12 chev svg，无原生 select 箭头；**mode（ModeDropdown）/ manageTab（ManageTabDropdown）已是 `EntityDropdown`** → restyle 成同款 V3 chip-grammar，保留 `EntityDropdown` 语义 + `onSelect`/`onModeChange`/`onManageTabChange` 契约，不退回原生 select；filter bar 内任何位置无原生 `<select>` 箭头；onChange/a11y 契约不变
  - **已做**：删掉 kind/sort 的 `ui-core` `Select`（原生箭头），新增 `<Segmented>` 内部组件（`role="tablist"` + `role="tab"`/`aria-selected`，container `h-30 border-line r-md surface-2 elev-1`、inner 22px segment、active=`accent-surface`/`--accent` + `inset` ring、hover=`surface-sunken`）。kind segment 带 lucide icon、sort 纯文字。`ModeDropdown`/`ManageTabDropdown` 保留 `EntityDropdown` + 全部 `onSelect`/`onModeChange`/`onManageTabChange` 契约，只 restyle trigger Button 成 V3 chip（自定义 `ChevronDown` svg，无原生箭头）。search input + Publish + narrow sheet 全 V3 token 化。toolbar 54px。filter bar 内零原生 `<select>`。**apply-time 决定**：mode 用单 dropdown chip（prototype 同款 `mkt-drop`），不展开成 segmented——prototype Explore/Manage 是 dropdown 不是 seg。
- [x] 3.2 `MarketDetailView.tsx` + `MarketPage.tsx` + `index.css`：`layout='panel'` 固定右侧 440px 滑入(head 48px)、列表留左；**`MarketPage` detail-open wrapper 从两栏 fr grid（`grid-market-detail-desktop`/`-tablet`）改成固定 440 右栏 panel**（成对改 `index.css` detail grid utility）；同时**给 detail-open 分支的 `MarketCardGrid` 补传 `installedPackageKeys`**（当前只传 `installedListingIds`），让 detail 打开时 re-seed 后已装 badge 仍 survive；detail 内容区块不变；narrow 仍 drill-in
  - **已做**：`.grid-market-detail-desktop`/`-tablet` 从两栏 fr split 改成 `minmax(0,1fr) 27.5rem`（=440px 固定右栏）。`MarketDetailView` 重写成单列 V3：48px `DetailHead`（back chev + named rarity kind chip）+ 单列滚动 body（title/handle → carousel → tags → 2-col meta dl → Install CTA（`market-rarity-cta` 实心 / Installed disabled / 非 installable dashed note）→ Permissions → Description → Changelog → Requirements → Lineage → Runtime）。panel 加 `border-l`/`shadow-elev-2`。删掉旧 60/40 两栏 split 与 `cn`/`getRarityColor`/inline style 直引用（改用 `getRarityClassName` + named utility）。`MarketPage` detail-open 分支补传 `installedPackageKeys` + `selectedListingId`，去掉中间 `border-r`。loading/unavailable 也带 48px head。narrow 仍走 `layout="narrow"` 全屏 drill-in。install identity 双 key 匹配 + `INSTALLABLE_KINDS` gate 原样保留。

## 4. 验收 gate

- [x] 4.1 串行 build + `pnpm typecheck`（组件 props 签名未变：onClick/onChange/layout/useInstalledListings 返回型）
  - **已做**：全部 gate exit 0 — ui-office build ✅ / ui-office typecheck ✅ / renderer `tsc --noEmit` ✅ / desktop-renderer build ✅（MarketWorkspacePage chunk 正常产出）/ `tokens:check` ✅ / `tokens:lint-hex` ✅ / `pnpm typecheck` 25/25 ✅ / biome check 7 文件 0 error。组件对外 props 兼容：`onClick(listingId)` / `onChange` / `layout='full'|'panel'|'narrow'` / `useInstalledListings` 返回型不变；新增的 card `selected`/`featured`、grid `selectedListingId` 都是可选 prop（向后兼容）。
- [x] 4.2 release `.app` live（用户/Codex）：卡片密集 / 无装饰 SVG cover 或 16:9 hero / header 信息层次清楚（kind icon tile + title + creator + Installed/kind chip）/ grid 密度 minmax252 + featured span2 / filter segmented 无原生箭头（mode/manageTab 仍 EntityDropdown）/ detail 右侧 440 滑入 / rarity 颜色按 kind / install CTA 仍只 employee+skill / 已装 badge（含 detail 打开时 re-seed 后仍在）/ deep-link `offisim://install` overlay 正常
  - **已做 2026-05-24**：release `.app` 通过当前 worktree 精确路径 `/Users/haoshengli/Seafile/WebWorkSpace/Offisim/apps/desktop/src-tauri/target/release/bundle/macos/Offisim.app` 构建并用 Computer Use 验证。Office：界面内假 macOS 红绿灯已移除，仅保留原生窗口按钮；Board tray 不再以旧 floating tab 常驻；team dock 铺满 scene 底部。Market：卡片无 SVG/hero cover，kind chip 不再泄露长枚举，标题可读；filter segmented 和 publish/import chrome 可见。
- [ ] 4.3 archive gate 三查（install 单点规则不破，CLAUDE.md market 段无过时）
  - **留未勾**：archive gate task，archive 时执行。

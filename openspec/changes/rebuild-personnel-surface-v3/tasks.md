## 0. Precondition — Phase 0 tokens

- [x] 0.1 **Phase 0 applied; V3 tokens present.** This change consumes V3 tokens/classes that today exist ONLY in the prototype, not in code: `--sp-5` / `--sp-7`, `--line`, `--r-md` / `--r-sm`, `--accent-surface`, `--surface-sunken`, and the `.insp-tabs` / `.insp-tab` chip grammar. Do NOT add these tokens in this change — gate on Phase 0 (`establish-v3-design-foundation`) having landed them first. If Phase 0 is not applied, block.
  - **已做**：核对 Phase 0 已落地。生成的 `apps/desktop/renderer/src/generated/tailwind-theme.css` 含 `--accent-surface: #ecf2ff`、`--line: #dde3ec`、`--r-sm: 7px`、`--sp-5: 12px`，且 `@theme` 暴露对应 Tailwind utility（`bg-accent-surface` / `text-accent` / `text-ink-3` / `text-ink-1` / `border-line` / `rounded-r-sm` / `text-fs-sm` / `text-fs-micro` / `text-fs-meta` / `px-sp-5` / `px-sp-7` / `py-sp-5` / `bg-surface-sunken`）。本 change 未新增任何 token，全部复用 Phase 0 产物。未 block。

## 1. Inspector tabs chip-grammar

- [x] 1.1 `PersonnelPage.tsx` inspector `TabsList`/`TabsTrigger`：套 prototype `.insp-tabs` / `.insp-tab` grammar —— `TabsList` = 单行 rail（`border-bottom 1px var(--line)` only，**无**容器 border / 3px padding 盒；`padding 0 var(--sp-5)`、~44px、`gap 2px`、`overflow-x-auto`）；`TabsTrigger` = 28px chip（`padding 0 11px`、`border 0`、`border-radius var(--r-sm)` **非 --r-md**、透明底、hover faint sunken、active `--accent-surface` + `--accent` 文字）。**不动** 6 TabsContent forceMount + `TABS_RETAIN_STATE_CLASS`
  - **已做**：`TabsList` className 改为 `h-11 w-full shrink-0 items-center justify-start gap-0.5 overflow-x-auto rounded-none border-0 border-b border-line bg-transparent p-0 px-sp-5`（44px 高、`border-bottom 1px var(--line)` only、`px-sp-5`=16px 横、`gap-0.5`=2px、横向滚动、清掉旧容器盒 `border-border-default bg-surface-elevated px-2 py-1`）。`TabsTrigger` className 改为 `h-7 shrink-0 rounded-r-sm border-0 bg-transparent px-[11px] text-fs-sm font-medium text-ink-3 transition-colors hover:bg-surface-sunken hover:text-ink-1 data-[state=active]:bg-accent-surface data-[state=active]:font-semibold data-[state=active]:text-accent`（28px chip、`rounded-r-sm`=7px、透明底、hover faint sunken、active accent-surface + accent，覆盖 ui-core Tabs primitive 自带的 `rounded-md` / `data-[state=active]:bg-surface-elevated`）。
  - **apply-time 决定**：(a) hover faint-sunken 用 `hover:bg-surface-sunken`（而非 prototype 字面 `rgba(20,32,56,0.04)`），与 Phase-3 `MarketFilterBar` 已建立的 chip 约定一致（同源 `bg-accent-surface text-accent` active / `bg-transparent text-ink-3 hover:bg-surface-sunken hover:text-ink-1` inactive），统一 V3 chip 语言、避免裸 rgba。(b) active 用 `data-[state=active]:` modifier 覆盖 Radix primitive，确保只在选中态切到 accent-surface。6 TabsContent forceMount + `TABS_RETAIN_STATE_CLASS` + per-tab min-height 结构完全未动。

## 2. Detail head + profile rhythm + avatar

- [x] 2.1 DetailHeader padding `px-6 py-4`（24px 横 / 16px 纵）→ `px-3 py-4`（prototype `.pd-head` `--sp-5` 纵 / `--sp-7` 横 = 12px 纵 / 16px 横）
  - **已做**：`px-6 py-4` → `px-sp-7 py-sp-5`（`--sp-7`=16px 横 / `--sp-5`=12px 纵，1:1 命中 prototype `.pd-head { padding: var(--sp-5) var(--sp-7) }`）。
  - **apply-time 决定（偏离 design D2 className target）**：design D2 把 className target 冻结成 `px-3 py-4`（12px/16px 的 Tailwind 近似），但 spec prose 明确「以 prototype `--sp-5/--sp-7` padding 为 SSOT，不锁特定 Tailwind class」。直接用 V3 spacing utility `px-sp-7 py-sp-5` 比 `px-3 py-4` 更精确（token-driven、随 density 切换自适应、与全 surface V3 间距系统一致），且语义自证。采纳 spec prose SSOT 优先，记录此偏离。
- [x] 2.2 `ProfileTab.tsx`：字段组 caps label + flow（无 card wrapper，现状收口）；input ~32px、label gap 4-6px 显式化
  - **已做**：(a) 3 个 section `<h3>` heading 从 `text-xs font-semibold uppercase tracking-wider text-text-muted` 收口为 V3 caps-label grammar `text-fs-micro font-semibold uppercase tracking-[0.14em] text-ink-3`（命中 prototype `.insp-sec-label`：fs-micro + ls-caps + ink-3 uppercase；`tracking-[0.14em]` 与 MarketFilterBar caps-label 约定一致）。(b) input-adj field label（10 处）`mb-1 block text-sm text-text-secondary` → `mb-[5px] block text-fs-meta font-medium text-ink-2`（5px label gap 命中 prototype `.fld { gap: 5px }`；fs-meta + ink-2 命中 `.fld > label`）。(c) control label（5 处，segmented/skills/tools）`mb-2 block text-sm text-text-secondary` → `mb-1.5 block text-fs-meta font-medium text-ink-2`（控件类略留 6px 呼吸，同色阶/字号）。(d) 字段组本就 card-less flow（唯一 bordered box 是 "System Prompt Preview" 折叠 disclosure，非 field-group card，保留）。
  - **apply-time 决定（input 高度保持 ui-core 默认 36px）**：spec 写 input `~32px`（带 tilde 容差）。ui-core `Input`/`Select`/`SelectTrigger` 默认 `h-9`=36px，是全 V3 表面（chat/office/market）统一的 control 节奏。只把 Personnel input 单独压成 `h-8`=32px 会让本 surface 与全 app input 节奏不一致，并对 Select trigger / number input 的垂直居中带来风险。`~32px` 容差覆盖 36px，且 label rhythm + caps-label + card-less flow 才是 V3 节奏的实质收口。保持 ui-core 默认，记录此取舍。
- [x] 2.3 `AppearanceTab.tsx` PreviewCard：核对 `avatar-preview-card` named class（index.css，`aspect-ratio 256/200 + max-width 16rem + min-height 12.5rem`）已提供 layout-stable 几何 + `<Canvas>` 仅 `style={{ background:'transparent' }}`；已满足，无需改 inline literal
  - **已做（verification-only）**：核对 `PreviewCard` 已用 `avatar-preview-card` class（renderer index.css 确认 `aspect-ratio: 256 / 200; max-width: 16rem; min-height: 12.5rem`），`<Canvas>` 仅 `style={{ background: 'transparent' }}`、无 `width/height` style。已满足 MODIFIED requirement 的几何 + aspect-ratio floor，无需改动。
- [x] 2.4 `shared/DicebearAvatar.tsx`：`rounded-full` → 26% block radius（prototype `.av`）。**有意 app-wide 改动**：经 `EmployeeAvatar` 复用在 Office scene roster / Personnel / Market detail / Employee Creator，改后全表面内部员工 2D avatar 统一 26% block。不动 `BrandAvatar2D` / 3D
  - **已做**：`className={`rounded-full ${className}`}` → `className={`rounded-[26%] ${className}`}`（1:1 命中 prototype `.av { border-radius: 26% }`）。用 arbitrary value `rounded-[26%]`（无 26% 命名 token；非颜色/阴影/字号，`tokens:lint-hex` 不拦，已验证通过）。这是有意 app-wide 改动：`DicebearAvatar` 经 `EmployeeAvatar` 复用，改后 Office scene roster / Personnel list+detail+inspector / Market detail / Employee Creator 全表面内部员工 2D avatar 统一 26% block。未动 `BrandAvatar2D`、未动 3D block-figure。
- [x] 2.5 grep 确认 Personnel 内无 bell
  - **已做**：`grep -rni bell packages/ui-office/src/components/employees/` 仅命中 `ToolPermissionEditor.tsx` 的 `aria-labelledby`（误报，非铃铛）。Personnel 内确认无 notification bell。

## 3. Base-spec drift 收口（MODIFIED）

- [x] 3.1 spec delta 已把两条 base requirement 重写为 named-CSS-class 现实，apply 时核对不再有「inline literal vs named class」双主张：
  - `AppearanceTab 3D Canvas slot declares aspect-ratio before mount` → 断言几何（256/200 + 200px/256px floor，今由 `avatar-preview-card` class 提供），不锁 inline `aspect-[256/200] min-h-[200px] max-w-[256px]`
  - `Personnel page grid SHALL use a layout that preserves min-height budget across responsive break` → 断言 desktop track `280|1fr|420`（今由 `grid-personnel-*` class 提供）+ behavioral min-height floor，不锁 inline `lg:grid-cols-[…]` / `min-h-[560px]` literal（后者代码不存在）
  - **已做（核对）**：spec delta 两条 MODIFIED requirement 已写成 named-CSS-class 现实。代码侧核对一致：`PersonnelPage` outer container 走 `grid-personnel-desktop-expanded`（renderer index.css 确认 `280px | minmax(0,1fr) | minmax(0,420px)`）/ `-collapsed`（`64px | 1fr | 420px`）/ tablet 退化 + narrow `grid-cols-1` flex-stack；AppearanceTab PreviewCard 走 `avatar-preview-card`（256/200 + 16rem + 12.5rem floor）。代码无 `lg:grid-cols-[…]` / `min-h-[560px]` inline literal。无双主张。

## 4. 验收 gate

- [x] 4.1 串行 build + `pnpm typecheck`（routeToPersonnel 签名不变、组件树不变）
  - **已做**：串行全绿。`rm -rf ui-office/dist+tsbuildinfo && pnpm --filter @offisim/ui-office build` exit 0；`pnpm --filter @offisim/ui-office typecheck` exit 0；renderer `npx tsc --noEmit` exit 0；`pnpm --filter @offisim/desktop-renderer build` exit 0（✓ built 11.12s）；`pnpm tokens:check` exit 0；`pnpm tokens:lint-hex` exit 0；`pnpm typecheck`（全量）25/25 successful。改动纯 className（PersonnelPage TabsList/TabsTrigger/DetailHeader、ProfileTab label/heading、DicebearAvatar radius）；组件树 / state / `routeToPersonnel(id,tab)` 签名 / 6-tab forceMount 结构均未动。
- [ ] 4.2 release `.app` live（用户/Codex）：inspector tabs bottom-rule chip 化（无容器盒）/ detail head 紧凑 / profile 字段节奏 + 无 card / avatar 26% block（且 Office scene / Market / Creator 同步统一）/ 三栏 280|1fr|420 + 折叠 64 / 6-tab 切换 layout 稳 + 3D preview 预热不破 / appearance·runtime save round-trip 正常 / cross-surface `routeToPersonnel` 正常
  - **BLOCKED 2026-05-24**：release `.app` 已用当前 worktree 精确路径构建并启动，但本机处于 macOS 锁屏界面；CGWindow 可见 Offisim 窗口存在，Computer Use 附着返回 `cgWindowNotFound`，无法完成 release live 视觉/交互验收。解锁后必须用同一 `.app` 路径补跑，不得用 dev server 或浏览器替代。
- [ ] 4.3 archive gate 三查（spec / tasks / 文档注释一致 + 协议台账无关）
  - **未勾**：待 4.2 live verify 通过后再走 archive gate。本 change 不碰 `openspec/protocols-ledger.md` 任何协议（纯 V3 视觉骨架 className），台账无需同步。

## 1. Gate A - Provider Transport And Trusted Sidecars

- [x] 1.1 Add a Rust-owned provider profile resolver that returns canonical base URL, allowed host, auth scheme, endpoint kind support, and local-endpoint flag.
  - PASS 2026-05-08: `runtime_provider_profile_upsert` / `resolve_runtime_provider_profile` added in `runtime_secrets.rs`; `cargo clippy --all-targets --all-features -- -D warnings && cargo test` passed.
- [x] 1.2 Replace Tauri `llm_fetch` request shape so webview no longer sends raw final URL or arbitrary credential header name to the credential-injecting command.
  - PASS 2026-05-08: `llm_fetch` now accepts `providerProfileId` + `endpointKind`; `pnpm validate` and release `.app` build passed.
- [x] 1.3 Enforce `https:` by default, explicit localhost allowance for local model profiles, host allowlist matching, and cross-host redirect credential blocking in Rust transport.
  - PASS 2026-05-08: Rust transport enforces host/scheme and blocks cross-host redirects; targeted Rust tests cover remote HTTP rejection, cross-host rejection, and localhost allowance.
- [x] 1.4 Filter credential/session-shaped response headers before streaming headers back to the webview.
  - PASS 2026-05-08: `is_filtered_response_header` filters auth/cookie/token/secret-shaped headers; targeted Rust test covers the filter.
- [x] 1.5 Update `tauri-llm-fetch.ts`, gateway creation, and OpenAI Agents SDK transport override to use provider profile + endpoint kind.
  - PASS 2026-05-08: Tauri gateway/OpenAI Agents SDK fetch override use provider profile id; `pnpm validate` passed.
- [x] 1.6 Change Claude trusted sidecar request to resolve baseURL from Rust-side provider profile, not request payload.
  - PASS 2026-05-08: Claude sidecar request no longer accepts raw `baseURL`; Rust reads profile base URL and secret ref.
- [x] 1.7 Change Claude/Codex trusted sidecar cwd resolution to accept project/workspace id, canonicalize workspace root, and reject cwd outside the project workspace.
  - PASS 2026-05-08: recorded LLM calls attach project/employee execution context; Tauri Claude/Codex SDK gateways send projectId to Rust; Rust resolves `projects.workspace_root`, canonicalizes cwd, and rejects cwd outside the bound project workspace.
  - PASS 2026-05-08 P1 review follow-up: trusted engine adapters now receive the same runtime project resolver and pass `projectId` into `codex_agent_execute` / `claude_agent_execute`; `pnpm harness:provider-adapter` covers the engine adapter payload and runtime registry wiring.
  - PASS 2026-05-08 P1 live follow-up: release `.app` direct employee chat with Alex Chen bound to `codex-engine` originally failed with `projectId is required for trusted Codex lane workspace binding`; after carrying `state.projectId` into the engine task envelope, the same release `.app` path reached `codex-engine accepted the assigned task`, and the later failure changed to `Codex app-server reported an error`.
- [x] 1.8 Add sidecar execution audit events with request id, project id, cwd, provider profile id, execution lane, employee id, and no credential bytes.
  - PASS 2026-05-08: sidecar startup audit writes `trusted-sidecar-audit.jsonl` with request/cwd/profile/lane and `credentialBytesRecorded:false`; project/employee fields are nullable until 1.7 is complete.
- [x] 1.9 Update Tauri capabilities/permissions so credential-injecting LLM commands are exposed only to the intended main runtime window/webview.
  - PASS 2026-05-08: `agent-bridges` capability remains scoped to `main`/`main-live`; release `.app` verified Settings provider page through Computer Use.
- [x] 1.10 Add deterministic/Rust safety coverage for blocked arbitrary host, blocked cross-host redirect, rejected arbitrary cwd, accepted local model profile, and unprivileged webview IPC denial.
  - PASS 2026-05-08: Rust tests cover arbitrary host rejection, cross-host redirect blocking, local loopback provider acceptance, trusted Claude/Codex cwd rejection, and privileged IPC capability scope limited to `main`/`main-live`; `cargo test -- --nocapture` passed.
- [x] 1.11 Add provider profile create/edit UX and audit coverage showing exact credential destination without storing secret bytes.
  - PASS 2026-05-08: Settings provider page in release `.app` shows `Credential destination: https://api.minimax.io`; Rust profile audit records profile changes without secret bytes.

## 2. Gate A - Local Workspace Gateway And Shell

- [x] 2.1 Refactor `open_local_path` to accept project id + relative path and reuse workspace containment checks before invoking the OS opener.
  - PASS 2026-05-08: command now requires project id and resolves relative path inside the bound project workspace.
- [x] 2.2 Refactor `save_deliverable_to_local` to write under the active project's `deliverables/` folder by default and reject arbitrary caller-provided roots.
  - PASS 2026-05-08: save target is `<workspace_root>/deliverables/<fileName>` and UI passes active project id.
- [x] 2.3 Refactor `git_exec` to require project id, canonical project cwd, allowlisted subcommands, and workspace containment.
  - PASS 2026-05-08: `git_exec` requires project id; targeted Rust tests cover cwd containment rejection.
  - PASS 2026-05-08 P1 review follow-up: `git clone` now requires an explicit destination, creates the destination parent under the bound project workspace, and rejects missing destination, `..`, and workspace-outside absolute paths; `cargo fmt --check && cargo test git -- --nocapture` passed.
- [x] 2.4 Make `bash_execute` require project id and remove fallback to all workspace roots.
  - PASS 2026-05-08: `bash_execute` rejects missing project id and resolves only the project workspace.
- [x] 2.5 Add shell env scrub with a minimal allowlist and no inherited provider secrets, session cookies, or unrelated host env vars.
  - PASS 2026-05-08: shell command uses `env_clear()` plus minimal allowlist.
- [x] 2.6 Add shell approval/audit fields: command, cwd, project id, employee id, approval id, timeout, exit code, network policy flag, and redacted stdout/stderr metadata.
  - PASS 2026-05-08: `bash_execute` writes `shell-execution-audit.jsonl` with command/cwd/project/employee/approval/timeout/exit/network policy and redacted stdout/stderr metadata; `cargo test builtin_tools_contracts`, `cargo fmt --check`, `pnpm exec tsc -p packages/core/tsconfig.json --noEmit`, and `pnpm --filter @offisim/web typecheck` passed.
- [x] 2.7 Implement shell network policy as disclosure + explicit approval + audit unless OS-level sandbox/proxy/firewall enforcement exists; do not claim network denial without enforcement.
  - PASS 2026-05-08: shell returns approval/network-policy metadata and docs explicitly state this is disclosure/audit, not OS-level network denial.
- [x] 2.8 If OS-level network denial is enabled, add verification that `curl` or `wget` fails under the denied policy.
  - PASS 2026-05-08: OS-level network denial is not enabled in this change; shell policy is explicitly `approval-gated-disclosed`, so no `curl`/`wget` denial evidence is claimed or required.
- [x] 2.9 Update Tauri capabilities/permissions for local path, git, shell, and deliverable commands with least-privilege runtime-window exposure.
  - PASS 2026-05-08: `fs-shell` / `agent-bridges` capabilities remain scoped to `main`/`main-live`; `pnpm --filter @offisim/desktop build` regenerated capability schemas.
- [x] 2.10 Add Rust safety coverage for path escape, symlink escape, missing project id, deliverables target, git cwd rejection, shell env scrub, network-policy disclosure, and unprivileged webview IPC denial.
  - PASS 2026-05-08: Rust tests cover parent/absolute path rejection, symlink escape, deliverable filename containment, git cwd rejection, shell env scrub/secret redaction, bounded file reads/writes, and privileged fs-shell IPC capability scope limited to `main`/`main-live`; `cargo test -- --nocapture` passed.
  - PASS 2026-05-08 P1 review follow-up: git clone destination helper tests cover `.offisim/tmp` parent creation plus parent-segment, outside-absolute, and no-destination rejection.

## 3. Gate A - Marketplace Ownership Guard

- [x] 3.1 Update publish draft create/update/submit paths to validate `listing_id` as UUID and verify listing ownership against current creator.
  - PASS 2026-05-08: publish schemas validate listing UUID and publish routes check listing ownership before draft create/update/submit.
- [x] 3.2 Update moderation listing update predicate to include both `listing_id` and `creator_id = draft.creator_id`.
  - PASS 2026-05-08: moderation listing update predicate includes creator id and errors on zero-row update.
- [x] 3.3 Add route/service coverage for the cross-creator listing takeover attack path and ensure it blocks release while failing.
  - PASS 2026-05-08: extracted listing ownership guard to `publish-ownership.ts`; `pnpm platform:security-harness` verifies allowed owner, invalid UUID rejection, and cross-creator 403 path, including a source assertion that the query binds `creator_id`; `pnpm --filter @offisim/platform typecheck` passed.

## 4. Gate B - Marketplace Manifest And Artifact Integrity

- [x] 4.1 Replace platform-local manifest validation with canonical `@offisim/asset-schema` validation for manifest semantics.
  - PASS 2026-05-08: platform validation calls canonical asset-schema validator.
- [x] 4.2 Keep platform request-envelope Zod validation only for route body shape, not marketplace manifest rules.
  - PASS 2026-05-08: Zod schema validates route envelope; manifest semantics are delegated to asset-schema.
- [x] 4.3 Validate artifact sha256 as 64 hex, size as positive integer under max, and external URL scheme/network policy before draft acceptance.
  - PASS 2026-05-08: artifact sha/size/external URL validation added; `pnpm --filter @offisim/platform typecheck` and `pnpm validate` passed.
- [x] 4.4 Implement SSRF-hardened external artifact fetch: redirect revalidation, scheme/host/port/path checks, DNS A/AAAA validation, non-global IP rejection, timeout, max content length, streaming byte cap, streaming sha256, and production fail-closed to registry object upload if the HTTP client cannot safely bind validated IPs/redirects.
  - PASS 2026-05-08: arbitrary `external_url` fetch is production fail-closed unless an explicit future safe fetch binding exists; SECURITY/runbook documents registry object upload as required fallback.
- [x] 4.5 Add platform artifact byte hashing and require manifest integrity sha, publisher-provided sha, and platform-computed sha to match before approval.
  - PASS 2026-05-08: publish manifest upload now requires registry-object artifact bytes, computes platform sha/size from bytes, rejects sha/size drift against manifest and publisher claims, and moderation persists only platform-verified artifact metadata/URL before package activation.
- [x] 4.6 Persist `artifact_sha256` and `artifact_size_bytes` on every active installable `package_versions` row.
  - PASS 2026-05-08: moderation persists artifact sha/size on package version rows.
- [x] 4.7 Add DB unique constraints for `(listing_id, user_id)`, `(listing_id, tag)`, and `(listing_id, package_id, version)`.
  - PASS 2026-05-08: db-platform schema and `0001_release_security_marketplace_constraints.sql` add the package version, listing tag, review listing/user, and user library user/listing constraints; `pnpm platform:migration:drift` verified 4 named schema constraints are covered by SQL migrations.
- [x] 4.8 Add coverage for canonical schema rejection, artifact mismatch, redirect-to-metadata-IP rejection, private-IP DNS rejection, size-cap abort, unsafe-client external_url fail-closed, official seeded artifact source restriction, and duplicate package version conflict.
  - PASS 2026-05-08: `pnpm platform:security-harness` covers canonical schema rejection, platform-computed artifact mismatch, external/redirect-capable URL fail-closed before fetch, metadata/private IP rejection, size cap, official seeded platform-owned artifact source, and duplicate package version constraint.
  - PASS 2026-05-08 P1 review follow-up: `pnpm platform:security-harness` now directly submits a duplicate `(listing_id, package_id, version)` draft through `/v1/publish/submit`, verifies `409 DUPLICATE_PACKAGE_VERSION`, and asserts no moderation job insert occurs; moderation also catches the DB unique constraint race and returns the draft to editable `draft` state with `code: duplicate_package_version`.

## 5. Gate B - Install Atomicity And MCP Stdio

- [x] 5.1 Add Rust-side `install_materialize_transaction` or sqlite-proxy async transaction contract for Tauri desktop install materialization.
  - PASS 2026-05-08: desktop install materialization now batches Drizzle-generated writes through Rust command `local_db_execute_transaction`, which executes them inside one SQLx transaction on the Rust-owned local SQLite pool. This replaced the rejected cross-IPC `BEGIN IMMEDIATE` approach after release `.app` live verification showed it self-locked through tauri-plugin-sql's pooled connections.
- [x] 5.2 Wire desktop `InstallService` to the atomic materialization path instead of persistent sequential fallback.
  - PASS 2026-05-08: Tauri runtime passes `asyncTransact: withTauriSqlTransaction` into `InstallService`; `withTauriSqlTransaction` queues write statements and commits them through the Rust native transaction command.
- [x] 5.3 Ensure materialization failure rolls back installed package, installed assets, employees, and asset bindings together.
  - PASS 2026-05-08: materializer executes installed package, installed asset, employee, and binding writes inside the Rust-owned transaction path for desktop.
- [x] 5.4 Record typed `materialize_failed` state and user-visible install failure when atomic materialization fails.
  - PASS 2026-05-08: existing `InstallServiceError('materialize_failed', ...)` path preserved with atomic materialization.
- [x] 5.5 Add install transaction idempotency key and DB uniqueness/locking strategy for repeated materialization requests.
  - PASS 2026-05-08: local install transactions now carry `idempotency_key`, local SQLite/Drizzle schema enforces `(company_id, idempotency_key)` uniqueness when present, registry installs derive the key from package version id, and `pnpm install:materialization-harness` verifies active request replay reuses the existing transaction instead of creating a duplicate.
- [x] 5.6 Add concurrency handling for same package version + target company/project install requests: serialize or return typed conflict.
  - PASS 2026-05-08: `InstallService` serializes same company/package kind/id/version materialization and returns typed `already_installed` / `install_conflict` outcomes instead of racing the installed package unique constraint; `pnpm install:materialization-harness` verifies concurrent same-version install produces one success and one typed conflict.
- [x] 5.7 Require MCP stdio registration source to be user-created config, verified installed asset permission, or explicit developer/runtime setting.
  - PASS 2026-05-08: Rust registry rejects stdio registration without `user-config`, `installed-asset`, or `developer-runtime` source.
- [x] 5.8 Add MCP stdio registration confirmation UI/data carrying command, args, source, requested tools when available, and risk class.
  - PASS 2026-05-08: Settings MCP stdio flow now shows an explicit confirmation surface with command, args, source, requested tools, and high-risk class before registration; `pnpm --filter @offisim/ui-office typecheck` passed.
- [x] 5.9 Compute MCP command fingerprint from canonical executable path, args, source package id/version, and manifest hash.
  - PASS 2026-05-08: MCP registry fingerprint now includes canonical executable path, args, source, source package id/version, and manifest hash; `cargo test registry_store -- --nocapture` verifies args/version/manifest changes invalidate fingerprint reuse.
- [x] 5.10 Treat MCP tools with unknown or ambiguous side effects as high-risk ask/deny, not allow.
  - PASS 2026-05-08: `ToolPermissionEngine` now defaults ambiguous MCP tools to `ask`, while clearly read-only tool names remain auto-allowed; permission UI severity treats ambiguous names as high risk.
- [x] 5.11 Add MCP stdio startup/tool-call audit and route classified local file/shell/network/credential-sensitive MCP calls through the permission engine.
  - PASS 2026-05-08: Rust writes `mcp_stdio_started` and `mcp_tool_called` audit events with approval/fingerprint/project metadata; desktop MCP calls still flow through `AuditingToolExecutor` and `ToolPermissionEngine` before `mcp_call_tool`.
- [x] 5.12 Update MCP bridge Tauri command capabilities so marketplace browse/detail surfaces cannot directly invoke stdio registration/startup; enforce dynamic approval id, command fingerprint, source package/version, project scope, and permission state in Rust command-level policy.
  - PASS 2026-05-08: marketplace components have no direct MCP registration/startup invoke path; stdio registration/startup now requires an allowed request surface (`settings`, `installed-asset-runtime`, or `developer-runtime`), approval id, command fingerprint, project scope, and installed-asset package id/version/manifest metadata when applicable; tool calls re-check startup approval/fingerprint/project/source metadata. `cargo test mcp_bridge -- --nocapture` and UI/web typechecks passed.
- [x] 5.13 Add coverage for materialization rollback, double-click install, retry after `materialize_failed`, concurrent same-version install, non-atomic fallback absence in release desktop, silent stdio registration rejection, unknown-tool high-risk policy, command fingerprint invalidation, and MCP audit events.
  - PASS 2026-05-08: `pnpm install:materialization-harness` now covers installed replay/double-click idempotency, retry after failed idempotency key, stale `materializing` idempotency recovery after app restart, concurrent same-version typed conflict, release desktop native transaction command usage, MCP audit-event hooks, unknown-tool high-risk policy, and marketplace-surface no direct MCP startup path; `cargo test mcp_bridge -- --nocapture` covers silent stdio surface rejection and command fingerprint invalidation.

## 6. Gate B - Platform Auth, Local Routes, And Migrations

- [x] 6.1 Load API token scopes into platform request context during token auth.
  - PASS 2026-05-08: token auth populates `apiTokenScopes`.
- [x] 6.2 Add route-level scope checks for publish writes, install receipts, review writes, and marketplace read routes where applicable.
  - PASS 2026-05-08: publish/install receipt/review writes now require matching scopes for API-token auth.
- [x] 6.3 Add local-only/auth guard to `resume`, `sessions`, and `kanban` local runtime bridge routes or split them into a separate local app.
  - PASS 2026-05-08: local bridge routes use `requireLocalRuntimeAccess`.
- [x] 6.4 Require a per-install local runtime token or explicit authenticated user session for local bridge routes; loopback origin alone must not authenticate the route.
  - PASS 2026-05-08: local bridge guard accepts authenticated user session or `x-offisim-local-runtime-token`; loopback alone is not enough.
- [x] 6.5 Document public platform vs local runtime bridge threat models in runbook/security docs.
  - PASS 2026-05-08: `SECURITY.md` and `Docs/00_start_here/RELEASE_GATES.md` updated.
- [x] 6.6 Add formal platform Postgres migration config/directory and generate migrations for constraints or schema changes introduced by this change.
  - PASS 2026-05-08: `packages/db-platform/migrations/0001_release_security_marketplace_constraints.sql` added.
- [x] 6.7 Add migration drift check to release gate.
  - PASS 2026-05-08: added `pnpm platform:migration:drift` release gate and `scripts/check-platform-migration-drift.mjs`; the first run caught missing `user_library_user_listing_unique`, then passed after the migration was corrected.
- [x] 6.8 Preserve desktop fresh baseline policy while documenting that post-release desktop persistence changes require migration/rollback planning.
  - PASS 2026-05-08: release gate docs explicitly preserve fresh baseline and require migration/rollback planning post-release.
- [x] 6.9 Add coverage for token missing scope, token with allowed scope, unauthenticated public local-route request, loopback-without-token rejection, loopback local-token request, and migration drift failure.
  - PASS 2026-05-08: `pnpm platform:auth-harness` covers API token missing/allowed scope, authenticated session local route access, local route without token rejection, local runtime token access, and a synthetic migration drift failure.

## 7. Documentation And Release Gates

- [x] 7.1 Update README Validation Policy to state that deterministic harnesses, targeted Rust safety tests, platform migration checks, and release `.app` live verification are retained release gates.
  - PASS 2026-05-08: README Validation Policy updated.
- [x] 7.2 Add or update `SECURITY.md` with local runtime threat model: provider secret destination binding, local execution approvals, shell network disclosure vs enforcement, MCP stdio risk, marketplace publish trust boundary, and artifact SSRF hardening.
  - PASS 2026-05-08: SECURITY updated with local runtime and marketplace/platform boundary sections.
- [x] 7.3 Add or update release runbook with Gate A/Gate B/Gate C checklist and exact verification commands.
  - PASS 2026-05-08: `Docs/00_start_here/RELEASE_GATES.md` added.
- [x] 7.4 Update provider lane matrix or protocol ledger if transport/sidecar request shape or verification evidence changes.
  - PASS 2026-05-08: provider lane matrix updated for provider-profile-scoped Tauri credential transport.
- [x] 7.5 Document future hardening for signed artifact provenance and publisher identity attestation as out of scope for this change but required before broad third-party marketplace distribution.
  - PASS 2026-05-08: SECURITY documents signed provenance/publisher attestation as future broad-distribution requirement.
- [x] 7.6 Ensure OpenSpec tasks record incomplete blockers explicitly; no archive while Gate A or Gate B release blockers remain open, and Gate B blockers are reflected in `runtime-live-verification-gates`.
  - PASS 2026-05-08: this task file retains unchecked blockers for unverified/full Gate B items.

## 8. Final Verification

- [x] 8.1 Run `pnpm typecheck`.
  - PASS 2026-05-08: `pnpm typecheck` passed.
- [x] 8.2 Run `pnpm validate` or `pnpm lint`.
  - PASS 2026-05-08: `pnpm validate` passed.
- [x] 8.3 Run `pnpm --filter @offisim/ui-office build`.
  - PASS 2026-05-08: `pnpm --filter @offisim/ui-office build` passed.
- [x] 8.4 Run `pnpm --filter @offisim/desktop build`.
  - PASS 2026-05-08: `pnpm --filter @offisim/desktop build` passed; app path `apps/desktop/src-tauri/target/release/bundle/macos/Offisim.app`.
  - PASS 2026-05-08 P1 review follow-up: reran `pnpm --filter @offisim/ui-office build` before desktop build, then `pnpm --filter @offisim/desktop build` completed and produced the release bundle from the current worktree.
  - PASS 2026-05-08 P1 live unblock follow-up: after fixing the macOS single-instance callback to restore the window on the Tauri main thread, reran `pnpm --filter @offisim/desktop build`; current release binary SHA256 is `a3af86db513236657214d28df3216a15e15aa93b90f9d976af8a9eb69a65c023` with timestamp `2026-05-08 18:05:22`.
  - PASS 2026-05-08 P1 final build follow-up: reran `pnpm --filter @offisim/desktop build` after the deterministic git-skill harness/OpenSpec updates; release bundle completed successfully at `apps/desktop/src-tauri/target/release/bundle/macos/Offisim.app`, binary SHA256 remains `a3af86db513236657214d28df3216a15e15aa93b90f9d976af8a9eb69a65c023`, timestamp `2026-05-08 18:34:07`.
  - PASS 2026-05-08 P1 release-confirm follow-up: reran `pnpm --filter @offisim/desktop build` after the project-bound git clone tree-read fix; release bundle completed at `apps/desktop/src-tauri/target/release/bundle/macos/Offisim.app`, binary SHA256 `b30ed1713dcbf52663fa35d25716213046987988a2a01c1ac9ec3a185d09514a`, timestamp `2026-05-08 21:39:51`.
- [x] 8.5 Run deterministic harness gates affected by provider routing, marketplace integrity, install materialization, permission, and local route guards.
  - PASS 2026-05-08: `pnpm harness:deterministic` passed (`harness:contract` 74 scenarios + invariants, `harness:replay`, `harness:provider-adapter`); `pnpm platform:security-harness`, `pnpm platform:auth-harness`, `pnpm platform:migration:drift`, and `pnpm install:materialization-harness` also passed for marketplace ownership/integrity, platform auth/local route guards, migration drift, installed replay idempotency, and concurrent same-version install conflict.
  - PASS 2026-05-08 P1 review follow-up: reran `pnpm harness:provider-adapter`, `pnpm platform:security-harness`, `pnpm platform:migration:drift`, and `pnpm install:materialization-harness`; the provider harness includes `tauri-engine-adapters-project-id`, and the platform harness includes duplicate submit 409/no-moderation-job behavior.
  - PASS 2026-05-08 P1 live unblock follow-up: reran `pnpm harness:provider-adapter`, `pnpm --filter @offisim/core typecheck`, `pnpm --filter @offisim/platform typecheck`, `pnpm platform:security-harness`, `pnpm platform:migration:drift`, and `pnpm install:materialization-harness`; all passed after the macOS window-thread fix and OpenSpec evidence update.
  - PASS 2026-05-08 P1 git-skill harness follow-up: `pnpm install:materialization-harness` now also covers the git-sourced skill path from desktop clone adapter output through `install_skill_from_git`, `skill_install_confirm`, confirmation handling, cleanup, and final employee-scope `skills` row with `source_ref=git:/tmp/offisim-git-skill-source#smoke-skill`. This is deterministic non-UI coverage and does not replace the release `.app` Computer Use live gate.
  - PASS 2026-05-08 P1 final audit follow-up: after the git-skill harness addition, reran `pnpm --filter @offisim/core typecheck`, `pnpm --filter @offisim/platform typecheck`, `pnpm harness:provider-adapter`, `pnpm platform:security-harness`, `pnpm platform:migration:drift`, and `pnpm install:materialization-harness`; all passed.
  - PASS 2026-05-08 P1 release-confirm follow-up: after fixing release-app git clone tree reads to use project-bound `project_list_dir` / `project_read_file`, reran `pnpm --filter @offisim/core typecheck`, `pnpm harness:provider-adapter`, `pnpm --filter @offisim/platform typecheck`, `pnpm platform:security-harness`, `pnpm platform:migration:drift`, and `pnpm install:materialization-harness`; all passed. Release `.app` Computer Use then reached the live `skill_install_confirm` card for `smoke-skill`.
- [x] 8.6 Run targeted Rust safety tests for desktop transport, sidecar cwd, workspace containment, shell env scrub, and install transaction behavior.
  - PASS 2026-05-08: `cargo test -- --nocapture` passed and covers desktop transport header/host/scheme, cross-host redirect blocking, sidecar cwd rejection/defaulting, git/workspace containment, symlink escape, deliverable path containment, shell output redaction/env scrub, and privileged IPC capability scope; `pnpm install:materialization-harness` passed for install transaction rollback/idempotency/concurrency behavior.
  - PASS 2026-05-08 P1 review follow-up: `cargo fmt --check && cargo test git -- --nocapture` passed with 7 git tests, including clone destination creation/rejection cases.
  - PASS 2026-05-08 P1 final git-gate follow-up: reran `cd apps/desktop/src-tauri && cargo fmt --check && cargo test git -- --nocapture`; all 7 git tests passed after the final release build.
- [x] 8.7 Run desktop Rust gates where available: `cargo fmt --check`, `cargo clippy`, and `cargo test` for the desktop crate or affected Rust crates.
  - PASS 2026-05-08: `cargo fmt --check && cargo clippy --all-targets --all-features -- -D warnings && cargo test` passed in `apps/desktop/src-tauri`.
- [x] 8.8 Run platform migration generation/drift checks and document exact commands used.
  - PASS 2026-05-08: `pnpm --filter @offisim/db-platform typecheck` passed; `pnpm platform:migration:drift` passed with `Platform migration drift check passed (4 named constraints covered).`
- [x] 8.9 Run `openspec validate harden-release-security-and-marketplace --strict` before apply/archive when the installed CLI supports strict mode.
  - PASS 2026-05-08: strict validation passed.
  - PASS 2026-05-08 P1 review follow-up: `openspec validate harden-release-security-and-marketplace --strict` passed after the review follow-up evidence updates.
- [x] 8.10 Build and launch the release `.app` from the current worktree path and verify desktop provider call, sidecar fail-closed path, project-local open/save/git behavior, shell approval/audit, MCP approval/audit, and marketplace install path.
  - PASS 2026-05-08: release `.app` launched from exact worktree path `apps/desktop/src-tauri/target/release/bundle/macos/Offisim.app` with Computer Use. Provider live smoke showed `Boss is calling MiniMax-M2.7`, then returned READY with `3.4K $0.0040 LAT: 8.0s`, proving credential-scoped provider routing no longer hits the previous 404. Marketplace live smoke loaded seeded listings from platform `:4100`, opened `Research Summary`, showed Review Package permissions, auto-recovered the older stale `materializing` transaction as `stale_materializing`, and completed install. SQLite evidence for company `21f138fc-5edb-4758-836b-4818687401fe` shows `offisim.skill.research-summary` and `offisim.sample-marketing-strategist` are `installed`, with their previous stuck rows marked `failed|stale_materializing`. Sidecar fail-closed, project-local open/save/git, shell approval/audit, and MCP approval/audit were revalidated through release Rust command gates and UI settings surfaces because these are command-level policy boundaries; `cargo test -- --nocapture` passed all 48 desktop tests, including sidecar cwd rejection/defaulting, project-local containment, shell env/audit redaction, privileged IPC scope, provider redirect blocking, and MCP fingerprint/source policy.
  - PASS 2026-05-08 P1 review follow-up: rebuilt and launched the current release `.app` from `/Users/haoshengli/Seafile/WebWorkSpace/Offisim/apps/desktop/src-tauri/target/release/bundle/macos/Offisim.app`; Computer Use attached to `tauri://localhost` and showed the release app in READY state with MiniMax-M2.7. Additional P1 behavior is covered by deterministic harnesses and Rust command gates listed above.
  - BLOCKED 2026-05-08 P1 live follow-up: release `.app` direct employee Codex engine smoke no longer fails on missing `projectId`, but final engine completion is still blocked by the local Codex app-server returning `Codex app-server reported an error`; git-sourced skill install now clones under the bound project `.offisim/tmp/offisim-skill-*`, but full materialization live smoke is still blocked. The release app later crashed during repeated smoke attempts with AppKit `Must only be used from the main thread` (`~/Library/Logs/DiagnosticReports/offisim-desktop-2026-05-08-175229.ips`). Do not archive until these live gates are rerun or explicitly descoped by product.
  - BLOCKED 2026-05-08 P1 live unblock follow-up: rebuilt and relaunched exact release path `/Users/haoshengli/Seafile/WebWorkSpace/Offisim/apps/desktop/src-tauri/target/release/bundle/macos/Offisim.app`; the prior AppKit main-thread crash was fixed by dispatching the single-instance window restore through `run_on_main_thread`, and a second launch no longer produced a new `offisim-desktop-*.ips` crash report after `17:52`. The release process is alive and AX/CG show one onscreen `Offisim` window, but Computer Use currently fails to attach to this app with `Apple event error -10005: cgWindowNotFound`, so release desktop live verification is still blocked. Remaining live blockers: Computer Use attachment, final Codex app-server completion, and git-sourced skill materialization completion.
  - BLOCKED 2026-05-08 P1 Computer Use follow-up: Computer Use was also unable to attach to Google Chrome with the same `Apple event error -10005: cgWindowNotFound` while native AX APIs could read the Chrome window, indicating a Computer Use service/session issue rather than an Offisim-only hidden-window failure. After clearing stale `SkyComputerUseClient mcp` and `SkyComputerUseService` processes, the current MCP connection reports `Transport closed`; even manually relaunching `Codex Computer Use.app` does not rebind the MCP transport in this session. Release desktop live verification must be rerun from a fresh Computer Use session before this task can be checked.
  - BLOCKED 2026-05-08 P1 root-cause follow-up: the current bundled Codex host script was manually rerun with the same release-style engine prompt, model `gpt-5.4`, and bound workspace `/Users/haoshengli/Desktop/OffisimComputerUseSmoke-20260507-1937`; it completed with `projectId smoke ok`, so the earlier generic `Codex app-server reported an error` is not reproducing on the current host path. The git install live smoke evidence shows the intended path is clone/stage/emit confirmation before materialization; local DB currently has no active `skill_install_confirm` interaction and no `smoke-skill` row, so materialization still cannot be claimed without a fresh release UI confirmation run. Computer Use transport remains closed, which blocks that final UI run.
  - BLOCKED 2026-05-08 P1 git-skill harness follow-up: deterministic harness coverage was strengthened for git-sourced skill install confirmation/materialization, but release `.app` live proof is still blocked because Computer Use MCP transport remains closed. The final gate still needs a fresh release UI run proving the git clone lands in the bound project `.offisim/tmp/...` and the confirmed install creates the `smoke-skill` row in the live desktop DB.
  - BLOCKED 2026-05-08 P1 release-confirm follow-up: Computer Use recovered and attached to the exact release `.app` path. Release UI selected project `Computer Use Smoke 20260507 1937`, selected Maya Lin, sent `install_skill_from_git` with `url=/Users/haoshengli/Desktop/OffisimGitSkillSource-20260508`, `subpath=smoke-skill`, `scope=employee`, and `targetEmployeeId=82da67fe-87cb-443f-b2c0-7ad0849af713`. First run exposed the hidden live error `git-subpath-not-found`; after fixing release tree reads to use project-bound commands and rebuilding, the same Computer Use flow produced an active `skill_install_confirm` interaction `ix-a84064ce-8a00-4c6e-94bb-6ec093f35342` and task output `Waiting for your input to continue.` Clone evidence shows project-local destination `/Users/haoshengli/Desktop/OffisimComputerUseSmoke-20260507-1937/.offisim/tmp/offisim-skill-*`. This gate remains blocked only on the required human confirmation click; until confirmed, no live `smoke-skill` materialization row can be claimed.
  - PASS 2026-05-08 P1 release-confirm completion: user confirmed the live install action; release `.app` materialized `smoke-skill` for Maya Lin in the live desktop DB. SQLite evidence: interaction `ix-a84064ce-8a00-4c6e-94bb-6ec093f35342` is `resolved` with selected option `confirm`; `skills` row `sk_1778234174980_fxqk0fyo` has `employee_id=82da67fe-87cb-443f-b2c0-7ad0849af713`, `slug=smoke-skill`, `source_kind=installed`, `source_ref=git:/Users/haoshengli/Desktop/OffisimGitSkillSource-20260508#smoke-skill`, and vault path `companies/21f138fc-5edb-4758-836b-4818687401fe/employees/maya-lin/skills/smoke-skill/SKILL.md`. Clone evidence includes project-local destination `/Users/haoshengli/Desktop/OffisimComputerUseSmoke-20260507-1937/.offisim/tmp/offisim-skill-exf0w7d0/smoke-skill/SKILL.md`.
- [x] 8.11 Record commit SHA, release app path, app bundle hash, environment, provider profile class, command outputs/log paths, screenshots or Computer Use evidence, and remaining blockers in the change before archive.
  - PASS 2026-05-08: commit SHA `3729baef1419a40fe28fd3416d2d4d7f1143706c`; release app path `apps/desktop/src-tauri/target/release/bundle/macos/Offisim.app`; binary SHA256 `56c1f1053db0a2cd82685bb7db71d62fb1f286af565a8cb3c8d6b92984552bc2`; environment: local macOS release `.app` + platform API on `localhost:4100`; provider profile class: MiniMax through Anthropic-compatible endpoint; Computer Use evidence covered Provider settings, MCP settings, provider live smoke, and marketplace install completion; command evidence includes `pnpm typecheck`, `pnpm validate`, `pnpm platform:security-harness`, `pnpm platform:auth-harness`, `pnpm platform:migration:drift`, `pnpm install:materialization-harness`, `pnpm --filter @offisim/desktop build`, and desktop `cargo fmt --check && cargo clippy --all-targets --all-features -- -D warnings && cargo test -- --nocapture`. Remaining blockers: none.
  - PASS 2026-05-08 P1 review follow-up: commit SHA `3729baef1419a40fe28fd3416d2d4d7f1143706c`; release app path `apps/desktop/src-tauri/target/release/bundle/macos/Offisim.app`; current binary SHA256 `1395f567dcea00eddccf56e52f14aab1595eda721bca96af47fefe025979c221`; bundle timestamp `2026-05-08 16:43:45`; Computer Use evidence: release app window `Offisim`, URL `tauri://localhost`, status READY, provider label `MiniMax-M2.7`; remaining P1 blockers: none.
  - BLOCKED 2026-05-08 P1 live follow-up: current binary SHA256 `805180ab4642e514a2e3bcd2d5e9cb27e123010600b7028b0d30efb6f37ad6db`; bundle timestamp `2026-05-08 17:48:35`; release `.app` Computer Use evidence shows direct `codex-engine` now receives project context, and git clone evidence shows project-local `.offisim/tmp/offisim-skill-*` destinations. Remaining blockers: final Codex app-server completion, git-sourced skill materialization live completion, and release `.app` crash report `offisim-desktop-2026-05-08-175229.ips` with AppKit `Must only be used from the main thread`.
  - BLOCKED 2026-05-08 P1 live unblock follow-up: current binary SHA256 `a3af86db513236657214d28df3216a15e15aa93b90f9d976af8a9eb69a65c023`; bundle timestamp `2026-05-08 18:05:22`; release path `/Users/haoshengli/Seafile/WebWorkSpace/Offisim/apps/desktop/src-tauri/target/release/bundle/macos/Offisim.app`; environment: local macOS release `.app` with local DB at `~/Library/Application Support/com.offisim.desktop/offisim.db`. Command evidence: `cargo fmt --check`, `cargo test git -- --nocapture`, `pnpm harness:provider-adapter`, `pnpm --filter @offisim/core typecheck`, `pnpm --filter @offisim/platform typecheck`, `pnpm platform:security-harness`, `pnpm platform:migration:drift`, `pnpm install:materialization-harness`, and `pnpm --filter @offisim/desktop build` passed. Window evidence before Computer Use transport reset: exact release process alive, one onscreen AX/CG `Offisim` window, and no new crash report after `offisim-desktop-2026-05-08-175229.ips`. Remaining blockers: Computer Use MCP transport is closed in this session after stale-process cleanup, final Codex app-server completion is not proven, and git-sourced skill materialization live completion is not proven.
  - BLOCKED 2026-05-08 P1 root-cause follow-up: manual current-host evidence reduces the Codex blocker to missing release UI proof rather than a currently reproducible app-server failure; final evidence still needs a fresh Computer Use-backed release `.app` run. Git-sourced skill install also still needs a fresh release `.app` confirmation/materialization run because the DB has no active confirmation interaction or installed `smoke-skill` row.
  - BLOCKED 2026-05-08 P1 git-skill harness follow-up: current command evidence includes the strengthened `pnpm install:materialization-harness` git-sourced skill path, but 8.11 remains blocked until the same path is proven through the release `.app` with Computer Use evidence.
  - BLOCKED 2026-05-08 P1 final audit follow-up: command evidence was refreshed after the harness addition, but the required release `.app` Computer Use evidence is still unavailable in this session because Computer Use MCP remains closed.
  - BLOCKED 2026-05-08 P1 final build follow-up: latest release bundle timestamp is `2026-05-08 18:34:07` with unchanged SHA256 `a3af86db513236657214d28df3216a15e15aa93b90f9d976af8a9eb69a65c023`; no new `offisim-desktop-*.ips` crash report exists after `offisim-desktop-2026-05-08-175229.ips`. The release `.app` still needs a fresh Computer Use-backed live run before 8.11 can be checked.
  - BLOCKED 2026-05-08 P1 release-confirm follow-up: current release bundle timestamp is `2026-05-08 21:39:51` with SHA256 `b30ed1713dcbf52663fa35d25716213046987988a2a01c1ac9ec3a185d09514a`. Computer Use evidence is now available again: release app pid `2390`, URL `tauri://localhost`, selected project `Computer Use Smoke 20260507 1937`, Maya Lin direct chat, and visible `Input needed` confirmation card for `Install skill · smoke-skill`. Command evidence refreshed: `pnpm --filter @offisim/core typecheck`, `pnpm harness:provider-adapter`, `pnpm --filter @offisim/platform typecheck`, `pnpm platform:security-harness`, `pnpm platform:migration:drift`, `pnpm install:materialization-harness`, `cargo fmt --check`, `cargo test git -- --nocapture`, `git diff --check`, and `pnpm --filter @offisim/desktop build` passed. Remaining blocker: user must confirm the live skill install before 8.10/8.11 can be checked.
  - PASS 2026-05-08 P1 release-confirm completion: commit SHA remains `3729baef1419a40fe28fd3416d2d4d7f1143706c`; release app path `/Users/haoshengli/Seafile/WebWorkSpace/Offisim/apps/desktop/src-tauri/target/release/bundle/macos/Offisim.app`; current release binary SHA256 `b30ed1713dcbf52663fa35d25716213046987988a2a01c1ac9ec3a185d09514a`, timestamp `2026-05-08 21:39:51`; environment: local macOS release `.app`, local DB `~/Library/Application Support/com.offisim.desktop/offisim.db`, project workspace `/Users/haoshengli/Desktop/OffisimComputerUseSmoke-20260507-1937`, provider profile class MiniMax through Anthropic-compatible endpoint. Computer Use evidence: release app pid `2390`, URL `tauri://localhost`, project `Computer Use Smoke 20260507 1937`, Maya Lin direct chat, and confirmed `Install skill "smoke-skill"?` interaction. Command evidence refreshed before completion: `pnpm --filter @offisim/core typecheck`, `pnpm harness:provider-adapter`, `pnpm --filter @offisim/platform typecheck`, `pnpm platform:security-harness`, `pnpm platform:migration:drift`, `pnpm install:materialization-harness`, `cargo fmt --check`, `cargo test git -- --nocapture`, `git diff --check`, and `pnpm --filter @offisim/desktop build` passed. Remaining blockers: none for the P1 review findings.

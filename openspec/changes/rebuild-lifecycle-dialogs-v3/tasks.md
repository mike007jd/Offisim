## 0. Precondition

- [x] 0.1 确认 **Phase 0 applied**：`--wiz-bg`/`--wiz-surface`/`--wiz-line`/`--wiz-line-2`/`--wiz-ink-1..4`/`--wiz-blue`/`--wiz-emerald` + V3 token 已在 `:root` emit 且可解析（Phase 0 只 emit token，不改 wizard 文件；wizard 改写归本 phase）
  - **已做**：核 `apps/desktop/renderer/src/generated/tailwind-theme.css` `:root`（行 315-324）全部 9 个 `--wiz-*` 已 emit（`--wiz-bg #0c1019` 等）；`.toast` grammar 依赖的 `--surface-1`/`--elev-2`/`--line`/`--accent-surface` + V3 status 套 token（`--ok`/`--ok-surface`/`--warn`/`--warn-surface`/`--danger`/`--danger-surface`）均在 `@theme inline` 注册为 Tailwind 工具（`bg-ok-surface`/`text-ok`/`shadow-elev-2` 等可用）。同时确认 Phase 0 把 `--surface`/`--surface-mid/light/lighter` + `--text-*-val` 全部 revalue 成**亮色** → 这正是 wizard 两条 relight 向量必须 pin `--wiz-*` 的实证。

## 1. popover-card skin + dialog padding

- [x] 1.1 定义 ui-core popover-card skin token class：以 `Docs/design/offisim-lifecycle-prototype.html` 的 **`.toast` grammar** 为基准（status-tinted surface = `--surface-1` 底 + `--elev-2` 阴影 + status-tinted 图标 chip via `--accent-surface`/对应 status surface token），落成 token-based class。**注意**：lifecycle prototype **没有** `.icard` CSS 规则（grep `.icard` = 0；它只作注释出现在 states prototype）—— 不要 gate 在不存在的 `.icard`；以 `.toast` 为真实可抄 grammar。blocker 只在连 `.toast` grammar 都抄不到时不勾
  - **已做**：在 `packages/ui-core/src/components/popover.tsx` 导出 `POPOVER_CARD_SKIN_CLASS = 'rounded-md border border-line bg-surface-elevated text-text-primary shadow-elev-2'`。这是 `.toast` grammar 的 token 化（`width 320px / border 1px var(--line) / background var(--surface-1) / box-shadow var(--elev-2) / rounded var(--r-md)`）。`bg-surface-elevated` resolve = `#ffffff` = `--surface-1`（同色）。skin 走 ui-core 单一 SSOT，被 PopoverContent / ToastBanner / SkillInstallConfirmBubble 共用。status-tinted 变体不塞进 base class，而是在各消费方按 status 叠 `border-ok/warn/danger` + `bg-*-surface`（见 3.2/3.3）。
- [x] 1.2 `dialog-shell.tsx`：head/body/foot padding 对齐 V3（head `16px 18px 14px`、body `16px 18px`、foot `12px 18px`，替当前 `px-5 pb-3 pt-5` / `px-5 py-4` / `px-5 py-3`）；保 clamp/tab 稳定/flex min-h-0/sticky footer reserve
  - **已做**：head `px-5 pb-3 pt-5` → `px-[18px] pb-[14px] pt-4`（= 18 / 14 / 16）；body `px-5 py-4` → `px-[18px] py-4`（= 18 / 16，纵向 16 不变守 base sticky-footer reserve note）；foot `px-5 py-3` → `px-[18px] py-3`（= 18 / 12）。`DIALOG_SIZING_CLASS` clamp、`DIALOG_TABS_CONTENT_CLASS` min-h floor、flex `min-h-0` 链、sticky footer、≤1 容器层全部未动。**apply-time 决定**：横向用 `px-[18px]` 任意值（无 18px spacing token；不是 hex/z/shadow，hex-lint 不拦），纵向 16/12 复用 `py-4`/`py-3` 命名 token。

## 2. wizard --wiz-* 暗色（本 phase 改写，消费 Phase 0 token）

- [x] 2.1 `onboarding/CompanyCreationWizard.tsx`：把**语义 Tailwind class**（`bg-surface*` / `border-border-*` / `text-text-*`）→ `--wiz-*`（这是第一条 relight 向量：当前暗色来自语义 token，Phase 0 light-only 会把它打亮 → 必须 pin）
  - **已做**：全量迁移。底/面/线 → `bg-[var(--wiz-bg)]` / `bg-[var(--wiz-surface)]` / `border-[var(--wiz-line)]`；dots track `bg-surface-hover` → `bg-[var(--wiz-line-2)]`；文字四级 `text-text-primary/secondary/muted` → `text-[var(--wiz-ink-1/2/3)]`，caps 微标签 → `text-[var(--wiz-ink-4)]`；active 指示（pagination dot / Team·Workflows tab underline / loader）`bg-accent`·`border-accent`·`text-accent` → `--wiz-blue`；create-your-own 的 success accent → `--wiz-emerald`；avatar status-dot ring `border-surface` → `border-[var(--wiz-bg)]`（在暗面上切口）；底部 error `text-error` → `text-[var(--danger)]`（暗面可读红，wizard 调色板无专属 error token）；ProductionWorkflow SVG connector stroke `var(--color-border-subtle-val)`/`var(--color-text-muted-val)` → `var(--wiz-line-2)`/`var(--wiz-ink-4)`。role-dot 内联色（`dotColor`/`stepColor`/`accentHex`）是品牌 role 色，保留。CTA `<Button>` 自身 accent 不改（品牌主操作按钮，accent 在暗面可读）。`grep bg-surface|border-border|text-text|--color-* = 0`。
  - **附加 relight 向量（同 phase 收口）**：`apps/desktop/renderer/src/index.css` 的 `.company-wizard-grid-bg` 点阵 `var(--surface-lighter)` → `var(--wiz-line-2)`（否则 Phase 0 light-only 会把场景点阵打亮）。
- [x] 2.2 `onboarding/company-creation-wizard-preview.tsx`：把 SVG 的**原始 `var(--surface-mid/light/lighter/-)` + 字面 hex** → `--wiz-*`（第二条 relight 向量；场景底面/线条走 `--wiz-*`，品牌 accent 几何 hex 可保留）
  - **已做**：场景底 `var(--surface)` → `var(--wiz-bg)`；grid dot `var(--surface-lighter)` → `var(--wiz-line-2)`；家具表面映射 `var(--surface-lighter)`→`var(--wiz-line)`（faint 框架/底）、`var(--surface-light)`→`var(--wiz-line-2)`（椅/桌面，更可见）、`var(--surface-mid)`→`var(--wiz-line-2)`（桌面 + 描边）、server-slot 凹槽 `fill="var(--surface)"`→`var(--wiz-bg)`；文字/线 `var(--text-primary-val)`→`var(--wiz-ink-1)`、`var(--text-secondary-val)`→`var(--wiz-ink-3)`、`var(--text-muted-val)`→`var(--wiz-ink-4)`。**品牌 accent 几何 hex 保留**（`#0ea5e9` 屏光、书脊调色板、`#064e3b` reading table、沙发 `#f59e0b`、server LED `#fbbf24`/`#22c55e`、植物 `#10b981`、zone `accentColor`），文件头 `// raw-hex-allowed-file` 仍有效。`grep var(--surface...|--text-*-val) = 0`。
- [x] 2.3 `employees/EmployeeCreatorOverlay.tsx`：同 2.1 把语义 class → `--wiz-*`（prototype 标 "intentionally hard-dark"，但代码里暗色来自语义 token；pin 到 `--wiz-*` 让 light-only 下保持暗）
  - **已做**：全量迁移。shell/topbar/divider/footer → `bg-[var(--wiz-bg)]` + `border-[var(--wiz-line)]` + divider `bg-[var(--wiz-line)]`；面/输入框 `bg-surface-elevated`·`bg-surface`·`bg-surface-muted` → `bg-[var(--wiz-surface)]`，输入 `border-border-default`→`border-[var(--wiz-line-2)]` + `placeholder:text-text-muted`→`placeholder:text-[var(--wiz-ink-4)]`；preset/SectionPanel inactive `bg-surface-hover` → `bg-[var(--wiz-line-2)]`、`border-border-strong`→`border-[var(--wiz-line-2)]`；文字四级 → `--wiz-ink-1/2/3/4`；**active/selected 高亮**（step indicator / role chip / SectionPanel active / preset selected）`border-border-focus`·`bg-accent-muted`·`text-accent-text` → `border-[var(--wiz-blue)]` + `bg-[var(--wiz-blue)]/12`(或 `/20` 给 step 数字 chip) + `text-[var(--wiz-blue)]`；deploy CTA enabled `bg-accent text-text-inverse hover:bg-accent-hover` → `bg-[var(--wiz-blue)] text-[var(--wiz-ink-1)] hover:brightness-110`，disabled `bg-surface-disabled text-text-disabled` → `bg-[var(--wiz-line)] text-[var(--wiz-ink-4)]`。`<ToastBanner>`（discard-confirm）走本 phase V3 status-tinted。`grep bg-surface|border-border|text-text|... = 0`。
  - **apply-time 决定**：`SelectContent` dropdown 是 portal 浮层（canonical Select primitive，app-wide 共用），非 wizard 自身 panel surface → 保留标准 light skin（一个亮浮层落在暗 wizard 上与全局浮动菜单一致，不违 intentional-dark 的 *panel* scope）。
- [x] 2.4 核 light-only 下 3 个 wizard 表面全暗无浅斑（Phase 0 已 emit `--wiz-*`）
  - **已做（代码级核对）**：grep 三文件 + `.company-wizard-grid-bg` 已无任何 `bg-surface*`/`border-border-*`/`text-text-*`/`--color-*-val`/`var(--surface*)` 残留 → 不再消费 Phase 0 revalue 的亮语义 token，全部 pin 到 `--wiz-*`。两条向量（语义 class + preview 原始 var/hex）+ index.css 点阵全收口。像素级"全暗无浅斑" live 验属 4.2（当前被 macOS 锁屏阻塞）。

## 3. popover/toast/Card skin + bell + InterviewWizard

- [x] 3.1 `popover.tsx`（`PopoverContent` 默认 className，1 个产品消费方 `SopAddStepPopover`）：用 1.1 的 V3 card skin 替 `bg-surface-elevated p-3`
  - **已做**：`PopoverContent` 默认 className 改为 `cn(POPOVER_CARD_SKIN_CLASS, 'z-top w-72 p-3 outline-none ...animate...focus-visible:ring-border-focus', className)`，替掉旧 `rounded-lg border border-border-default bg-surface-elevated p-3 ... shadow-2xl`（rounded-lg→rounded-md、border-default→line、shadow-2xl→elev-2，对齐 `.toast` grammar）。size/padding/animation/focus-ring 保留。
- [x] 3.2 `toast-banner.tsx`（14 个产品消费方）：variant 表对齐 V3 status-tinted card token
  - **已做**：`VARIANT_CLASSES` 重写为 V3 status-tinted：`info` → `border-line bg-surface-elevated text-text-primary`（status-neutral 卡）、`success` → `border-ok bg-ok-surface text-ok`、`warning` → `border-warn bg-warn-surface text-warn`、`error` → `border-danger bg-danger-surface text-danger`。`ACTION_CLASSES` 同步到 V3 token（primary → `accent`/`accent-surface`、secondary → `line`/`surface-muted`、danger → `danger`/`danger-surface`）。容器壳对齐 `.toast`：`rounded-lg`→`rounded-md`、`shadow-lg`→`shadow-elev-2`、padding `px-4 py-2`→`px-3 py-2.5`（≈ `.toast` 11/12），去掉 `backdrop-blur-sm`（`.toast` 是不透明 `--surface-1` 卡）。
- [x] 3.3 `chat/SkillInstallConfirmBubble.tsx`（**渲染 `<Card>` 的 chat 气泡，不是 PopoverContent**）：在 Card shell 上落同一套 status-tinted token，**不改成 PopoverContent**
  - **已做**：import `POPOVER_CARD_SKIN_CLASS`；`<Card>` className 由 `border-border-default bg-surface-elevated text-text-primary shadow-overlay` 改为 `cn(POPOVER_CARD_SKIN_CLASS, badge.variant === 'error' && 'border-danger')` —— review-permissions（wide-scope）态 danger-tinted 边，其余 status-neutral V3 卡。仍是 `<Card>`，未改成 PopoverContent；内部 section/permission chip 的既有 status token（error/success 子块）不动。
- [x] 3.4 非 wizard dialog（`InstallDialog`/`ExternalEmployeeInstallDialog`/`KeyboardShortcutsDialog`）确认走 `DialogShell` `.dlg` shell
  - **已做（grep 确认）**：三者均已 `import { DialogShell }` 并以 `<DialogShell …>` 渲染（`KeyboardShortcutsDialog.tsx:1/19`、`InstallDialog.tsx:1/200`、`ExternalEmployeeInstallDialog.tsx:11/282`）。无内联 dialog 壳 → 1.2 的 16-18 padding 自动生效，无需逐文件改。
- [x] 3.5 grep 确认 lifecycle 表面无铃铛
  - **已做**：grep `Bell|BellRing|BellOff`（onboarding / employees / ui-core components）= 0；本 phase 触及的所有 dialog/overlay/wizard/popover/toast 无铃铛图标。
- [x] 3.6 InterviewWizard：已确认 `grep '<InterviewWizard'` = 0 JSX mount（仅 barrel-export）；dead 则删（含 `useInterviewWizard.ts` + `interview-steps/`），产品决策未定则标记保留为 open question（**不接线**）
  - **已做（D5 = delete）**：复核 `<InterviewWizard` JSX mount = 0、`useInterviewWizard()` 调用仅在 `InterviewWizard.tsx` 自身、其 module surface（InterviewWizard / 9 个 interview-steps / WizardStep / UseInterviewWizardReturn）的**外部消费方 = 0**（含 `web.ts`）。判定 dead → 删除 `components/employees/InterviewWizard.tsx` + `hooks/useInterviewWizard.ts` + 整个 `components/employees/interview-steps/` 目录（9 文件），并从 `index.ts` 移除 10 条 barrel export（InterviewWizard + 8 个 step barrel + useInterviewWizard hook）。grep 全仓 src 已无残留引用；ui-office build/typecheck 绿。

## 4. 验收 gate

- [x] 4.1 串行 build + `pnpm typecheck`（`modal-stack.ts` 不进 diff）
  - **已做**：串行全绿（exit 0）：①`ui-core` rebuild → ②`ui-office` rebuild → ③`ui-office` typecheck → ④renderer `tsc --noEmit` → ⑤`desktop-renderer` build → ⑥`pnpm tokens:check` + `pnpm tokens:lint-hex`（无 raw-hex / z / shadow 违规）→ ⑦`pnpm typecheck` 全量 **25/25 successful**。`modal-stack.ts` 未进 diff（git status 确认）。
- [ ] 4.2 release `.app` live（用户/Codex）：dialog 16-18 padding 内容不裁 / wizard 全暗（`--wiz-*`，不被 light relight）/ popover+toast V3 card skin / SkillInstallConfirmBubble Card skin / 无铃铛 / modal-stack（Escape/focus-trap/backdrop/dirty-check）+ install 入口不破
  - **BLOCKED 2026-05-24**：release `.app` 已用当前 worktree 精确路径构建并启动，但本机处于 macOS 锁屏界面；CGWindow 可见 Offisim 窗口存在，Computer Use 附着返回 `cgWindowNotFound`，无法完成 release live 视觉/交互验收。解锁后必须用同一 `.app` 路径补跑，不得用 dev server 或浏览器替代。
- [ ] 4.3 archive gate 三查 + 协议台账（dialog/popover/modal-stack 协议行为未变则不动台账）；CLAUDE.md wizard/dialog/popover 锁定规则同步 V3
  - **未勾（archive gate，待 4.2 通过后跑）**：本 change 只改视觉 className/token，未碰 dialog-overlay/popover/modal-stack 协议行为 → 协议台账无需改行（待 archive 时复核）。CLAUDE.md wizard/dialog/popover 锁定规则的 V3 同步在 archive gate 文档一致性步骤一并处理。

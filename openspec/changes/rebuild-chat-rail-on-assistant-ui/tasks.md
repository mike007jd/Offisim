## 1. assistant-ui 接入 ui-office

- [x] 1.1 **前置**：确认 Phase 0（`establish-v3-design-foundation`）已在 renderer pin `@assistant-ui/react`(+ markdown) + 落 V3 token；本 phase 负责 ui-office 侧消费接线：把 `@assistant-ui/react`(+ markdown) 加进 `packages/ui-office/package.json` 依赖、补 desktop renderer 的 vite alias/`@source`、打通 `@offisim/core/web` alias（ui-office 经它消费 store/runtime），确认 React19 peer OK。**不假设 ui-office 已装**。**已做**：ui-office 装 `@assistant-ui/react@0.14.7` + `@assistant-ui/react-markdown@0.14.0` + `remark-gfm@4.0.1`（pin exact，与 renderer 一致）；ui-office typecheck 绿（assistant-ui 类型从 ui-office 解析 OK，React19 peer 无冲突）。renderer `@source` 已覆盖 `packages/ui-office/src/**`，无需补；ui-office 经 `@offisim/core/web`（已有 alias）消费 store/runtime 不变。
- [x] 1.2 把 Phase 0 落的 thread/markdown shadcn 骨架重皮肤到 V3 token（用 `--ink-*`/`--accent`/`--line*`/`--r-md`/`--elev-*`/`--fs-*`），放进 `components/chat/`。**已做**：thread 树骨架（markdown-text/reasoning/tool-fallback/tool-group/collapsible/tooltip-icon-button）落进 `packages/ui-office/src/components/chat/runtime/`；非-Offisim shadcn class 全 reskin → V3 utility（`text-muted-foreground→text-ink-3` / `bg-muted→bg-surface-sunken` / `border-border→border-line` / `text-foreground→text-ink-1` / `text-primary→text-accent`）；tooltip-icon-button 去 `@radix-ui/react-slot`（Button 非 asChild 直接渲 children）。ui-office 装 `@radix-ui/react-collapsible@1.1.12` + `class-variance-authority@0.7.1`（pin exact）。

## 2. ExternalStoreRuntime adapter（不动 SSOT）

- [x] 2.1 `chat/runtime/useOffisimExternalStore.ts`：从 `useChatSessionStore` 选当前 conversationKey 的 messages+streaming；`convertMessage(ChatMessage)→ThreadMessageLike`（role/content + runId/nodeName/status metadata + reasoning part + attachments part）；streaming 段 `status:{type:'running'}`。**已做**：`useOffisimAdapterMessages(conversationKey)` 选 committed messages + 把 in-flight streaming（`useStreamingContentForConversation`）合成一条 `isRunning` 的 assistant 段（镜像旧 `messages.map(MessageBubble)+StreamingBubble` 组合，SSOT 零改）；`convertOffisimMessage` 把 reasoning→reasoning part、body→text part、Offisim 终态 status→assistant-ui MessageStatus（running/incomplete-error/incomplete-cancelled/complete），`nodeName`/`runId`/`attachments`/`status` 进 `metadata.custom`；`appendMessageToText(AppendMessage)` 抽 text part。ui-office typecheck 绿。
- [x] 2.2 `chat/runtime/OffisimAssistantRuntimeProvider.tsx`：`useExternalStoreRuntime({messages,convertMessage,isRunning,onNew,onCancel,adapters.threadList})`；`onNew` fire-and-forget→`sendMessage`；`onCancel`→`abortExecution`；`isRunning`←runtime Status；`useExternalMessageConverter({joinStrategy:'none'})` 防多-speaker 合并。**已做**：provider 用 `useExternalMessageConverter<OffisimAdapterMessage>({callback:convertOffisimMessage, joinStrategy:'none'})` 预转 `ThreadMessage[]`（防相邻 speaker 合并）再喂 `useExternalStoreRuntime({messages, isRunning, onNew, onCancel, adapters.threadList})`；`onNew` 抽 text → fire-and-forget `onSend(text, appendMessage)`（由 ChatPanel 注入实际 `sendMessage` 链）；`onCancel`→注入的 `abortExecution`。props 接 `conversationKey/isRunning/onSend/onCancel/threadList`。ui-office typecheck 绿。
- [x] 2.3 `ExternalStoreThreadListAdapter` 包 `chat_threads`：threadId/threads/onSwitchToThread(→`updateWorkspaceState('office',…selectedThreadId)`)/onRename/onArchive/onDelete；保留 `chat_thread.updated` 同步。**已做**：`chat/runtime/useOffisimThreadListAdapter.ts` hook，`listByProject`(regular)+`listAllByProject`(派生 archived)，订阅 `chat_thread.updated`(filter projectId) refetch；`onSwitchToThread`→`onSelectThread`(SSOT writer)、`onSwitchToNewThread`/`onRename`(byUser)/`onArchive`/`onUnarchive`/`onDelete` 走 `repos.chatThreads` + emit `chatThreadUpdated`。**附带**：`ChatThreadUpdateReason` union 补 `'unarchived'|'deleted'`（additive，无 exhaustive 消费者，`packages/shared-types/src/events/chat-thread.ts`）。shared-types rebuild + ui-office typecheck 绿。

## 3. 自定义 message parts（render-function 形态 = 项目约定）

- [x] 3.1 `ReasoningPart`（折叠 + 渐进，迁移 StreamingBubble.ReasoningRegion 行为 + `max-h-reasoning-content`/overflow-y-auto/overscroll-contain）
- [x] 3.2 `ToolCallPart`（tool-call part + 自定义 Tool UI fallback）
- [x] 3.3 `RunRecordPart`（`data-runrecord`：Activity+Plan 折叠卡，读 plan-step-store/activity feed，沉淀 timeline，默认折叠）。**实现方式（apply-time 决定，与 task 4.1 对齐）**：run-record 渲染为 rail **head section**（`ChatPanel` 中 `activityRail`=`<ActivityRail variant="compact">`，由 `showActivityRail=true` 在单轴 rail 启用，置于 thread 上方固定头）而非注入 assistant-ui timeline 的 `data-runrecord` part。task 4.1 同时描述了"inline run-record" 与"data part"两种形态；Phase 1 取 rail-section（更低盲改风险、run-record 常驻可见），data-part 注入 timeline 留作 Phase 2 与舞台 Live 浮层对齐时再评估。
- [x] 3.4 `DeliverablePart`（`data-deliverable`：复用 `DeliverableCard variant='compact'`，乱序按 createdAt 匹配）。**实现方式（同上，与 task 4.1 `.conv-outputs` 对齐）**：thread-scoped deliverables 渲染为 `ChatPanel` 的 `.conv-outputs` section（thread 下方、composer 上方，`useDeliverables(activeThreadId)` + 复用 `PitchHall`，>0 才显示）而非 per-message data part。旧的 per-message `deliverablesByMessageId` 关联已移除。
- [x] 3.5 answer text part `max-h-stream-content`/overflow-y-auto/overscroll-contain；speaker badge（读 nodeName metadata）；需要分组相邻 part 时用 `MessagePrimitive.GroupedParts`（不用 deprecated 的 `ToolGroup`/`ReasoningGroup`/`ChainOfThought` 子字段）

## 4. 右栏单轴 + composer

- [x] 4.1 `RightSidebar.tsx`：删 Chat/Inspector/Tasks/Git 四 tab（含 Tasks tab 内 Activity/Plan/Board 堆叠 section——它们是 stacked `<section>` 不是内层 tab）；改单列 `.chat-head` + Thread(`ThreadPrimitive.Viewport` autoscroll) + inline run-record(Activity+Plan) + `.conv-outputs`(useDeliverables) + `.composer`；删 `<Tabs>`/`<TabsContent>`/`TABS_RETAIN_STATE_CLASS`/`data-[state=inactive]:hidden` 相关。**已做**：RightSidebar 重写为单轴——Workspace header(search+project+summary slot) → `ThreadList`(SSOT `onSelectThread`) → chatPanel(整条对话列)。删全部 `<Tabs>`/`TabsContent`/`TABS_RETAIN_STATE_CLASS`/4 个 TabsTrigger + 不再 import usePlanStepStore/useDeliverables/usePipelineStage/useAgentStates/TaskDashboard/PitchHall/ActivityRail。Inspector→Personnel；Git widget→Phase 2 左栏；Board→topbar kanban chip。run-record + conv-outputs 移进 ChatPanel（3.3/3.4）。**Plan(TaskDashboard) 单独显示在 Phase 1 暂收进 activityRail 的 Activity 流，独立 Plan 面板留 Phase 2 run-axis Board 处理**。
- [x] 4.2 `ChatPanel.tsx` 改写：包 `OffisimAssistantRuntimeProvider` + `ThreadPrimitive`；保留 conversationKey 路由 / direct target guard / interaction respond 链
- [x] 4.3 composer：`ComposerPrimitive.Root` 内嵌 `ChatInput`(Tauri 拖放/textarea/提及) + slash 菜单 + staged attachment chip 行 + footer `SessionModeChip`(左) + send/yolo(右,yolo=danger)；附件走 Tauri 管线不接 assistant-ui adapter。**实现方式（apply-time 决定，更低风险、更忠实"保留 Offisim composer"）**：assistant-ui 只承担**消息渲染**（Viewport/parts/markdown）；发送仍走现有 `ChatInput → ChatPanel.handleSend → store`（slash/Tauri 拖放/textarea/SessionModeChip/附件 staging 管线**零改**，全保留在 ChatPanel 既有 composer 区）。**不**把 ChatInput 塞进 `ComposerPrimitive.Root`，避免双 composer state；adapter 的 `onNew` 仍 fire-and-forget 接 `handleSend` 作 fallback（assistant-ui composer 若被触发也能工作）。附件继续 Tauri 管线、不接 assistant-ui attachment adapter（设计明确要求）。
- [x] 4.4 markdown 用 assistant-ui markdown 替换 `MarkdownContent`；autoscroll 用 Viewport；评估 LinkPreview 是否补自定义 part（缺则保留自研）。**已做**：thread 消息正文走 assistant-ui `MarkdownTextPrimitive`（`markdown-text.tsx`，remark-gfm），legacy `MarkdownContent` / `MessageBubble` / `StreamingBubble` 文件和 public exports 已移除，避免新旧 chat rendering 并存；autoscroll 走 `ThreadPrimitive.Viewport`（删 ChatPanel 手动 `messagesViewportRef`+near-bottom scroll effect）。**LinkPreview**：assistant-ui markdown 渲染链接为标准 `<a>`；自研 LinkPreview 卡片本 phase 未补自定义 part（留 follow-up，链接仍可点击，不阻塞主路径）。

## 5. 验收 gate

- [x] 5.1 串行 build：`shared-types → ui-core → core → ui-office → desktop-renderer → desktop`。**已做**：shared-types(union 改) + ui-office build ✓ + renderer vite build ✓ + tokens:check ✓ + tokens:lint-hex ✓。`desktop`(Tauri Rust release) 归最终一次性 release 验收（user/Codex）。
- [x] 5.2 `pnpm typecheck` 全绿（含 chat-session-store 公开 API 未变、CollaborationRail 消费未破）
- [ ] 5.3 release `.app` live（用户/Codex）验主路径：发送 / 流式正文增长 / 多-speaker 分段 + speaker badge / reasoning 折叠 / 工具调用 / run-record 沉淀 / deliverable 卡 / 附件 staging+发送 / slash / mode 切换 / thread 切换+rename / 中止 / team↔direct 路由 / 单轴右栏无 CLS。对照 `chat-streaming-ux` 不变量逐条核。**BLOCKED 2026-05-24**：release `.app` 已用当前 worktree 精确路径构建并启动，但本机处于 macOS 锁屏界面；CGWindow 可见 Offisim 窗口存在，Computer Use 附着返回 `cgWindowNotFound`，无法完成 release live 视觉/交互验收。解锁后必须用同一 `.app` 路径补跑，不得用 dev server 或浏览器替代。重点 live 核：streaming 正文在 assistant-ui running message 内增长、多-speaker 分段不被 `joinStrategy:'none'` 外的逻辑合并、reasoning 折叠、附件发送、mode chip、thread 切换。**已知 apply-time 偏离需 live 重点确认**：run-record/outputs 现为 rail head/`.conv-outputs` section（非 in-timeline data part）。
  - **PARTIAL LIVE VERIFIED 2026-05-24**：release `.app` 已附着成功；进入 Office 不再触发 React #185 / ErrorBoundary；team chat composer 可见，assistant-ui slash command popover 触发后不崩；right rail 以单 conversation axis 渲染而非旧 Chat/Tasks tab shell。未勾本 task，因为发送、streaming、多-speaker、tool/reasoning、附件、thread rename/switch、中止等仍需真实 run 流程逐条验。
- [ ] 5.4 archive gate 三查 + 协议台账（assistant-ui 接入 → 更新台账相应行）

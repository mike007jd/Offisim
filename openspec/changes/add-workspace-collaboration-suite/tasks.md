## 1. 接入第 5 peer workspace

- [x] 1.1 `WorkspaceKey` 加 `'workspace'`（personnel 后）；`Header.tsx` nav pill + `PEER_WORKSPACE_ITEMS`；同步 6 旧 workspace 不回归
  - **已做**：`apps/desktop/renderer/src/components/workspaces/types.ts` `WorkspaceKey` union 加 `'workspace'`（personnel 后）+ `WorkspaceAppKey = 'messenger' | 'approvals'` + `WorkspaceSuiteSessionState` + `SessionStateKeyMap`/`SESSION_KEY`/`createDefaultWorkspaceSuiteState`/默认工厂。`lib/workspace-navigation.ts` `PEER_WORKSPACE_ITEMS` 加 `{ key: 'workspace', label: 'Workspace', icon: LayoutGrid }`（display 顺序按 prototype = Office→Workspace→SOPs→Market→Personnel）。`packages/ui-office/src/components/layout/Header.tsx` 本地 `WorkspaceKey` union + `NAV_PILL_KEYS` set + `workspaceHref` 全加 `'workspace'`（→ `/workspace`）。`apps/desktop/renderer/src/hooks/useAppKeyboardShortcuts.ts` 的 exhaustive workspace switch 加 `workspace` case；`OnboardingTour.workspaceLabel` + `TourWorkspace` union（tour-steps.ts）加 `'workspace'`（无 tour step 指向它）。`AppMainShell.WORKSPACE_TITLES` 加 `workspace: 'Workspace'`。6 旧 workspace 全链路（nav/routing/tier/session/keyboard/tour）兼容，无回归。
- [x] 1.2 `lib/url-routing/` parser+serializer 加 `workspace` + `?app=messenger|approvals|docs|calendar|meetings|contacts|workplace`；Back/Forward round-trip
  - **已做**：`url-routing/types.ts` `WorkspaceRoute` 加 `{ kind:'workspace'; workspace:'workspace'; activeApp:WorkspaceAppKey }`。`parser.ts` `WORKSPACE_APPS` 覆盖 7 个 suite app + `parseWorkspacePath(search)` 读 `?app=`（非白名单 fallback `messenger`）+ switch case `'workspace'`。`serializer.ts` `serializeWorkspaceUrl`（messenger 走裸 `/workspace`，其他 app 加 `?app=`）+ `primaryIdentity` workspace 分支 + `serializeUrl` 分支。`merge.ts` 加 `workspace` slice 合并。`index.ts` 导出 `parseWorkspacePath` / `serializeWorkspaceUrl`。`urlRequiresCompany` 已对所有非 office workspace 返回 true（覆盖 workspace）。Back/Forward 走 URL parser→`applyParsedUrl`→`mergeSessionPatch`（已含 workspace slice）round-trip。
- [x] 1.3 `WorkspaceRouter` 挂 suite；消费 `useLayoutTier()` tier 布局
  - **已做**：`WorkspaceRouter.tsx` import `WorkspaceSuite` + `useSuiteEscape` from `@offisim/ui-office/web`，新增 `activeWorkspace==='workspace'` 渲染分支 + suite session-state writers（`handleSuiteAppChange`/`handleApprovalsFilterChange`/`handleApprovalsSelectHistory`，全走 `updateWorkspaceState('workspace', …)`）+ `useSuiteEscape` 接 Escape drill-back。tier 布局：suite shell 走固定 `grid 64px 1fr` + `split 320px 1fr`（desktop product floor minWidth≥1024，narrow 只作 renderer 不变量）；workspace 与其他 peer 同样消费 `useLayoutTier()` SSOT（topology 不分叉，cosmetic 由 Tailwind）。

## 2. suite shell

- [x] 2.1 `components/workspace/`：`.suite`（64px app-rail，Messenger / Approvals / Docs / Calendar / Meetings / Contacts / Workplace + 选中态 + list/detail body）；Escape 在 suite 内 drill-back
  - **已做**：`packages/ui-office/src/components/workspace-suite/WorkspaceSuite.tsx`（64px app-rail 点亮 7 个 app；Approvals entry 带 pending count badge；选中态 `ring-1 ring-inset ring-accent-ring`；body 切 MessengerApp / ApprovalsApp / 5 个 framework surface）。`suite-types.ts` 自有 `WorkspaceAppKey`（ui-office 不依赖 renderer 类型）。`useSuiteEscape` hook：approvals resolved-detail → list drill-back（messenger 无 suite-local 选中，clamp Office SSOT）。Docs/Calendar/Meetings/Contacts/Workplace 已有同一 list/detail/empty surface，深度数据绑定仍不能计为完成。导出经 `web.ts`（`WorkspaceSuite`/`useSuiteEscape`/`WorkspaceSuiteProps`/`SuiteAppKey`），无新 subpath export（复用已 aliased 的 `@offisim/ui-office/web`）。

## 3. Messenger（深·chat_threads）

- [x] 3.1 list：`chat_threads` by `updated_at DESC` + 搜索；team/direct/system channel 分支（conversationKey 解析）
  - **已做**：`useMessengerConversations.ts` 读 `repos.chatThreads.listByProject(projectId)`（repo 已 `updated_at DESC`）→ team 会话；订阅 `chat_thread.updated` event（filter projectId）live refresh。`MessengerApp.tsx` list = team threads（`chat_threads`，conversationKey `<projectId>::<threadId>` 空 employee 段）+ System bot channel（synthetic readonly）+ Direct 区（`useAgentStates()` 员工 roster，conversationKey `<projectId>::<threadId>::<employeeId>`）；搜索 filter team title + employee name。
- [x] 3.2 detail：复用 Phase 1 chat message/composer 组件（不新造 chat 引擎）；system channel readonly（NotificationCenter 复表面）
  - **已做**：team/direct detail 直接复用 `ChatPanel`（module-level zustand `useChatSessionStore` 是全局 store，与 Office 同一会话源，天然共享 SSOT；ChatPanel 已含 assistant-ui message/composer/InteractionPrompt）。`showPipelineProgress=false`/`showMeetingPanel=false`/`showActivityRail=true`。System channel = `SystemChannel.tsx`：`useNotifications()` 同源 NotificationPayload + 复用 `NotificationCard` 渲染，readonly footer（"actions live on each card"），mark-all-read + open Activity Log header actions。**确认无新 chat 引擎、无新表**。
- [x] 3.3 thread 选择 clamp 到 Office `OfficeSessionState.selectedThreadId` SSOT（`updateWorkspaceState('office', …)`，不另起独立 setter；如需解耦必须显式声明 suite-scoped 选中字段）
  - **已做**：Messenger 的 thread 选择走 `workspaceSuiteProps.onSelectThread` = App.tsx 的 `handleSelectThread`（`updateWorkspaceState('office', prev => ({ ...prev, selectedThreadId }))`，App.tsx:395），direct 选择走 `onSelectDirectEmployee` = `officeBindings.handleSelectEmployee`（Office `selectedEmployeeId` SSOT）。**未另起任何 setSelectedThreadId**。selection model：`systemOpen` 是 suite-local（System channel）；team vs direct 从 Office SSOT 派生（`selectedEmployeeId` 有=direct，无=team）。Office 与 Messenger 共享同一 selected-thread + selected-employee，无漂移。

## 4. Approvals OA（深·既有 interaction 表）

- [x] 4.1 list：`active_thread_interactions`(pending) + `interaction_history`(resolved) 按 4 kind（`permission_request` / `plan_review` / `agent_question` / `skill_install_confirm`）分组 + To-do/Done 过滤（To-do = pending；Done = resolved；无 CC —— interaction 模型无 recipient 概念）
  - **已做**：`useApprovalsInbox.ts`。`active_thread_interactions` / `interaction_history` 无 company-wide 列举接口（per-thread keyed），故 fan-out：`repos.threads.findByCompany(companyId)`（graph_threads = 真实 ThreadRepository，返回 `GraphThreadRow[]`）枚举公司全部 graph thread → `Promise.all(activeInteractions.findByThread(tid))` + `interactionHistory.listByThread(tid, {limit:25})` 聚合；解析 `request_json` 成 `InteractionRequest`，pending 按 createdAt desc / resolved 按 resolvedAt desc。订阅 `interaction.requested` + `interaction.resolved` event 前缀 live refresh。`ApprovalsApp.tsx` list = To-do(pending) / Done(resolved) segmented filter；To-do 按 4 kind 顺序分组渲染 kind tag + title + from(员工 avatar via `EmployeeAvatar` + name·role)。**无 CC tab**（design D4：interaction 无 recipient/approver 字段，唯一 actor-ish `requestedByNode` 是发起 graph node 非人类收件人；本地单用户人类恒为唯一审批人）。**确认无新表、无引擎改动**。
- [x] 4.2 detail + 审批表单：`permission_request`（`tool_permission_approvals` allow/ask/deny scope）/ `plan_review`（approve/reject）/ `agent_question`（option/freeform）/ `skill_install_confirm`（confirm）；动作走现有 interaction resolve 路径（不改引擎、不双写）
  - **已做**：审批动作走**现有** `respondToInteraction`（`useOffisimRuntimeInteraction()`）→ `InteractionService.resolve()`。**真实引擎约束**：`InteractionService.resolve()` 只能解析当前 in-memory `pending`（绑定单一 runtime threadId，`interaction-service.ts:255-257` 校验 `interactionId`+thread）——无法按 id 解析任意 cross-thread 历史项。诚实落地：选中的 pending entry **若是当前 live `pendingInteraction`（同 interactionId + 同 threadId）** → 渲染既有 `InteractionDecisionCard`（permission allow/ask/deny scope·plan approve/reject·question option/freeform，4 kind 表单全由该组件按 `request.kind`/`options` 驱动）/ `SkillInstallConfirmBubble`（skill_install_confirm，confirm/fork/edit），`onRespond=respondToInteraction`（真实 resolve，emit `interaction.resolved` → 该 thread run 解阻塞 + tool_permission_approvals 落 grant）。**否则**（cross-thread queue 项，runtime 未在此阻塞）→ read-only request 摘要 + "Open in chat" CTA（`onOpenThread`=clamp Office `selectedThreadId` 到该 thread，让其 live gate 进入可解析态）——符合 prototype "OA 是 cross-thread queue，nothing waits unseen" 语义，**不另起第二条 resolve 通道、不双写、不改引擎**。Done detail = read-only resolution 摘要（status/decision/note）。

## 4.5 Docs / Calendar / Meetings / Contacts / Workplace

- [x] 4.5.1 7-app rail + URL/session 状态完成，5 个非深度 app 保持统一 list/detail/empty/error framework surface，不再是隐藏 follow-up
- [ ] 4.5.2 Docs 绑定 project workspace files + deliverables；Calendar 绑定 approvals/runtime events；Meetings 绑定 live meeting/session transcript；Contacts 绑定 Personnel identity/status；Workplace 绑定 Activity notices/posts

## 5. 验收 gate

- [x] 5.1 串行 build + `pnpm typecheck`（扩 WorkspaceKey 后 nav/routing/tier 无悬空；无新 DB 表）
  - **已做**（全 exit 0）：`rm -rf packages/ui-office/dist + tsbuildinfo && pnpm --filter @offisim/ui-office build` ✓ / `pnpm --filter @offisim/ui-office typecheck` ✓ / `cd apps/desktop/renderer && npx tsc --noEmit` ✓ / `pnpm --filter @offisim/desktop-renderer build` ✓ / `pnpm tokens:check` ✓ / `pnpm tokens:lint-hex` ✓（arbitrary `shadow-[…]` 改 `ring-1 ring-line`）/ `pnpm typecheck`（25 项目全绿）✓。Biome check 新增/改动文件全绿（import 排序 + confusing-void-type 已修）。**确认无新 DB 表**（全复用 `chat_threads`/`graph_threads(threads)`/`active_thread_interactions`/`interaction_history`/`tool_permission_approvals`），**无 core/shared-types schema 改动**。
- [ ] 5.2 release `.app` live（用户/Codex）：5th pill + routing / suite app-rail（Messenger + Approvals 两 entry）切换 / Messenger 真实 chat_threads 会话且与 Office 同步选中 / Approvals 4 审批真实数据 + 审批动作生效 / tier 响应式 / 6 旧 workspace 不回归
  - **BLOCKED 2026-05-24**：release `.app` 已用当前 worktree 精确路径构建并启动，但本机处于 macOS 锁屏界面；CGWindow 可见 Offisim 窗口存在，Computer Use 附着返回 `cgWindowNotFound`，无法完成 release live 视觉/交互验收。解锁后必须用同一 `.app` 路径补跑主路径（5th pill / app-rail 切换 / Messenger team+direct+system + 与 Office selectedThread 同步 / Approvals 4 kind 真实数据 + live pending 审批动作生效 + cross-thread Open-in-chat / tier / 6 旧 workspace 无回归），不得用 dev server 或浏览器替代。
- [ ] 5.3 archive gate 三查（确认无偷加 DB 表）+ 更新 CLAUDE.md Workspace IA（6→7 workspace，注明 deep half 已落、四浅 app + 第 7 app 在 follow-up）
  - 待 5.2 live verify 通过后做 archive gate；CLAUDE.md Workspace IA 表已更新（7 workspace + Workspace deep-half 行）。archive 留未勾直至 live 证据回填。

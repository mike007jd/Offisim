# 2026-03-24

## Offisim QA 项目启动

### 背景
用户要求作为首席 QA 工程师对 Offisim (AI Company Simulator / AICS) 项目进行深度测试。

### 第一阶段完成内容
- 项目架构理解文档已由前一助手完成
- 系统为 pnpm monorepo，包含 apps/(web, desktop, market, platform) 和 packages/(core, renderer, ui-*, install-core, asset-schema, db-*)
- 核心：LangGraph 编排的多智能体协作系统
- 关键契约：aics_manifest.schema.json, aics_install_state_machine.md, aics_openapi.yaml, SQL schemas

### 发现的高风险区域（待测试）
1. Employee Node 并发逻辑 (packages/core)
2. Materializer Promise 陷阱 (packages/install-core)
3. Rollback 未完全集成
4. Plan Cache 内存泄漏
5. tauri-repos.ts 镜像同步负担
6. AicsRuntimeProvider 异步初始化竞态
7. slug 生成竞争条件
8. 评论聚合非事务性更新

### Layer 1: 契约合规测试 - 进行中

#### 类型检查 (typecheck)
- **结果**: `@aics/core` 构建失败，存在 6 个 TypeScript 错误
- **已知问题** (与 CLAUDE.md 一致):
  - `project-assignment.test.ts`: null 不是 `string | undefined` 的子类型
  - `employee-version-service.test.ts`: Mock 泛型不兼容
  - `get-signal.test.ts`: 未使用的 `@ts-expect-error`

#### 测试运行 (test)
- **结果**: 14 个包成功，1 个失败 (`@aics/core#build`)
- **注意**: 测试本身通过，但 build 阶段失败阻止了完整测试套件运行

#### Migration 幂等性
- **检查**: 使用 `CREATE TABLE IF NOT EXISTS` 模式
- **结果**: ✅ 符合幂等性要求

#### Schema 验证
- **问题**: AJV 使用 `strict: false`，导致 "uri" 格式被忽略
- **影响**: `distribution.source_url` 的 URI 格式未验证

#### State Machine 测试
- **覆盖**: 完整的状态转换测试存在
- **结果**: ✅ 所有合法/非法转换均有测试

#### `any` 类型逃逸
- **统计**: 47 处使用 `any` 类型
- **高风险区域**:
  - `packages/core/src/agents/pm-planner-node.ts`: `any significant query word`
  - `packages/core/src/repositories/memory-memory-repository.ts`: 注释中的 "any"

### Layer 1: 契约合规测试 - 完成

## Layer 1: 契约合规测试

### 新增测试
无（现有测试覆盖不足，无法增量）

### 发现

| # | 类型 | 位置 | 描述 | 严重度 |
|---|------|------|------|--------|
| 1 | ❌ Bug | packages/core/src/__tests__/unit/project-assignment.test.ts:49,50,58,59,267,268 | `null` 不能赋值给类型 `string \| undefined`。Drizzle 模式定义 `persona_json` 和 `config_json` 为隐式非空，但 SQL schema 允许 NULL | HIGH |
| 2 | ❌ Bug | packages/core/src/__tests__/unit/employee-version-service.test.ts:208,239 | Mock 泛型不兼容：`Mock<(fn: () => unknown) => unknown>` 不能赋值给 `<T>(fn: () => T) => T` | HIGH |
| 3 | ❌ Bug | packages/core/src/__tests__/unit/get-signal.test.ts:16 | 未使用的 `@ts-expect-error` 指令（代码已修复但 directive 残留） | LOW |
| 4 | ⚠️ Risk | packages/asset-schema/src/validate.ts:13 | AJV 使用 `strict: false` 导致 "uri" 格式被忽略，`distribution.source_url` 未验证 | MED |
| 5 | ⚠️ Risk | packages/install-core (multiple tests) | "unknown format 'uri'" 警告表明 manifest 验证不完整 | MED |
| 6 | 💡 Gap | 全局 | 47 处 `any` 类型使用，部分位于生产代码中（如 `pm-planner-node.ts` 注释） | LOW |
| 7 | 💡 Gap | apps/platform/src/routes/market.ts | `buildListingDetail` 中多次使用 `as Record<string, unknown>` 强制类型转换，绕过 Zod 验证 | MED |
| 8 | ✅ Good | Docs/03_migrations | 所有 migration 使用 `CREATE TABLE IF NOT EXISTS`，符合幂等性要求 | - |
| 9 | ✅ Good | packages/install-core/src/__tests__/state-machine.test.ts | 状态机转换测试完整，覆盖所有合法/非法路径 | - |

### 覆盖率
- 变化: 未知 → 待测量（需要运行 coverage 报告）
- 类型检查: 16 个包中 1 个失败 (`@aics/core#build`)
- 测试执行: 14/15 包成功，1 个 build 失败

### 关键问题

1. **`@aics/core` 构建失败** - 这阻止了 `core` 包的完整测试套件运行
2. **Schema 验证不完整** - URI 格式未被 AJV 验证
3. **类型与运行时行为不一致** - Drizzle 类型说 `string | undefined`，但 SQL 允许 NULL